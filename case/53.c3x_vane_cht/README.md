# case/53 C3X タービン翼 (CHT の公知データ検証 V5 — 亜音速出口)

**壁温が結果として測られている**公知試験に対して forge の共役熱伝達を当てる。
主対象は超音速出口の Mark II ([case/54](../54.markii_vane_cht/)) で、**本 case はその足場** (亜音速出口)。
計画は [`plans/active/boundary-conjugate-heat-transfer.md`](../../plans/active/boundary-conjugate-heat-transfer.md) §4.9 / §6 V5、
選定理由は [`notes/investigations/cht-validation-case-survey.md`](../../notes/investigations/cht-validation-case-survey.md)。

## 一次資料

**NASA CR-168015** (Hylton, Mihelc, Turner, Nealy, York, 1983; NTRS **19830020105**)。
PDF は `papers/cht/NASA-CR-168015_Hylton_1983.pdf` (**git 追跡外**。再取得は
`curl -L -o papers/cht/NASA-CR-168015_Hylton_1983.pdf https://ntrs.nasa.gov/api/citations/19830020105/downloads/19830020105.pdf`)。

抽出は [`tools/extract_vane_data.py`](tools/extract_vane_data.py)。**スキャン PDF の OCR なので検証してから使う**:

- 表 II / III は **cm と inch の両方**を載せているので、全点に `cm == 2.54 × in` (相対 0.5 %) を課す。
  通らない点は**捨てて**、ページ画像を目視して読んだ値 (`REPAIRS`、各行を同じ 2.54 則で検算済み) で埋める。
  点が 1 つでも欠けたら**書き出さずに落ちる**。
- 現状: C3X **73/78 が OCR 検証を通過、5 点を目視補修**。Mark II **54/60 通過、6 点を補修**。
  形状の自己検査: 連続点の最大間隔は C3X 0.618 cm / Mark II 1.626 cm (後者は**鈍頭前縁** $R_{LE}$=1.280 cm の
  円弧区間で、点列に無いのが正しい)。`ref/vane_shapes.png` で目視確認。

### 報告自身の不整合 (2 件、いずれも記録して回避)

1. **表 VIII / IX の SI 圧力列が psia 列と 51.7 倍ずれている** (例: C3X 4411 は "6177 (46.34 psia)" だが
   46.34 psia = 319.5 kPa)。**psia 側を正**とする (文献が引用する ~3.2 atm と一致する)。
2. **C3X 表 III の点 29 は inch 値が誤植**: "0.4115 cm (0.0162 in)" だが 0.0162 in = 0.0411 cm。
   前後の点間隔 (~0.6 cm) から **cm 値 0.4115 が幾何的に正しい** (0.0411 だと点 30 と 0.047 cm しか離れない)。

## 抽出済みデータ

| ファイル | 中身 |
| --- | --- |
| `ref/vane_c3x.csv` | C3X 翼型 78 点 (x, y [cm])。$R_{LE}$=1.168 cm, $R_{TE}$=0.173 cm |
| [`../54.markii_vane_cht/ref/vane_markii.csv`](../54.markii_vane_cht/ref/vane_markii.csv) | Mark II 翼型 60 点。$R_{LE}$=1.280 cm, $R_{TE}$=0 (blunt) |
| `ref/test_conditions.csv` | 表 VIII / IX の主要 run |
| `ref/vane_shapes.png` | 形状の目視確認図 |

主要 run (表 VIII / IX、ページ画像から読取):

| vane | code | run | $P_{T1}$ | $T_{T1}$ | $M_1$ | $Re_1$ | $M_2$ | $Re_2$ | Tu | $T_w/T_g$ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **C3X** | 4411 | **108** | 46.34 psia (319.5 kPa) | 786 K | 0.17 | 0.52e6 | **0.90** | 1.99e6 | 6.5 % | 0.73 |
| **Mark II** | 5411 | **42** | 48.89 psia (337.1 kPa) | 788 K | 0.19 | 0.56e6 | **1.04** | 2.01e6 | 6.5 % | 0.68 |

4411 (亜音速出口) と 5411 (超音速出口) は入口条件がほぼ同じなので、**出口マッハだけを変えた対**として使える。

## 抽出済み: カスケード幾何 (表 IV) と冷却孔 (図 6/7)

| 量 | Mark II | C3X |
| --- | --- | --- |
| 取付角 [deg] | 63.69 | 59.89 |
| 出口空気角 [deg] | 70.96 | 72.38 |
| スロート [cm] | 3.983 (1.568 in) | 3.292 (1.296) |
| 翼高さ [cm] | 7.620 (3.000) | 7.620 (3.000) |
| **翼間ピッチ** [cm] | 12.974 (5.108) | 11.773 (4.635) |
| 負圧面弧長 [cm] | 15.935 (6.274) | 17.782 (7.001) |
| 正圧面弧長 [cm] | 12.949 (5.098) | 13.723 (5.403) |
| 真弦長 [cm] | 13.622 (5.363) | 14.493 (5.706) |
| **軸弦長** [cm] | 6.855 (2.699) | 7.816 (3.077) |

**自己検査**: 軸弦長は座標表の $x$ 範囲と一致する (Mark II 6.8544 / C3X 7.8161 cm)。

**冷却孔** (各翼 10 本、`ref/cooling_holes_*.csv`): 直径は前方 7 本が 0.630 cm、次の 2 本が 0.310 cm、
最後が 0.198 cm。各孔に助走区間補正 $C_r$ (1.118 / 1.056 / 1.025) が付く。
図の座標系 (U,V) から翼型座標 (x,y) への変換行列は**報告に無い**ので、
**全孔が翼型内に入り最小肉厚が最大になる剛体変換を最適化して決めた** (`place_holes`)。
結果の**最小肉厚は C3X 0.164 cm / Mark II 0.159 cm** で、`ref/vane_shapes.png` に描いて確認できる。

## 計測の不確かさ (報告 p.24–27、V5 の誤差帯に積む)

| 量 | 不確かさ |
| --- | --- |
| 表面温度 (CA 熱電対、約 80 点/翼、midspan) | ±1 °C |
| 自由流ガス温度 | ±11 °C |
| 外形プロファイル | ±0.008 cm |
| 冷却孔位置 | ±0.013 cm |
| 冷却孔直径 | ±0.005 cm |
| 冷却孔の熱伝達係数 (相関 + 助走補正) | ±3 % |
| 材料熱伝導率 | ±3 % |

## 実験データの定義と自己検査 (`ref/run108_surface.csv`)

報告 p.125 (付録 A の前書き) より:

- **温度は 811 K (1460 °R) で正規化**、**熱伝達係数は $h_0$ = 1135 W/m²K (200 Btu/hr/ft²/°F) で正規化**。
- $h$ は**ガス全温 $T_g$ 基準** (p.21: 有限要素の入力は「実測外表面温度・冷却孔の熱伝達係数・2D 断面形状・
  材料熱伝導率・**ガス流の全温**・各孔の平均冷却剤温度」)。よって $q = h\,(T_g - T_w)$。
- run 108 に当てると: $T_w$ = 513–612 K、$h$ = 327–895 W/m²K、$q$ = 87–206 kW/m²。

**自己検査**: 転記した $T_w$ の単純平均は 562.4 K で、報告の $T_w/T_g$ = 0.73 ($T_{T1}$=786 K → 573.8 K) に対し
**0.716 (−1.9 %)**。平均の定義 (弧長重み・対象点の範囲) の差と考えられる。**転記ミスではない**ことの根拠として、
全 75 点が単調性・範囲の検査を通り、分布形も物理的 (前縁で $h$ 最大、負圧面で遷移して上昇) である。

## 内部条件 — **冷却剤データは公開されている** (2026-09-20 訂正)

~~冷却孔ごとの冷却剤温度・流量は報告に無い~~ → **誤り**。付録 A は run ごとに 2 ページあり、
**偶数ページが表面データ (弧長/軸距離/$T_w$/$h$)、奇数ページが表面圧力 $P_s/P_T$ と
`COOLANT FLOW DATA`** (孔ごとの平均冷却剤温度 °F/K・$Re_D$・流量 lbm/s と kg/s) である。
run 108 は報告 p.165 (PDF p.171)、run 42 は p.145 (PDF p.151)。
**表面データ表だけを見て「無い」と結論していた**。転記は [`tools/ref_data.py`](tools/ref_data.py)
(°F↔K・lbm↔kg・$Re=4\dot m/(\pi D\mu)$ の 3 つで相互検査)。

**報告の不整合 3 件目**: run 42 の hole 9 は °F 244.00 と K 360.89 が合わない
((244.00+459.67)/1.8 = 390.93)。他 9 孔は両欄が厳密一致で、°F 欄は後縁へ単調なので
**°F 側を正** (390.93 K) とする。

### それでも段 (b) は「逆算」のままである (理由)

公開されているのは**冷却剤温度と流量**で、**孔の熱伝達係数 $h_c$ そのものではない**。
報告は「孔径・実測流量・冷却剤温度から Nusselt 数を計算し $C_r$ を掛ける」としか書かず、
**どの相関かを書いていない**。Dittus–Boelter $Nu=0.023Re^{0.8}Pr^{0.4}$ に $C_r$ を掛けると
$h_c$ = 1489–2892 W/m²K (C3X) になるが、**これを固体に課すと外表面温度が実測より 30 K 冷たくなる**
(RMS 62 K、全体倍率 1 個で合わせても RMS 60 K = **分布が違う**)。

さらに**冷却孔の位置は公開されていない** (図 6/7 の (U,V) 系から剛体変換を最適化して決めた、下記)。
したがって内部側には**推定が 2 つ** (孔位置・相関) 入り、$h_c$ の同定はその誤差を吸収する。
実際、同定値と相関値の比は C3X で **0.14–4.0** とばらつく (Mark II は 0.44–1.70 でまし)。
感度解析では $\partial T_w/\partial\ln h_c$ の最小特異値が **65 K** で条件数 36 — 逆問題自体は
解けている。**ばらつきは条件数のせいではなく、モデル (孔位置) の誤差**である。

→ **V5 の扱いを次のように分ける** (2026-09-20 改訂):
- **段 (a) 実測 $T_w$ → $h$** が forge の検証の本体。内部条件を一切使わないので clean。
- **段 (c) 連成壁温**は、内部条件が推定を含むぶん **forge の検証としては弱い**。
  連成機構 (固定点に到達するか・界面熱量が保存するか) の実証と感度解析として扱い、
  実測との一致度を forge の精度として宣伝しない。
- **$T_c$ は公開値 (孔ごと) を使う** (`--Tc-published`)。これで旧稿の「$T_c$ と $h_c$ の縮退」は
  帯の項目から外れる ($T_c$ はデータであって自由度ではない)。残差床は C3X 9.98 K / Mark II 7.70 K。

## 固体メッシュと内部条件の逆算 (V5 段 (b) 相当、**CFD 不要**)

- **固体メッシュ**: `tools/gen_solid_mesh.py` → `mesh/solid_c3x.npz`
  (4127 節点 / 7712 三角形、外周 **240 節点** を弧長等間隔に配置 = 流体メッシュと 1 対 1 で合わせるため、
  冷却孔は各 32 辺の物理曲線)。外周弧長 31.98 cm (表 IV の SS+PS = 31.505 cm に対し +1.5 %。
  復元した前縁/後縁円弧の分)。
- **内部条件の逆算**: `tools/infer_internal_bc.py` → `ref/run108_internal_bc.{json,png}`。
  外表面に $q = h\,(T_g-T_w)$ (実測) を Neumann で課し、孔ごとの $h_c$ を最小二乗で同定する。

**結果 (run 108, $T_c$=300 K, $k_s$=17.2 W/mK)**:

| 量 | 値 |
| --- | --- |
| 同定した $h_c$ | **313 … 4207 W/m²K** (平均 1744)。前方の $\phi$6.3 mm 孔で 300–500、後縁側の小径孔で 2900–4200 |
| 外表面温度の残差 | **RMS 10.7 K / 最大 35.4 K** (実測 $T_w$ の幅 513–612 K) |
| 熱収支 | 外表面入熱 41.4 kW/m = 孔からの排熱 (機械精度で一致) |

**この逆算で分かったこと (V5 の設計に効く)**:

1. **$h_c$ と $T_c$ は縮退する** — データが拘束するのは概ね $h_c\,(T_w-T_c)$ の積。
   $T_c$ を 280→350 K と振っても RMS は 10.65→10.83 K とほぼ動かず、$h_c$ の平均だけが 1595→2662 と動く。
   $T_c \gtrsim 400$ K では一部の $h_c$ が 1e5 W/m²K へ走る (局所壁温が $T_c$ に近づくため) ので**非物理**。
   → **$T_c$=300 K を代表に採り、280–350 K の幅を帯に積む**。
2. **残差床 10.7 K は弧長原点の取り方に鈍感** (原点を ±0.5 cm 動かして RMS 10.5–10.7 K)。
   つまり**マッピングの曖昧さが原因ではなく、10 孔 Robin + 2D 伝導というモデルの限界**。
   → **V5 段 (c) の CHT 予測を「実測と一致」と言えるのは、この 10 K 級の床より内側に入ったときだけ**ではなく、
   逆に**床より小さい差を主張してはいけない**。帯の下限としてこの値を登録する。
3. **後縁側 35/240 節点はデータ範囲外** ($s/S>0.87$)。外挿した熱流束を課すと固体温度が 950 K まで跳ねる
   (図の破線)。**後縁の固体温度は検証対象外**と明示する。

~~$h_c$ の妥当性: $\phi$6.3 mm 孔で Dittus–Boelter 相当は $Re_D\sim3\times10^4$~~ →
**誤り (2026-09-20)**。公開された流量から $Re_D$ は **2.2×10⁵** (7 倍大きい)。この $Re$ で
Dittus–Boelter は ~2000 W/m²K になり、同定値 (218–4706) の**前方 7 孔は 3–9 倍小さい**。
「整合する」という旧稿の主張は、間違った $Re$ 見積りに基づいていた。上節の扱いに従う。

## まだ取っていないもの (次の作業)

1. ~~**材料熱伝導率 $k_s(T)$**~~ — **`ref/material_astm310.json` に用意済み**。
   **報告には数値が無い** (「材料を指定してあるので既知」として ±3 % とだけ) ので**外部出典**を使う
   (Engineering ToolBox の AISI 310 表、0–700 °C)。実測壁温域 (240–339 °C) で **$k_s$ = 16.5–18.0 W/mK**。
   V5 の帯には **(a) 報告の ±3 %** と **(b) 出典間のばらつき**を別項目で積む
   (別の出典は 100 °C で 10.8 W/mK を載せており、表の 14.1 と 2 割違う)。
2. ~~冷却孔ごとの熱伝達係数と冷却剤温度~~ → **公開されていない** (上節)。逆算する方針に変更。
3. ~~**測定壁温・熱伝達率の分布**~~ — **run 108 は転記済み** (`ref/run108_surface.csv`, 75 点)。
   残るは **run 42 (Mark II, PDF p.150)**。付録 A (報告 p.131〜) に run ごとの表がある
   (「弧長 / 軸方向距離 / 正規化温度 $T_w/811\,\mathrm{K}$ / 正規化熱伝達係数」+ 圧力分布)。
   **run 108 (C3X) は PDF p.170–171、run 42 (Mark II) は p.150–151**。OCR が崩れているので
   **ページ画像から転記**する (座標表と同じ作法で検算しながら)。
   報告のデータ整理は**薄肉ではなく 2 次元伝導 FEM** ("a 2-D plane of the test vane as a fluxmeter") なので、
   forge 側も `fem2d` で合わせる。
4. **流体メッシュ** (翼列周期)。固体側は上記で完了。
5. run 42 (Mark II) の実測データ転記と、同じ逆算。

## 流体メッシュ

`tools/gen_fluid_mesh.py` → `mesh/fluid_c3x.{msh,h5}` (22442 節点)。翼列 1 ピッチ分の通路で、
周期境界はキャンバー線 ± ピッチ/2、翼面は `BoundaryLayer` フィールドで低 Re 解像。
**node 離散化**なので変換は `discretization=node` の config で行う ([[node-mesh-must-convert-with-node-config]])。

## 計算 run 一覧

| `run_*` | 目的・主要設定差分 | 主要結果・成果物 | 状態 |
| --- | --- | --- | --- |
| `run_0001_measTw` | V5 段 (a) 初回。入口 `inlet_uniformVelocity` (M1=0.1628 を課す)、5 段起動 (warm/sst_soft/sst_mid/ramp/main 計 59000 step) | **失敗 (使わない)**。`NOT CONVERGED (stalled/plateau)`、`rms_roOmega` 増大。入口ブロックに大きな低速塊が残り、$x$ 面の $\int\rho u\,dy$ が 12.8 / 6.1 / 15.6 と非保存、$P_t$・$T_t$ が下流ほど**上昇**。**真因は入口 BC** (下記) | 破棄予定 |
| `run_0002_ptinlet` | 同上を**全圧入口** `inlet_Pressure_dir` (Pt 319.5 kPa, Tt 786 K, 軸方向) に置換。出口静圧は損失込みで 185.06 kPa に追い込み。`run_0001` の場から継続 | **`PASS (converged)`** (全残差 3.7–3.9 dec 低下)。入口 $M_1$=0.165 (報告 0.17)、出口 $P_t$=313.0 kPa (損失 2.0 %)・$T_t$=782.5 K・$M_2$=0.881。ただし壁 $q$ が節点間で 52↔302 kW/m² と振れ、$h$ 分布が読めない (真因は**翼型の折れ角**、下記) | ref (形状修正前の比較対象) |
| `run_0003_smoothgeom` | **平滑化した翼型** (`ref/vane_c3x_smooth.csv`) から流体メッシュを作り直し (壁 480 節点, 30703 節点, `check_mesh_quality` **PASS** AR 491 / skew 0.72)。BC は run_0002 と同じ。run_0002 の場から cross-mesh restart | **V5 段 (a) の本命**。入口 $M_1$=0.167・$P_t$ 319.5 kPa・$T_t$ 786.0 K、出口 $M_2$=**0.901** (報告 0.90)・$P_t$ 313.5 kPa (損失 1.9 %)。$h$ の振動は消え、実測との差は下表。成果物 `h_compare.png` / `h_compare.npz` / `wall_series.csv` | active |

### V5 段 (a) の結果 — 実測 $T_w$ を課した $h$ (`run_0003_smoothgeom`, 2026-09-20)

$h = q_w/(T_g-T_w)$、$T_g$ = 入口全温 786 K、$h_0$=1135 W/m²K。$q_w$ は界面診断
`iface_q_compact` ($k_{\rm eff}(T_1-T_w)/d_1$)。比較はデータ範囲 $s/S\le0.87$ の 402 節点。

| 領域 | 節点数 | バイアス | RMS |
| --- | ---: | ---: | ---: |
| 正圧面 (全域) | 164 | **+10.6 %** | 21.7 % |
| 負圧面 **層流域** $s/S<0.25$ | 109 | **+40.5 %** | 53.9 % |
| 負圧面 **遷移後** $0.25\le s/S\le0.87$ | 129 | **+4.0 %** | 11.0 % |
| 全体 | 402 | +16.6 % | 31.9 % |

**読み方**: 負圧面の遷移後は **+4 %** で一致する。外れるのは**負圧面前縁の層流域**で、
forge は前縁から乱流になり、実測が $s/S\approx0.17$ まで $h/h_0$ を 0.42 まで下げてから遷移するのを
再現しない。**低 Re SST に遷移モデル ($\gamma$–$Re_\theta$ 等) が無いため**で、C3X で広く報告されている
挙動と同じ。CHT (段 (c)) の壁温予測にはこの層流域の過大評価がそのまま効くので、**V5 の帯に
「遷移モデル無し」の項目として積む**。
$q_w$ の定義差は小さい (compact 基準で 2 次片側 +3.1 %、再構成勾配 −0.05 %)。
ソルバ出力 `qwall` は `iface_q_recon` と**同値で符号が逆** (ガス側正)。

収束: `check_convergence.py` は `NOT CONVERGED (stalled/plateau)` (前 run の収束場から
cross-mesh restart したので低下幅が 1.2–2.1 dec しか無い)。**報告量の側で判定**すると
`check_quasisteady.py --series-csv wall_series.csv` が **`q_total: STEADY` (drift 0.1 %)**、
壁 $h$ 分布の step 30000→60000 の変化は **rms 1.45 % / 最大 7.6 %**、場の残る変化は前縁付近で
$|\Delta\rho|/\sigma_\rho \le 0.08$。**「残差はプラトー、報告量は定常」**として扱う。


### 負圧面後縁の壁圧 — 背圧の定義誤りと SU2 対照 (2026-09-20, plan #44)

後半 ($s/S>0.45$) の壁圧が $P_s/P_{T1}$ で実測より **$-0.026$ 低く**、後縁へ向かって拡大していた。
**同一メッシュ・同一 BC の SU2 8.5 も同じだけ低い** (下表、差 0.0002) ので**ソルバ起因ではない**。

真因は背圧の定義。報告の $M_2=0.90$ は翼列の標準定義である**入口全圧からの理想出口マッハ**
($P_{s,\rm exit}=188{,}908$ Pa) だが、`setup_run.py` は「出口面の**局所** $P_t$ から計算した $M_2$」が
0.901 になるまで追い込んでいた ($185{,}064$ Pa = 理想 $M_2$ **0.919**)。
**翼列損失 1.9 % を二重に引いて**背圧が 2.1 % 低かった。

| $s/S>0.45$ の bias (Ps/Pt) | forge 185.1 kPa | SU2 185.1 kPa | forge 188.9 kPa |
| --- | ---: | ---: | ---: |
| 負圧面 | $-0.0257$ | $-0.0259$ | **$-0.0125$** |
| 負圧面 rms | 0.0349 | — | **0.0232** |
| 正圧面 | $-0.0261$ | $-0.0263$ | $-0.0232$ |
| 負圧面 前半 ($s/S<0.45$) | $-0.0129$ | $-0.0131$ | $-0.0094$ |

訂正後も**後縁へ向かって残る欠損**があり (正圧面 $-0.0232$ / 負圧面 $-0.0125$)、これも両コードで同一。

**消した候補 (2026-09-20)**:

1. **翼型の digitise — 無罪**。報告 Table III の座標そのものから測った喉は $o/\text{pitch}=0.2881$ (出口角 **73.26°**)、
   メッシュ実測は 0.2871 (73.32°)、計算された気流角は 73.04°。三者が一致する。
   平滑化翼の表点からの最大ずれ 0.0104 cm は報告の外形不確かさ ±0.008 cm と同等。
2. **周期出口ブロックの角度 — 効かない**。`gen_fluid_mesh.py` の `exit_angle` 72.38° (表 IV 設計値) は
   報告座標が与える 73.26° と **0.88° (喉幅で 5.4 %)** 食い違っているが、
   `run_0026_exitangle_7326` の A/B で**壁圧の bias/rms は 4 桁まで同一**、出口気流角も 73.07 → 73.06°。
   流れが自分で出口角を決めるので、ブロックの向きは境界条件として効いていない。

3. **$s/S$ 定義のずれ — 説明にならない**。測点の abscissa に一律シフトを当てて rms を最小化すると、
   **負圧面は最適シフトが $-0.006$ しかなく効果も僅か** (rms 0.0261 → 0.0248)。
   正圧面は $-0.042$ で rms が半減する (0.0268 → 0.0139) が、**符号が淀み点仮説と逆**
   (計算の淀み点は幾何 LE から正圧面側へ $+0.021$ なので、報告が淀み点起点なら $+0.021$ が要る)。
   急勾配曲線への当てはめの産物とみなす。

**残る候補 (未検証)**: 2D 仮定 (端壁 BL による AVDR≠1 — forge に流管収縮を与える手段が無く、
報告も AVDR を載せていない)、鈍後縁のベース圧。**現時点では未帰属**。
ただし両コードで同一なので **forge の欠陥ではない**。plan §5.1 #44(c)。
根拠 run: `case/53.c3x_vane_cht/run_0009_uniformTw/`, `run_0025_pb_idealM2/`, `su2_run108/`。

### 壁熱流束リップルの外部レビュー結果 (2026-09-20)

[`notes/reviews/2026-09-20-codex-c3x-wall-flux-ripple.md`](../../notes/reviews/2026-09-20-codex-c3x-wall-flux-ripple.md)。
**この README と plan に書いていた診断のうち 5 件が反証された** (GG 境界閉包説・中心勾配説・
480→960 のメッシュ非収束説・$h$ と $q$ の取り違え・Mark II の相関値)。真因は**特定できていない**。
確定しているのは「後処理由来でない」「圧力連成でない」と、forge/SU2 の等温壁離散化の違い。
処置は**報告図の平滑化のみ** (積分熱量への影響 0.002 %)、収支・CHT には未加工 `q_eff` を使う。
$T_w$ の無制約スプライン化は不採用 (測点を通る spline でも最大 7.6 K / 11.5 K 動く)。


### 壁熱流束の市松 — 3 仮説の切り分け (2026-09-20)

codex 記録: [`notes/reviews/2026-09-20-codex-c3x-checkerboard-triage.md`](../../notes/reviews/2026-09-20-codex-c3x-checkerboard-triage.md)。
対象は負圧面 $s/S$ 0.30–0.80 の odd-even 振幅 (基準 `run_0009_uniformTw` で 1680 W/m² = **0.919 %**、
同一メッシュの SU2 は 84 W/m² = **0.046 %**)。

| 切り分け | 結果 | 判定 |
| --- | --- | --- |
| **対流スキーム** (当方 A/B) | SLAU 0.919 % / ROE **0.901 %** / HLLE 2.10 % | **棄却**。SU2 も ROE で 0.046 % なので、同じスキーム族で 20 倍違う = 対流離散化は原因でない |
| **低マッハ前処理** (当方 A/B + codex) | `lowMachPrecond: 2` は**破綻** (残差 2.3 桁上昇、odd-even 56.9 %)。backstep の前処理効果は**圧力** odd-even (38.2→5.6 Pa) に対するもので、本件は P が交番していない (corr $-0.04$) | **整合しない** |
| **float32** (codex) | 600 K の 1 ULP = 6.10e−5 K → $k_{\rm eff}\Delta T/d_1$ で **1.237 W/m²**。観測 0.083 K は**約 1360 ULP**、温度差 5–9 K に対し相対 $10^{-5}$ | **説明不足**。ただし残差組立ての桁落ちは別問題で未立証 |
| **熱的壁閉包** (codex) | 壁 $T$ ピン + 壁エネルギー残差ゼロ化 + 陰解法エネルギー行の単位行化 | **残る有力候補 (未検証)** |

**注意 (ROE/HLLE run の制約)**: どちらも `NOT CONVERGED` (rms_ro 1.4e−2、SLAU 基準は 2.3e−7)。
収束場と未収束場の比較なので参考値であり、「ROE でも同じ」は**同程度の振幅が出る**ことまでを言う。

**次の A/B (codex 設計)**: 壁 $T$ ピン・壁エネルギー残差ゼロ化・陰解法エネルギー行拘束を**一組で解除**し、
指定 566 K と第一内部点から SU2 型の壁熱流束を整合する符号・面積・線形化で与える。
**`nodeWallDirichlet: 0` 単独では不可** (運動量条件まで変わり、`viscousFlux_d.cu:529` が壁勾配を使う)。
効いても**一式の効果までしか確定しない**。plan §5.1 #43。


### 弱形式等温壁 (SU2 型) の A/B — 市松は悪化 (2026-09-20)

plan [`boundary-weak-isothermal-wall`](../../plans/active/boundary-weak-isothermal-wall.md)。
`mesh.nodeIsothermalEnergyBC: 1` で壁ノード $T$ のピン・エネルギー残差ゼロ化・陰解法エネルギー行の
単位行化を**一組で外し**、壁半割面の伝導を $k_{\rm eff}(T_I-T_w)/d_1\cdot A_{\rm half}$ で置換する。

| | 壁ノード $T_W$ | odd-even ($s/S$ 0.30–0.80) | 第一内部点 $T$ の交番 |
| --- | --- | ---: | ---: |
| A 強制 (`run_0009_uniformTw`) | 566.000–566.000 K | **0.9186 %** | 0.0827 K |
| C 弱形式 (`run_0039_weakbc`) | **566.001–566.831 K** | **1.2092 %** | 0.1120 K |
| SU2 (参考、同一メッシュ) | 566.011–566.886 K | 0.046 % | — |

$T_W$ が SU2 とほぼ同じ範囲に落ち着いたので**実装は SU2 の挙動を再現している**。
にもかかわらず**市松は 32 % 悪化**した。`check_convergence.py` `NOT CONVERGED`、`rms_roK`/`rms_roOmega` **RISING**。

**判定: 熱的壁閉包一式を SU2 型に替えるだけでは市松は解消せず、むしろ悪化する。**

#### 決定的事実 — 強制側では壁半割面の流束が解に入らない (`run_0040_wallflux0`)

強制のまま**壁半割面の熱流束だけを 0** にしても、場は run-to-run ノイズ床の **0.70–0.85 倍**しか変わらない。
`zero_res_roe_bplane_d` ([`nodeWallDirichlet_d.cu:89`](../../solver_density_cuda/cuda_forge/nodeWallDirichlet_d.cu)) が
壁ノードの `res_roe` を 0 にし、陰解法がエネルギー行を単位行にするため、`viscousFlux_wall_d` の寄与は
**まるごと捨てられている** (診断 `ifaceFw`/`qwall` にしか出ない)。

したがって**強制側の市松は壁 BC の流束離散化からは原理的に生じ得ない**。
ただし**壁温ピン自体は内部勾配に効く**ので「壁 BC 全体が無関係」とまでは言えない
(反例: cell はゼロ化対象外、`Qw_Wall` の W–I 置換は内部点側に残る。codex 2026-09-20)。

#### 切り分けの現状

| # | 候補 | 結果 |
| --- | --- | --- |
| ① | 対流スキーム | ROE 0.901 % / HLLE 2.10 % vs SLAU 0.919 % — **外れ** (SU2 も ROE) |
| ② | 低マッハ前処理 | 破綻 (残差 2.3 桁上昇)。P が交番していないので機構も不整合 — **外れ** |
| ③ | float32 | 温度丸めは外れ (0.083 K = 1360 ULP)。`pRef: 0` と陰解法倍精度も不変だが、**これは特定の 2 経路の試験**であって残差組立て全般ではない — **float32 全体は未棄却** (codex 2026-09-20) |
| ④ | 熱的壁閉包 | 弱形式で 0.919 → 1.209 % と **32 % 悪化** — **外れ** |
| ⑤ | 非直交補正の形 | SU2 型で 0.9186 → 0.9159 % (相対 0.3 %) — **外れ** |
| ⑥ | 勾配・制限関数 | 設定名は同一 (WEIGHTED_LEAST_SQUARES + Venkatakrishnan K=0.05) だが、**設定名の一致は離散演算子の一致を意味しない** (近傍集合・重み・再構成変数・壁での扱い・長さ尺度は未照合。codex 2026-09-20 指摘) — **未棄却** |

**残るのは ③ の残差組立て (float32)** — codex が唯一「未検証」と留保した項目。倍精度ビルドで A/B する。

**設計上の訂正 (2026-09-20)**: 当初「A↔C は流束形と拘束形が交絡している」と書いたが**誤り**。
**強制側は壁ノードの `res_roe` をゼロ化するので壁半割面の流束は解に一切入らない** (診断にのみ出る)。
したがって A↔C の差は拘束一式だけで交絡はない。一方**弱形式では壁半割面の流束が初めて解に効く**ので、
その離散化が残る唯一の未検証変数になる (plan §5.1 #6b)。
初版は純粋な 2 点差分で、forge が内部面で使う **over-relaxed 非直交補正が入っていない**。
**弱形式のまま $\nabla T\cdot S$ に戻すのは不可** ($T_W$ が自由だと指定 $T_w$ がどこにも入らない = 旧弱形式)。

**既定パス不変の確認**: node の C3X は `atomicAdd` で **run-to-run 非決定**なので「ビット同一」は不成立。
両側 4 反復でノイズ床を測り、RMS 差の中央値で交差/群内 = **0.91–0.97** (同一 population)。
交番振幅自体は再現的で旧 0.9112 % / 新 0.9111 %、幅 **0.0001 %**。


### 市松の切り分け 第 2 ラウンド (2026-09-20 夜) — 全候補が外れる

**全 run はクリアンビルド・同一シード (`run_0009` の収束場)・2000 step で揃えた。**
基準は負圧面 $s/S$ 0.30–0.80 の odd-even (有意差閾値 0.001 %、run-to-run 幅 0.0001 % の 10 倍)。

| run | 変更点 | odd-even | 第一内部点 $T$ |
| --- | --- | ---: | ---: |
| `run_0051_ctrl_recheck` | SLAU 既定 (対照) | **0.9106 %** | 0.0819 K |
| `run_0052_slau_heatcorr` | 内部面の熱伝導を SU2 corrected-gradient へ | 0.9107 % | 0.0820 K |
| `run_0053_slau_pref0` | `pRef: 0` (float32 桁落ち対策 OFF) | 0.9109 % | 0.0820 K |
| `run_0054_weak_clean` | 弱形式等温壁 (SU2 型) | **1.2066 %** | 0.1115 K |
| `run_0042_implicitdbl` | 陰解法の線形 solve を**倍精度** | 0.9165 % | 0.0825 K |
| `run_0049_roe_clean` | ROE | 1.2547 % | 0.0634 K |
| `run_0050_roefix_clean` | ROE + エントロピー床 0.001 (SU2 既定) | 1.2257 % | 0.0635 K |
| `run_0055_roefix0p05` | ROE + 床 0.05 | 1.6527 % | 0.0738 K |
| `run_0056_roefix0p2` | ROE + 床 0.2 | **3.0331 %** | 0.0912 K |

**エントロピー波の散逸を増やすほど交番は悪化する** (単調) ので、「$M\to0$ で接触波の上流化散逸が消えるのが原因」
という説も棄却。`pRef: 0` は桁落ちを**意図的に悪化**させる方向なのに不変で、float32 相殺説も否定。
陰解法の倍精度化も不変。

### 測り方は公平だった — 20 倍は温度場そのものにある (2026-09-20)

「forge は `iface_q_compact` ($k_{\rm eff}(T_1-T_w)/d_1$)、SU2 は `Heat_Flux` と**別定義**なので
比較が不公平では」という疑いを潰した。**完全に同じ節点** (座標差 **0.00 m**)、**同じ量** ($T$) で:

| | 第一内部点 $T$ の odd-even | 平均 |
| --- | ---: | ---: |
| forge | **0.08268 K** | 575.03 K |
| SU2 | **0.00414 K** | 575.09 K |

**比は 20.0 倍**で、熱流束の比 (0.919 / 0.046 = 20) と一致する。
平均温度は 0.06 K 差 = **解そのものは一致**しており、**節点間の交番だけ**が 20 倍違う。
したがって流束の定義・区間の取り方・指標の定義の問題ではなく、**温度場に実在する差**である。

再現: 壁節点ごとの第一内部点を「壁でない最近傍節点」として取り (距離 1.989–2.012 µm)、
SU2 の同一座標節点の `Temperature` と突き合わせる。

### 層別プロファイルとスペクトル — 2 節点モードは実在し、温度だけにある (2026-09-20)

壁から外向きに層を辿り、**両コードの同一節点**で交番を測った (SU2 節点との距離 0.00 m)。

| 層 | $y$ [µm] | forge $T$ [K] | SU2 $T$ [K] | 比 | forge $P$ [Pa] | SU2 $P$ [Pa] |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0 (壁) | 0.00 | 0 (ピン) | 0.00077 | — | 27.84 | 20.58 |
| 1 | 2.00 | **0.08268** | 0.00414 | **20.0** | 27.54 | 19.81 |
| 2 | 4.30 | 0.16030 | 0.00724 | 22.1 | 27.67 | 19.98 |
| 3 | 6.95 | **0.21203** | 0.00990 | 21.4 | 27.53 | 20.07 |
| 4 | 9.99 | 0.20208 | 0.01186 | 17.0 | 27.48 | 20.17 |
| 5 | 13.49 | 0.15647 | 0.01318 | 11.9 | 27.51 | 20.20 |
| 6 | 17.51 | 0.11617 | 0.01409 | 8.2 | 27.47 | 20.18 |

**わかったこと 3 つ**:

1. **交番は壁で生まれていない。** 壁から離れるほど増え、**$y\approx7$ µm (第 3 層) で最大**になってから減衰する。
   「壁の市松」という枠組み自体が正しくない。
2. **$P$ は両コードでほぼ同じだけ交番している** (27.5 vs 20.1 Pa、**1.4 倍**)。
   一方 $T$ は **20 倍**違う。圧力-速度連成は同程度で、**温度 (エネルギー) だけが違う**。
   これまで「$P$ は交番していない」と書いていたのは**相関**の話で、**振幅の話ではなかった** (訂正)。
3. **スペクトル** (第 3 層、$s/S$ 0.30–0.80、3 次トレンド除去 + Hann):

   | | ピーク波長 | Nyquist 成分/ピーク |
   | --- | ---: | ---: |
   | **forge** | **2.06 節点** (真の市松) | 0.608 |
   | **SU2** | 25.25 節点 (長波長のうねり) | 0.064 |

   codex が指摘した「$A_{oe}$ は 4 節点周期にも応答するので 2 節点モード専用ではない」という懸念は解消。
   **forge には実在の 2 節点モードがあり、SU2 には無い。**

### 真因の半分 — メッシュが強制源 (2026-09-20)

**壁の接線間隔そのものが交番していた。**

| 段階 | 交番 |
| --- | ---: |
| 3 点円で推定した曲率 $\kappa$ (平滑化輪郭 3200 点) | **16.6 %** |
| → `resample_closed` の曲率重みで再標本化した壁 $\Delta s$ (480 点) | **2.78 %** (メッシュ実測 **2.80 %**) |
| → 第一層厚 $d_1$ / `wall_dist` | 0.060 % (幾何と**比 1.00** = `wall_dist` はバグでない) |
| → 解の $T$ (層 1) | 0.014 % |

壁 $\Delta s$ は 838.9–954.5 µm (標準偏差 **6.3 %**、隣接差分の符号反転率 0.65)。
`resample_closed` は後縁 (R=1.7 mm) に点を載せるため曲率重み付けをしているが、
**曲率推定のノイズがそのまま節点間隔に乗る**。
弧長等間隔なら 0.01 % だが後縁に 4 点しか載らず発散するので、**曲率だけ平滑化する**
(`gen_fluid_mesh.py --curv-smooth N`、既定 0 = 従来とビット同一)。

#### forge の感度 — メッシュを滑らかにしたときの応答

| 壁 $\Delta s$ 交番 | $T$ 層1 [K] | $T$ 層3 [K] | ピーク波長 [節点] | run |
| ---: | ---: | ---: | ---: | --- |
| **2.802 %** (現行) | 0.0820 | 0.2110 | **2.06** (Nyquist) | `run_0051_ctrl_recheck` |
| 0.782 % | 0.0632 | 0.1552 | 3.67 | `run_0062_smoothcurv` |
| 0.162 % | 0.0255 | 0.0555 | 7.00 | `run_0063_cs400` |
| **0.026 %** | 0.0153 | 0.0255 | **47.50** | `run_0063_cs2000` |

**2 節点モードはメッシュを滑らかにすると消える** (λ 2.06 → 47.5。SU2 は同一メッシュで 25.25)。
ただし応答は**劣線形**: メッシュ 100 倍改善に対し解は 5–8 倍しか改善しない。

### 2×2 — 2 つの効果は独立ではない

| | 乱流熱伝導 ON | OFF | 改善比 |
| --- | ---: | ---: | ---: |
| **粗メッシュ** (Δs 2.80 %) | 0.2110 K | 0.0741 K | 2.85× |
| **平滑メッシュ** (Δs 0.026 %) | 0.0255 K | 0.0197 K | 1.30× |

乗法的なら 0.0090 K のはずが実測 0.0197 K。**乱流熱伝導の増幅はメッシュが粗いときだけ効く** =
「増幅」は独立した欠陥ではなく、**メッシュのノイズが $T$ に届く経路**である。したがって**直すべきはメッシュ**。

### 生産ケースでの効果 (2026-09-20)

`--curv-smooth 400` を生産値にした (交番 2.78 → **0.31 %**、後縁の間隔は 152 → **156 µm** で
クラスタリング維持、`check_mesh_quality` **PASS** AR 483 / skew 0.72)。

| | 壁熱流束 odd-even | $h$ PS | $h$ SS 層流 | $h$ SS 遷移後 | $h$ 全体 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 粗メッシュ `run_0027_measTw_pb` | **1764.5 W/m² = 0.986 %** | +10.3 % | +40.4 % | +4.7 % | +16.7 % |
| 平滑メッシュ `run_0065_prod_smoothmesh` | **765.6 W/m² = 0.428 %** | +9.8 % | +39.9 % | +4.7 % | +16.3 % |

**リップルが半分以下**になり、**$h$ の結論は変わらない**。
`check_convergence.py` は両方 `NOT CONVERGED (stalled/plateau)` (後縁渦放出)。

### 真因のもう半分 — forge がメッシュ非一様を増幅している

**SU2 は同じメッシュを見ている。** それでも $T$ の交番は forge の 1/20。
したがって「メッシュが悪い」は半分で、**残り半分は forge の感度**である。

増幅経路は**乱流熱伝導**と分かっている (下表、`run_0060_noturbcond` は ${\rm Pr}_t=10^6$ で
$c_p\mu_t/{\rm Pr}_t\to0$):

| run | 変更 | $T$ 層1 [K] | $T$ 層3 [K] | λ層3 |
| --- | --- | ---: | ---: | ---: |
| `run_0051_ctrl_recheck` | 対照 | 0.0820 | 0.2110 | 2.06 |
| `run_0060_noturbcond` | **乱流熱伝導 OFF** | **0.0351** | **0.0741** | 2.06 |
| `run_0061_nokato` | `katoLaunder: 0` | 0.0827 | 0.2115 | 2.06 |
| `run_0057_laminar` | SST OFF (層流) | 0.0224 | 0.0436 | 50.50 |
| `run_0058_constvisc` | `viscMethod: 0` (定数 $\mu$) | 0.0759 | 0.2110 | 2.06 |
| `run_0059_energyK` | `sstEnergyIncludesK: 1` | 0.0823 | 0.2124 | 2.06 |

**乱流熱伝導を切るだけで交番が 1/3 になり層流値に近づく。** $\mu_t$ の交番が
**巨大な平均温度勾配** ($\sim4.5\times10^6$ K/m) に掛かって交番熱流束を作っている。
$\mu_t/\mu_{\rm lam}$ は層 1 で $10^{-4}$、層 3 で 5 % にすぎないが、
交番成分としては支配的になる。

同一節点での相対交番の比 (forge/SU2): $T$ **20×** > $\omega$ 5–9× > $k$・$\mu_t$ 2.6–3.2×。
**$\mu_t$ が 3 倍ノイジーなだけで $T$ が 20 倍になる**のが増幅の実体で、ここが未解明。

**LSQ の重みは両者一致** ($w=1/|d|^2$。SU2 `computeGradientsLeastSquares.hpp:249`、
forge `calcGradient_d.cu:530`) なので、勾配の重み付けは差ではない。

### 副産物 1: forge の Harten エントロピー補正が次元不整合 (未修正)

[`convectiveFlux_roe_d.inc.cuh`](../../solver_density_cuda/cuda_forge/convection/convectiveFlux_roe_d.inc.cuh) の有効経路:

```cpp
eta_vl = 0.1*(abs(Ua)/ca + 1.0);   // |Ua|/ca は無次元 -> eta_vl も無次元 (0.1-0.2)
if (lam[1] < eta_vl) ...            // lam は m/s (Ua, ca とも m/s)
```

すぐ下にコメントアウトされた `h_fix = 0.05*(abs(Ua)+ca)` が本来の Harten 形。
この case ($c\approx530$ m/s) では本来 ~53 m/s のところ実効 ~0.1。SU2 の床 $0.001(|U_a|+c_a)\approx0.53$ m/s。
**市松の原因ではない**ことは上の掃引で分かったが、**次元不整合そのものは欠陥**なので別途起票する。
opt-in の `space.roeEntropyFixCoeff` (既定 0 = ビット不変) を SU2 同形で追加済み。

### 副産物 2: `solverConfig.hpp` 変更後の差分ビルドで ROE が step 3 NaN

`roeEntropyFixCoeff` をメンバに足した直後、**係数 0 でも** ROE が step 3 で NaN になった。
bisect で「committed 状態では完走」を確認し、**クリーンビルドで解消**。
既知の罠 ([[stale-build-struct-layout-trap]]) を踏んだもので、エントロピー床のロジックは無罪。
**`solverConfig.hpp` を触ったら差分ビルドを信用しない。**

### ROE の TP / 凝縮対応 (ユーザ指摘 2026-09-20)

`thermalMethod == 2` の分岐はあるが、**固有値・固有ベクトルは CPG の $\gamma$ で組んでいる**
(`ca_L = sqrt(gamma P/ro)`, `roe_L = P/(gamma-1) + rho v^2/2`)。実効 $\gamma$ や
$\partial p/\partial\rho|_e$ を使う厳密な TP Roe にはなっていない。
凝縮も `condModel` で物性を切り替えるだけで、二相 frozen 音速は入っていない。
**本件 (C3X) は `thermalMethod: 0` なので影響しないが、将来課題。**

## 計算 run 一覧 (追補) — CHT 連成

| `run_*` | 目的・主要設定差分 | 主要結果・成果物 | 状態 |
| --- | --- | --- | --- |
| `run_0004_cht` | **V5 段 (c)**: 外部弱連成 CHT (`cht_loop.py --solid-mode fem2d`)。固体は `mesh/solid_c3x.npz` (外周 480 節点 = 流体壁と 1 対 1)、孔は逆算した $h_c$ / $T_c$=300 K、$k_s(T)$ は ASTM310。1 反復 = forge 4000 step、25 反復 | $T_w$ **523.7–617.2 K** ($s/S\le0.87$)、実測 512.6–611.8 K。**バイアス +11.9 K / RMS 18.8 K**。`dTw` 0.02 K・`res_rel` 1.2e−3 まで落ちた (既定 tol 1e−3 をわずかに超えて `NOT CONVERGED` 表示)。成果物 `Tw_compare.png` / `cht_history.csv` | active |
| `run_0009_uniformTw` | **SU2 対照用** (plan #28)。壁を一様 566 K にして `wallProfile` を外した以外 `run_0003` と同じ。`su2_run108/` と同一メッシュ・同一 BC | forge vs SU2 は遷移後 −0.6 %/2.1 %、層流域 −3.0 %/6.0 %。**層流域の過大は両者共通** | active |
| `run_0010_energyK` | #41 切り分け。`run_0009` に `sstEnergyIncludesK: 1` (SU2 形) を足しただけ | SU2 との差は **−4.31 → −4.50 %** で改善せず。**仮説棄却** | ref |
| `run_0011_laminar` | #41 切り分け。`model: "none"` で基礎スキームのみ比較する狙い | **残差 0.0 dec で全く収束しない** (負圧面剥離で非定常)。SU2 は同条件で定常解。切り分けに使えず | 破棄予定 |
| `run_0021_qeff` | #30 保存的界面熱量の実機確認。`run_0003` の場から 4000 step | `iface_q_eff` は `q_compact` と **bias +2.26 % / rms 3.28 % / 局所最大 19.1 %** 違う。480/480 節点で有限 (壁ノードが周期に属さないため) | ref |
| `run_0022_cht_qeff` | #30。`cht_loop --flux q_eff` (保存的界面熱量) で連成。他は `run_0008` と同じ | 壁温 bias **+11.5 → +13.5 K** / rms 18.4 → 19.5 K。`q_compact` より 2 K 熱い側へ動く | active |
| `run_0023_cht_tight` | #30b/#34 ゲートの実証。`run_0022` と同じだが **1 反復 16000 step** (4000 → 4 倍) | **G-cons `PASS` 不釣合い 0.0272 %** (11.6 / 42513 W/m)。G-if は `res_abs` 487 → **3.2 W**、`res_solid` 1.02 → **9.4e-05 W** まで落ちたが `res_rel` 1.7e-2 で未収束 | active |
| `run_0024_wall960` | リップル調査。`run_0009` の壁を 480 → 960 節点に細分 | odd-even 0.92 → 1.01 % だが、**指標が Nyquist を分離していない** (codex 反証、plan §6.1 R3)。960 ではピークが 4.02 節点 = 同一物理波長へ移動。メッシュ非収束の根拠にならない | ref |
| `run_0025_pb_idealM2` | plan #44。`run_0009` と同一で**出口静圧のみ** 185,064 → **188,908 Pa** (報告の理想 $M_2$=0.90 の定義) に訂正。`run_0009` の場から restart、20000 step | 負圧面後半の壁圧 bias $-0.0257\to\mathbf{-0.0125}$、rms $0.0349\to0.0232$。熱流束は +0.4 % で $h$ の結論は不変。`check_convergence.py` は **`NOT CONVERGED (stalled/plateau)`** (後縁渦放出のため定常解なし)。成果物 `press_backpressure.png` | active |
| `run_0026_exitangle_7326` | plan #44(c)。`run_0025` と同一で**メッシュの周期出口ブロック角のみ** 72.38° (表 IV 設計値) → **73.26°** (報告 Table III 座標の喉 o/pitch 0.2881 から) に変更。`run_0025` の場から restart、20000 step、30846 節点 | **空振り (仮説棄却)**。壁圧 bias/rms が `run_0025` と **4 桁まで同一** (SS 後縁 −0.0125 / rms 0.0232、PS 後縁 −0.0232 / rms 0.0353)、出口気流角 73.07 → 73.06°、壁平均 $q$ −0.015 %。**流れが自分で出口角を決めるので周期ブロックの向きは効かない**。`check_convergence.py` は `NOT CONVERGED (stalled/plateau)` | ref |
| `run_0027_measTw_pb` | **訂正背圧の実測 $T_w$ 本命**。`run_0003_smoothgeom` と同一で出口 Ps のみ 185,064 → **188,908 Pa**。`run_0003` の場から restart、20000 step | 壁圧 SS bias $-0.0187\to\mathbf{-0.0107}$ / rms 0.0315 → **0.0259**、PS $-0.0145\to-0.0128$ / 0.0295 → 0.0268。$h$ はほぼ不変 (PS +10.6→**+10.3 %**、SS 層流 +40.5→**+40.4 %**、SS 遷移後 +4.0→**+4.7 %**、全体 +16.6→**+16.7 %**) = **背圧訂正は $h$ の結論を変えない**。`check_convergence.py` `NOT CONVERGED (stalled/plateau)`。成果物 `h_compare.png` | active |
| `run_0028_precond2` | 市松の仮説 (2)。`run_0009` に `lowMachPrecond: 2` を足しただけ | **破綻**。残差 2.3 桁**上昇** (rms_ro 4.2e−2)、odd-even **56.9 %**。前処理は本件に使えない | 破棄予定 |
| `run_0029_roe` | 市松の対流スキーム切り分け。`run_0009` の `solver` を SLAU → **ROE** | odd-even **0.901 %** (SLAU 0.919 %) = **改善しない**。`NOT CONVERGED` (rms_ro 1.4e−2) なので参考値 | ref |
| `run_0030_hlle` | 同上、**HLLE** | odd-even **2.10 %** = 悪化。`NOT CONVERGED` | ref |
| `run_0039_weakbc` | **弱形式等温壁 (SU2 型) の A/B** (plan boundary-weak-isothermal-wall)。`run_0009` と同一で `mesh.nodeIsothermalEnergyBC: 1` のみ追加。`run_0009` の場から restart、20000 step | 壁ノード $T_W$ = **566.001–566.831 K** (SU2 実測 566.011–566.886 K と同等 = 実装は SU2 の挙動を再現)。しかし odd-even は **0.9186 → 1.2092 %** と **32 % 悪化**、第一内部点 $T$ の交番も 0.0827 → 0.1120 K。`check_convergence.py` `NOT CONVERGED`、`rms_roK`/`rms_roOmega` **RISING** | active |
| `run_0031`–`run_0038` | 既定パス不変の確認 (ノイズ床を両側から測る)。200 step を旧バイナリ 4 反復 / 新バイナリ 4 反復 | **node の C3X は `atomicAdd` で run-to-run 非決定** (旧 1 回目 vs 2 回目が新 vs 旧と同程度に違う)。RMS 差の中央値で交差/群内 = **0.91–0.97** = 同一 population。**交番振幅自体は再現的** (旧 0.9112 % / 新 0.9111 %、幅 **0.0001 %**) → 有意差閾値 0.001 % を事前登録 | ref |
| `run_0040_wallflux0` | **決定的診断**。強制のまま等温壁の**壁半割面の熱流束だけ**を 0 に (`nodeIsothermalEnergyBC: 2`)、200 step | 場の変化は run-to-run ノイズ床の **0.70–0.85 倍** = **変わらない**。`zero_res_roe_bplane_d` と陰解法エネルギー行の単位行化で、壁半割面の寄与は**まるごと捨てられている** (診断にしか出ない) | ref |
| `run_0041_heatcorrsu2` | 市松の切り分け。内部面の**熱伝導だけ** SU2 の corrected-gradient $a=(d\cdot S)/\|d\|^2$ に (`space.heatCorrSU2: 1`)。運動量の $\tau$ は不変 | odd-even **0.9186 → 0.9159 %** (相対 0.3 %)、第一内部点 $T$ の交番 0.0827 → 0.0825 K = **ほぼ不変**。近壁が直交なので両係数が一致する (codex の予測どおり) | ref |
| `run_0005_band_Tc350` | 帯: 冷却剤温度 $T_c$=350 K (孔の $h_c$ も同じ $T_c$ で再同定) | $T_w$ 差 **最大 2.2 K** | active |
| `run_0005_band_ks_hi` / `_ks_lo` | 帯: 材料熱伝導率 ±3 % (報告の不確かさ) | $T_w$ 差 **最大 2.0 / 1.7 K** | active |

### V5 段 (c) の結果 — 連成 CHT の壁温 (2026-09-20)

| 領域 | 節点数 | バイアス | RMS | max\|err\| |
| --- | ---: | ---: | ---: | ---: |
| 全体 ($s/S\le0.87$) | 402 | **+11.9 K** | 18.8 K | 61.0 K |
| 正圧面 | 164 | +9.5 K | 16.1 K | 61.0 K |
| 負圧面 層流域 $s/S<0.25$ | 109 | +21.4 K | 27.4 K | 42.1 K |
| 負圧面 遷移後 | 129 | **+6.8 K** | 12.1 K | 24.7 K |
| 後縁 $s/S>0.87$ (**データ範囲外**) | 78 | +26.6 K | 43.5 K | 64.8 K |

**帯との照合**: 事前登録した帯の項目のうち、**内部条件の推定に由来する分は小さい** —
$T_c$ 280→350 K で $T_w$ が最大 2.2 K、$k_s$ ±3 % で 2.0 K。同定そのものの残差床は
**RMS 9.9 K** (480 節点固体で再同定。240 節点では 10.7 K だったのでメッシュ依存ではない)。
したがって **観測された +11.9 K / RMS 18.8 K は内部条件の不確かさでは説明できず、CFD 側**
— とくに**負圧面層流域の $h$ 過大 (+40.5 %、段 (a))** — が主因である。遷移後の負圧面は
**+6.8 K / RMS 12.1 K** で、同定の残差床 9.9 K とほぼ同じ桁にある。

**次の一手** (plan §5.1 に登録): ① 遷移モデル無しの影響を切り分けるため、同一メッシュ・同一壁温で
**SU2 の低 Re SST と対照**する (#28)。② Mark II (超音速出口) を同じ手順で通す。


## V5 の結論の撤回と格下げ (2026-09-20, codex result レビュー **NO-GO** を受けて)

記録: [`notes/reviews/2026-09-20-boundary-conjugate-heat-transfer-result.md`](../../notes/reviews/2026-09-20-boundary-conjugate-heat-transfer-result.md) (C1/M9/m1、**全件採用**)。

**撤回する主張**:
1. ~~「連成そのものは正しい」~~ — 段 (c) は**同定した内部条件のもとでの整合性評価**であって
   独立検証ではない。$h_c$ は実測 $T_w$ との差を最小化して決めており、同じ $T_w$ に近づくことを
   連成の正しさの根拠にできない (循環)。
2. ~~「差の主因は遷移モデルの欠如」~~ — **有力仮説**に留める。確定には #28 SU2 対照・
   #29 遷移感度・格子感度が要る。
3. ~~「両側の熱量が一致した」~~ — 一致したのは**採用したコンパクト差分と固体側**であって、
   拘束反力を含む流体のエネルギー授受が閉じた証拠ではない ($Q_f=\sum F^E-C$ は未実装、plan §5.1 #30)。
4. ~~「残差プラトーだが報告量は STEADY」~~ — 積分 `q_total` の `STEADY` は**局所量を保証しない**。
   codex の再判定では C3X 局所 $q$ 402 点中 **255 点が `DRIFTING`**。局所量にも事前登録した許容が要る (#36)。

**実装の欠陥 4 件は修正済み** (反例を回帰試験化、`test_solid_shell.py` T6 / `test_solid_fem2d.py` T5):
棄却時の状態混在、初回の壁温不一致 (課 512–612 K / 仮定 566 K 一様)、`fem2d` の $k_s(T)$ が
界面平均で全域一様、収束ゲートの規格化。**ただし V5 の数値はほとんど動かなかった** (下表)。

| | 旧 (`run_0004_cht`, $T_c$ 一様 300 K・欠陥あり) | 新 (`run_0008_cht_fixed`, $T_c$ 公開値・修正後) |
| --- | --- | --- |
| 全体 | +11.9 K / rms 18.8 | **+11.5 K / rms 18.4** |
| 正圧面 | +9.5 / 16.1 | +8.7 / 14.6 |
| 負圧面 層流域 | +21.4 / 27.4 | +21.9 / 27.7 |
| 負圧面 遷移後 | +6.8 / 12.1 | +6.2 / 11.7 |

**連成反復の到達点**: $\Delta T_w$ は 5e−5 K まで落ちるが、**界面残差は `res_rel` 3.7 % で止まる**。
これは固体側ではなく **CFD 側のノイズ床** (1 反復 4000 step では $q_w$ が再現性 rms 1.5 %・最大 7.6 %)。
1 反復あたりの step を増やさないとこれ以上は詰められない。


## SU2 対照 — 遷移仮説の切り分け (plan §5.1 #28、2026-09-20)

**同一 `.geo` から生成した同一メッシュ・同一 BC・一様壁温 566 K** で forge と SU2 8.5 を回し、
壁面 $h$ を比べた。実測分布を課すと壁温内挿の差が交絡するので、両者とも一様壁温にしてある。

- forge: `run_0009_uniformTw/` (60000 step、残差 2.4–4.7 dec 低下でプラトー。
  `check_quasisteady`: `q_total: STEADY` drift 1.1 %、48.05 kW/m)
- SU2: `su2_run108/` (`SOLVER=RANS`/`KIND_TURB_MODEL=SST`、低 Re 壁、20000+8000 iter。
  `rms[Rho]` −5.21 プラトー、出口 massflow は 1e−4 まで定常。壁 $y^+$ 0.09–1.85)
- 壁節点は**座標が完全一致** (最大距離 0.00e+00 m) — 同一メッシュであることの確認。

| 領域 | forge vs 実測 | SU2 vs 実測 | **forge vs SU2** |
| --- | ---: | ---: | ---: |
| 正圧面 | +12.2 / 22.1 % | +18.9 / 24.9 % | **−6.0 / 7.1 %** |
| 負圧面 **層流域** $s/S<0.25$ | +42.7 / 55.6 % | **+45.8 / 55.2 %** | **−3.0 / 6.0 %** |
| 負圧面 遷移後 | +6.1 / 10.4 % | +6.6 / 10.3 % | **−0.6 / 2.1 %** |
| 全体 | +18.5 / 32.8 % | +22.3 / 33.4 % | −3.4 / 5.6 % |

**結論**: 負圧面層流域の $h$ 過大は **SU2 でも +45.8 %** で、forge (+42.7 %) と **−3.0 %/6.0 %** しか
違わない。遷移後は **−0.6 %/2.1 %** で一致する。**この誤差は forge 固有ではなく、遷移モデルを
持たない低 Re SST に共通**である — 2026-09-20 に「仮説」へ格下げした主張は、独立ソルバで裏付けられた。

図: `run_0009_uniformTw/h_forge_su2.png`。

**残る solver 間差**: 正圧面 −6.0 %/7.1 %、層流域 −3.0 %/6.0 %。forge が SU2 より一貫して低い。
遷移後の 2.1 % に比べれば大きく、**壁近傍の層流熱伝達の差**として別途追う価値がある
(両者とも $y^+\le2$ で、壁解像は交絡しない)。


### 残った forge–SU2 差の切り分け (plan §5.1 #41、2026-09-20)

遷移後は −0.6 %/2.1 % まで一致するのに、**正圧面と層流域で forge が SU2 より 4–7 % 低い**。
定義・物性・幾何の交絡を 1 つずつ潰した結果は次のとおり。

| 消した可能性 | 確認 |
| --- | --- |
| 熱流束の**定義**の違い | SU2 側も**同じコンパクト差分**で組み直した (同じ第一内点・同じ $d_1$)。残差 1.9e−8 m で同定。差は縮まらない |
| **幾何**の違い | 壁節点は座標完全一致 (0.00e+00 m)、$d_1$ は両者 1.989–2.012 µm で同一 |
| **層流物性**の違い | $\mu$ は Sutherland に対し forge 1.0012 / SU2 0.9999 (**両者 0.14 % 以内**)。$k=\mu c_p/Pr$、$Pr$=0.72、$c_p$=1004.5 も同一 |
| 第一内点の**乱流粘性** | 両者 $\mu_t/\mu\approx0$ (壁で 0)。壁直上の差ではない |
| **エネルギー式に $k$ を含めるか** (`sstEnergyIncludesK`, SU2 形) | `1` にしても **−4.31 % → −4.50 %** と改善しない (`run_0010_energyK`)。**棄却** |

**残っている事実**: 差は $T_1-T_w$ そのもの (PS で forge 5.68 K / SU2 6.03 K)。境界層プロファイルを
見ると、**速度は ±1 % で一致しているのに温度だけ壁から 20 µm の範囲で系統的にずれる**
(PS: 2 µm で −0.21 K → 17.5 µm で −1.19 K)。負圧面層流域は逆に forge が高温側 (+2.5 K @13 µm) で
速度も +2 % 速い。`run_0009_uniformTw/bl_profiles.png`。


## 合否ゲート G-cons / G-if の実測 (2026-09-20)

**2 つは別のものを測っている**ので両方要る。G-cons は**積分**の収支、G-if は**局所最大**の残差。

| run | 1 反復の forge step | G-cons (積分収支) | G-if `res_abs` | `res_solid` | `res_rel` |
| --- | ---: | --- | ---: | ---: | ---: |
| `run_0022_cht_qeff` | 4 000 | **FAIL 2.77 %** | 487 → 数十 W | 1.02 → 1e-4 W | 3.5e-2 |
| `run_0023_cht_tight` | **16 000** | **PASS 0.0272 %** | **3.2 W** | **9.4e-05 W** | 1.7e-2 |

### 定義を変えるだけで収支が閉じなくなる (C1 の実証、2026-09-20)

`run_0023_cht_tight` の**同一状態**で、界面熱量の定義だけ変えて G-cons にかけた結果:

| `--flux` | 流体側 $\sum Q_f$ [W/m] | 固体側 [W/m] | 不釣合い | VERDICT |
| --- | ---: | ---: | ---: | --- |
| **`q_eff` (保存形)** | **42512.6** | 42501.0 | **0.0272 %** | **PASS** |
| `q_2nd` (2 次片側) | 42626.5 | 42501.0 | 0.2944 % | PASS |
| `q_compact` (コンパクト差分) | 41401.2 | 42501.0 | **2.6565 %** | **FAIL** |
| `q_recon` (再構成勾配) | 41398.4 | 42501.0 | **2.6635 %** | **FAIL** |

**コンパクト差分と再構成勾配は積分で 2.7 % 足りない**。これで連成すると、固体が持ち去る熱を
流体側が供給していない解を「収束した」として通してしまう — codex が Critical に挙げた
「熱量が閉じない解を合格させてしまう」そのものである。**`q_eff` を正本にする根拠**。

V1 (`case/52.conjugate_slab/run_0005_qeff`、ソルバ内連成 `local1d`) でも
**不釣合い 0.000125 %** (2e-6 / 1.62 W) で PASS。面積重み・節点対応・符号を含めて閉じている。

- **G-cons の不釣合いは CFD 側のノイズ床で決まる**。1 反復の step を 4 倍にすると 2.77 % → 0.027 % と
  100 倍良くなった。固体でも反復でもない。
- **G-if は `res_rel` で止まる** (1.7e-2)。局所最大の界面残差は壁の一部の節点で CFD のばらつきが
  そのまま出るため、積分が閉じていても下がりきらない。**「積分が閉じた」と「局所が収束した」を
  混同しない**ために別判定にしてある。
- 壁温 (`run_0023`, $s/S\le0.87$): 全体 **+16.1 K / rms 21.4**、正圧面 +13.8 / 18.3、
  負圧面 遷移後 +11.5 / 15.2、層流域 +24.9 / 30.3。`q_compact` 版 (`run_0008`, +11.5 K) より
  熱い側へ動く (保存形のほうが 2.26 % 多く熱を渡すため)。

### 層流対照が成立しなかった件 — 訂正と追跡 (2026-09-20)

**旧稿の 2 つの記述を訂正する** (ユーザ指摘「層流で収束しない? まずくない?」+ codex 診断
[`notes/reviews/2026-09-20-codex-laminar-nonconvergence.md`](../../notes/reviews/2026-09-20-codex-laminar-nonconvergence.md)):

1. ~~「負圧面が剥離して非定常」~~ → **後縁後流の変動**。スナップショット間の最大変化は
   $(x,y)\approx(0.076,-0.005)$ m = **後縁**、壁から 3.7–5.0 mm。負圧面ではない。
2. ~~「SU2 は同条件で定常解に落ちる」~~ → **SU2 も残差基準では `NOT CONVERGED`**
   (`rms[RhoE]` +0.465、低下 0.9–1.3 桁)。定常なのは出口流量 (0.0023 %) という**積分量**。

**ただし場の静定度は 67 倍違う** (対称に測り直した結果、2026-09-20):

| | $P$ rms差 | $T$ rms差 | $U_x/U_y$ rms差 | 最大変化の位置 |
| --- | ---: | ---: | ---: | --- |
| SU2 (15000→25000 iter) | **44.1 Pa** | 0.22 K | 0.45 / 1.26 m/s | (0.0525, 0.0829) 正圧面 |
| forge (40000→60000 step) | **2934 Pa** | 3.58 K | 13.3 / 14.6 m/s | (0.076, −0.005) **後縁** |

**forge が収束しない原因の切り分け** (すべて同一初期場・同一メッシュ):

| 試したこと | run | 結果 |
| --- | --- | --- |
| `cfl_pseudo` 0.5 → **4.0** | `run_0012_lam_cfl4` | **出口で圧力床に落ちて step 292 で発散** (x 0.176–0.195、壁から 0.3–0.44 m)。SST 版も step 754 で同様 (`run_0013_sst_cfl4`)。**別件として plan §5.1 #42 に起票** (下記) |
| `cfl_pseudo` 1.5 / 4.0+`implicitRelax` 0.5 | `run_0014` / `run_0015` | 安定だが**収束しない** (残差はむしろ上がる) |
| `nStepInner` 4 → **30** | `run_0016_lam_inner30` | 残差不変 (4.2e−4)。**陰解法の解き込み不足ではない** |
| `cfl_pseudo` 0.5 → **0.05** (codex 推奨) | `run_0017_lam_cfl005` | **残差不変** (6.5e−4)。**擬似時間反復の不安定性という説は支持されない** |
| **時間精度 (dual-time)** で後縁シェディングを見る | `run_0018_lam_dualtime` | **コヒーレントなカルマン渦列を確認** (下記) |
| $\Delta t$ 半減で時間解像を確認 | `run_0019_lam_dt_half` | ピーク 29.3 → **29.9 kHz** (差 2 %、分解能以内)。**時間解像できている** |

#### 結論: **物理的な後縁カルマン渦列だった** (`run_0018_lam_dualtime`)

dual-time ($\Delta t$=50 ns、サブ反復 12、擬似 CFL 15、各物理 step で残差 −2.8 桁) で 200 µs
(4000 step) 回し、後縁 2 mm 下流の 2 点で $U_y$ を追った:

- **周期的で、2 点が逆位相** — 200 µs に約 6 周期。振幅 ±100 m/s (A 点 変動幅 350 m/s)。
- PSD のピークは**両点とも 29.3 kHz** (高調波 58 kHz)。→ $St = fD/U = 29300\times0.00346/351 = $ **0.29**。
- 後縁 $Re_D$ = **4.2×10⁴** ($D$=3.46 mm, $U$=351 m/s)。鈍頭後縁の渦放出域そのもの。

図 `run_0018_lam_dualtime/te_shedding.png`。

→ **forge の層流 run が収束しないのは欠陥ではなく、安定な定常解が存在しないから**である。
定常ソルバは存在しない解に収束できない。SU2 の定常解のほうが、陰解法 (CFL 5 + FGMRES/ILU の
厳密解き) が実在する不安定モードを減衰させた結果と解釈するのが整合する
(SU2 の場の静定度 44 Pa は、この渦列が消えていることを意味する)。

#### $\Delta t$ 半減で時間解像を確認済み (`run_0019_lam_dt_half`)

$\Delta t$ を 50 → **25 ns** に半減し、窓も 200 → **300 µs**、出力も 5 → **1.25 µs** 間隔に細かくした:

| | $\Delta t$ | `maxCFL` | 点数 / 窓 | 分解能 | **PSD ピーク** | 振幅 (std) |
| --- | ---: | ---: | --- | ---: | ---: | ---: |
| `run_0018_lam_dualtime` | 50 ns | 27–32 | 41 点 / 200 µs | 5.0 kHz | **29.3 kHz** | 108.5 m/s |
| `run_0019_lam_dt_half` | **25 ns** | **14.5–14.9** | 241 点 / 300 µs | 3.3 kHz | **29.9 kHz** | 102.8 m/s |

ピーク差 0.6 kHz (**2 %**) は両者の分解能より小さく、振幅も 5 % 以内。**時間解像できている**。
`maxCFL` も半減して推奨の ≲12 に近づいた。$St = fD/U = 29900\times0.00346/351 =$ **0.295**。
図 `run_0019_lam_dt_half/dt_convergence.png`。

#### 副産物として追った「出口 BC が擬似 CFL を律速する」は**誤りだった** (plan §5.1 #42、2026-09-20)

`cfl_pseudo` 4.0 で出口ブロックが発散するのは事実だが、**原因は出口 BC ではなく 2 次再構成**で、
しかも **CFL を上げても速くならない**。順に:

**(1) 発達の様子** (`run_0020_diag_outlet`, 層流, 25 step ごと): 出口面 49 節点の静圧の幅が
step 0 の **1.1 kPa** → step 275 の **41.7 kPa** と単調に開き、内部 (M max 1.10→1.21) は健全。
場所は確かに出口である。

**(2) ゴーストの遅れ説は棄却**: `outlet_statPress` のゴーストを緩和 (w=0.3) しても
step 290 で発散 (無緩和は 292)。**BC の遅れが原因ではない**。

**(3) 真因は 2 次再構成**: `cfl_pseudo` 4.0 で

| 設定 | 結果 |
| --- | --- |
| **1 次** (`convMethod: 0`) | **3000 step 安定** |
| 2 次 + Venkatakrishnan (既定 `venkatK` 0.05, `limiterScaled` 1) | step 291 で発散 |
| 2 次 + Barth | step 98 |
| 2 次 + `venkatK` 0.01 | step 271 |
| 2 次 + `limiterScaled: 0` (旧経路) | step 291 |

**リミッタの選択ではなく 2 次再構成そのもの**が上限を決めている (陰解法の Jacobian が 1 次で
組まれる defect-correction の典型的な CFL 上限)。

**(4) 上限は外せるが、速くはならない**。`implicitRelax` 0.7 か `nStepInner` を増やすと
cfl 4–8 でも安定になる (DPLUR の近似解きが律速だという説と整合)。しかし **SST 本番設定で
20000 step 回して残差を比べると**:

| 設定 | 20000 step の所要 | 最終 `rms_ro` | 最終 `rms_roe` |
| --- | ---: | ---: | ---: |
| **`cfl_pseudo` 0.5 / `implicitRelax` 1.0 / `nStepInner` 4 (現行)** | 158 s | **4.6e−06** | 3.69 |
| `cfl_pseudo` 2.0 | 156 s | 4.5e−03 | 3565 |
| `cfl_pseudo` 8.0 + `implicitRelax` 0.7 | 162 s | 1.6e−03 | 1309 |
| `cfl_pseudo` 4.0 + `nStepInner` 30 | 281 s | 7.0e−03 | 5755 |

**現行設定が圧倒的に良い** (残差で 300–1500 倍)。CFL を上げると安定でも高い残差で頭打ちになる。
→ **「出口 BC が生産 run の計算時間を律速している」という 2026-09-20 の記述は撤回する**。
case/53 の `cfl_pseudo` 0.5 は妥当な選択である。

**残る事実**: この case では 2 次再構成が `cfl_pseudo` の上限を 2 前後に決めている。
SU2 が CFL 5 で回るのとの差は残るが、**forge 側で CFL を上げても収束は速くならない**ので、
実用上の問題ではない。速度が要るなら詰めるのは擬似時間の CFL ではなく別のところ。

**SST の非収束は別物**: スナップショット間の $P$ rms 差が層流 2934 Pa に対し
`run_0009_uniformTw` 15.9 Pa・`run_0003_smoothgeom` 1.07 Pa と **200〜2700 倍静か**で、
後縁 4 量は SST でいずれも `STEADY` (codex 再判定)。ただし壁の局所熱流束は
`run_0009` で 480 点中 81 点が `TRANSIENT-UNSETTLED`・31 点が `DRIFTING`。

**次の候補** (plan #41): 対流スキーム (forge SLAU+MUSCL vs SU2 ROE+Venkatakrishnan) の
近壁温度への効き、node median-dual の壁半 CV のエネルギー流束、前縁からの境界層発達履歴。

### `run_0001` が収束しなかった理由 (2026-09-20)

`inlet_uniformVelocity` は「速度 3 成分 + config エントロピー $P_s/\rho^\gamma$」を課して $R^+$ を内点から
取る**正しく posed な**亜音速入口だが、**全温を固定しない**。内部静圧が config アンカー (313.6 kPa) でなく
252 kPa に落ち着いた結果、入口 $T_t$ が 786 → **738 K** (−48 K) になった。$h$ は $T_g$ 基準なので
この時点で比較に使えない。あわせて入口ブロックが定常に落ち着かず (低マッハ域の大規模モード)、
残差が 1e−3 台で停滞した。**全圧入口に替えたら同じメッシュ・同じ CFL で残差が 3 桁下がり**、
$P_t$・$T_t$ が全面で指定値どおりになった。

その `inlet_Pressure_dir` は step 214 で入口節点が NaN 化して使えなかったが、真因は BC カーネルの
欠陥 2 件で 2026-09-20 に修正した ([`methods/boundary.md`](../../methods/boundary.md) 「例: 全圧固定流入」):
(1) $P_c>P_t$ で境界マッハの根号内が負、(2) 方向ベクトルを保持する `bvar` を毎 step 次元付き速度で
上書きするため $M\to0$ で長さ 0 → 0/0。
なお `cfl_pseudo` は 0.5 で安定、**2.0 は 8000 step で残差が 3 桁上がる**ので使わない。

### 壁熱流束の節点間振動は**形状**だった (2026-09-20)

`run_0002` では壁 $q$ が隣り合う節点で 52 ↔ 302 kW/m² と振れた。node の壁市松
([[node-wall-entropy-checkerboard]]) を疑ったが、`iface_align`=1.0000・`iface_d1`=2.00 µm 一定で
幾何は健全、第一内点の $|U|$ が 38 → 68 → 13 → 1.4 m/s と飛ぶ位置が**輪郭の角と一致**した
(折れ角と $|q-\mathrm{smooth}(q)|$ の相関 0.57)。

原因は 2 つ。(i) 座標表が 78 点 / 周長 32 cm しかなく、負圧面前縁の高曲率域で 1 弦 6 mm が 20° 曲がる。
(ii) 復元した前縁/後縁円弧の**中心を「頂点から $x$ 方向に $R$」**と置いていたが、取付角 60° の翼では
鼻が $x$ を向いていないので中心が 0.06 cm ずれ、区間端の表点が円から 0.05 cm 外れ、そこに円弧を
繋いで**半径方向の段差 = 折れ角 60°** ができた。

対策は [`tools/smooth_profile.py`](tools/smooth_profile.py): 円弧は **$R$ 固定の最小二乗で中心を決め**
許容 0.003 cm で区間を伸ばし端点を円上へ射影 → 全周に**周期平滑化スプライン** (重み = 報告の
座標不確かさ ±0.008 cm、$s=0.05m$)。4 ゲート (残した表点からのずれ / 折れ角 / $R_{LE}$・$R_{TE}$ /
自己交差) を**落ちたら書き出さない**。結果: 折れ角 60° → **3.8°** (0.1 mm 刻み)、表点からのずれ
rms 0.0016 cm、$R_{LE}$ 1.179 (報告 1.168)・$R_{TE}$ 0.188 cm (報告 0.173)。
