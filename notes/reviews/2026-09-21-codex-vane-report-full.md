# codex レビュー — 冷却翼 CHT 検証レポート全体 (2026-09-21)

対象 plan: [`plans/active/boundary-conjugate-heat-transfer.md`](../../plans/active/boundary-conjugate-heat-transfer.md)
対象: Artifact「Cooled Vane CHT Validation」V32 の本文全文 (43 KB を Markdown 化して渡した)
依頼: 主張と根拠の不一致 / 章をまたいだ数値の矛盾 / 見落とした交絡 / 誤解を招く書き方。
各指摘に「どの文か・なぜ問題か・何をすれば決着するか」を要求。

## 判定

**Critical 0 / Major 13 / Minor 6**。全件を下表のとおり採否した。**却下は 0 件** —
指摘はいずれも本文の具体的な文を引用しており、こちらで数値を確認して妥当と判断した。

## Major

| # | 指摘 | 採否 | 対応 |
| --- | --- | --- | --- |
| M1 | シェディング確認 (層流 dual-time) から「定常解が存在しない」「全 run の停滞原因」まで断定 | 採用 | 「停滞原因の候補」「定常解に到達していない」に限定。本番 (低 Re SST) と同条件の dual-time で時間平均の報告量を出すのは残作業 |
| M2 | 「全量が平坦」宣言に `DRIFTING` (MkII 層流域 −7 %) が反する。run 表に 16k/20k/60k があり「C3X は 200k」とも合わない | 採用 | 量ごとの準定常判定表を置き、−7 % は過渡値として明示。包括宣言は撤回 |
| M3 | 「メッシュは無関係」の比較が `run_0107` (実測 Tw・188.9 kPa・cs400) 対 `run_0109` (566 K・185.1 kPa・as gen.) で、壁温と背圧も違う | **採用 (こちらの誤り)** | 正しい組は `run_0108` 対 `run_0109`。結論を「一様壁条件での節点配置感度」に限定 |
| M4 | Mark II の格子収束宣言が旧形状 (丸め後縁) の試験に依存 | 採用 | 後縁切り口の形状で 340/680 節点をやり直す (`run_0022_tecut_coarse` / `run_0023_tecut_fine`) |
| M5 | Mark II の CHT 表が、修正後形状の $h$ と修正前形状の $T_w$ を並べている | **採用 (こちらの誤り)** | 修正後の流体・固体形状で連成をやり直す (`run_0021_cht_tecut`、固体も新しい壁に合わせて再生成)。「44 K が遷移欠落の代償」の因果表現は撤回 |
| M6 | $h$ と $T_w$ の地域対応から「連成独自の誤差は無い」と断定 | 採用 | 「気相誤差と整合する地域傾向」に限定。固体単独検証・界面収支・固体格子感度は残作業 |
| M7 | 層流/SST の挟み込みを遷移欠落の「直接検証」として扱っている | 採用 | 「遷移欠落を支持する感度試験」と書く。直接検証には遷移位置を制御した計算が要る |
| M8 | SU2 との類似から共通誤差 (形状・入力・モデル・解像度) を除外して原因を確定 | 採用 | 「forge 固有の大きな差は見えない」に限定。2D 化・発達履歴への帰属は撤回。一致の主張は同じ評価式での差 (2.3–3.1 %) に限る |
| M9 | Tu 掃引は μt/μ=10 固定の経路、−5/+18 pt は選んだ範囲の感度幅、掃引条件 (566 K・185.1 kPa) と本番が違う | 採用 | 「固定粘性比での Tu 感度」「選択範囲での感度幅」と書き、本番の +6.6 % への移し替えはしない |
| M10 | リップル振幅の式と説明が 2 倍食い違う | **採用 (検算で確認)** | 合成列 $z_i=a(-1)^i$ で log 交番射影 = 2a = 隣接差、局所 2 節点振幅 = a。「0.36 % → 隣同士が 0.7 % 違う」は誤り。指標を 1 つに統一し説明を直す |
| M11 | リップルの原因排除と「$h$ を 0.1 pt 動かさない」が測った指標を超えている | 採用 | 各対照で実際に評価した量に限定。$h$ への影響は同じ対照の地域 bias 差で示す |
| M12 | 「y⁺ < 2」の根拠が特定されていない。既定目標は $y_1^+\le1$ | **採用 (実測して FAIL)** | `check_wall_resolution.py`: C3X $y_1^+$ 平均 1.16 / 最大 1.81 / 1 超過面積 67.7 % → **FAIL**、MkII 1.22 / 2.33 / 65.0 % → **FAIL**。緩和の裏付けとして第一セル厚 2→1 µm の壁法線感度 run (`case/53/run_0120_hwall1um`) を実施 |
| M13 | 局所の恒等式 ($Q_{eff}=I$) と全体収支の不動点条件を混同。$F_w$ の符号規約が無い | 採用 | $F_w=-\sum F^E$ を明記し、「未収束で残るのは全体収支の内部残差項」と分けて書く。`q_eff` 採用と 2.5 % を誤差幅にしない判断は維持 (codex も撤回不要と明記) |

## Minor

| # | 指摘 | 採否 | 対応 |
| --- | --- | --- | --- |
| m1 | 「出口マッハ数だけが違う」が条件表と矛盾 ($T_w/T_g$ 0.73/0.68、入口 M・圧力・ピッチ・翼形も違う) | 採用 | 「同じ試験系列の、近い Re・同じ公称 Tu の 2 条件」に直し相違を列挙 |
| m2 | Mark II の不確かさ要約範囲が `uncertainty.py` と合わない (42–55 % の ±10.0 等が落ちている)。圧力面は 8 帯 | 採用 | 実際の帯境界で要約を作り直す |
| m3 | Tu 3→15 % は $k\propto Tu^2$ で 25 倍 (本文 22 倍)。57.4 は旧 run の値。単位が無い | 採用 | 実入力比に訂正、m²/s²・s⁻¹ を付記 |
| m4 | 報告の図 17 は run 46 で、run 42 と「同じデータ」ではない | 採用 | 図 17 は不確かさ表示の例と書き分ける |
| m5 | 地域平均を「面全体が合う」表現に広げている | 採用 | 「評価節点の 54 % が帯内」等に具体化、重みと内挿方法を表注に |
| m6 | 章参照の誤り、「converged solution」、run 一覧の漏れ (dual-time 2 本など) | 採用 | 参照を修正、「報告量が準定常になった未収束場」と書く、一覧を補完 |

## 付記

レビュー後にユーザ指摘から追加で判明した事項 (本レビューの指摘ではないが同じ改訂に入れる):
§04 の「forge 0.021 % 対 SU2 0.011 %、2 倍」は Figure の run (`run_0108`) の数字ではなく
`run_0088` 系列のもの。同じ測り方での正しい対比は **0.045 % 対 0.011 %、4 倍**。
さらに Kato–Launder を切ると 0.027 → 0.038 %、SU2 の入口 k でさらに 0.045 % と**悪化する**。
