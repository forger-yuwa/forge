# codex レビュー — モデル分担とエスカレーションの仕組み (2026-09-22)

対象 plan: 無し (運用ルール。正本は [`AGENTS.md`](../../AGENTS.md)「モデル分担とエスカレーション」)
対象: コミット `df82b46e` — `AGENTS.md` の新節、`.claude/agents/{run-watcher,implementer,diagnostician}.md`、
`plans/_template.md` §5.1 担当列、および関連フック 3 本 (`hook_forge_guard.py` / `forge_gate_claims.py` / `hook_convergence_gate.py`)
プロンプト: [`2026-09-22-codex-model-tiering.prompt.md`](2026-09-22-codex-model-tiering.prompt.md) / 生ログ: 同名 `.log` (git 追跡外)
実行: 自由形式 `codex exec` (read-only, reasoning high, 39,477 tokens)

## 依頼内容

上位モデル (Fable) を判断の場面だけに使うための分担・委譲・エスカレーション条件の健全性。当方が疑っている弱点 6 点
((a) 条件 3・4 が自己認識依存 / (b) サブエージェントの `session_id` と Stop ゲートの整合 / (c) ブリーフが誤った枠組みを固定する /
(d) 既存ルールとの矛盾 / (e) 長時間 run を下位サブエージェントに持たせる妥当性 / (f) 費用対効果) を先に渡し、
判定・指摘 (`ファイル:行` つき)・最初に直す 1 点を求めた。

## 結論 (原文要約)

**判定: GO-with-changes。Critical 0 / Major 6 / Minor 2。**
推奨構成は「Opus を主セッションにし、**親が run の所有・完了確認・共有文書の更新を管理**し、Sonnet に有限の作業を委譲、
判断時に Fable を呼ぶ」。役割分担の方向は妥当だが、「ゲートが拾うから任せられる」という前提が実装より強い。
最初に直すべき 1 点: **親が委譲した run を、親の終了・報告時に必ず検査対象へ含めること**。

## 当方の採否

指摘の事実はフックのソースで確認した。M1 は codex が「仕様未確認」とした点を**実測して決着**させた。

| # | 指摘 | 採否 | 対応 |
| --- | --- | --- | --- |
| M1 | 子が別 `session_id` で claim すると、親の台帳は不在 = 空集合 (`forge_gate_claims.py:67`) になり、親の Stop ゲートが子の run を無言で検査しない | **前提を実測で否定 → 台帳の作り替えは却下、測り方を採用** | 親と haiku サブエージェントの双方で `echo "run_case.sh case/99.claimprobe_*"` を打ち、**両方が同じ `~/.cache/forge-gate-claims/<親 session_id>.txt` に claim された**ことを確認 (プローブ行は削除済み)。現行の Claude Code では子の run は親の検査対象に入る。run パス単位の所有台帳への作り替えは、壊れていない経路の再設計なので見送り。挙動が将来変わる可能性と測り直し方を `AGENTS.md` に書いた。「同じ case の別 run を分離できない」は今回の変更以前からの制限で、範囲外 |
| M2 | ゲートの保証範囲が過大 (`hook_convergence_gate.py` は VERDICT ファイルの有無と時刻だけ、120 秒未満・180 分超は対象外)。`run-watcher` が背景実行したあと「完了後」へ戻す責任が未定義 | **採用** (Stop フックの改修のみ見送り) | 事実確認済み。`AGENTS.md` の前提を「フックが保証するのは判定の痕跡まで。VERDICT が揃ったことの確認は親の責任」に修正。`run-watcher` を `prepare` / `early-check` / `post-check` の 3 モードに分け、状態 `PREPARED / RUNNING / CHECKED / FAILED`・未実施チェック・次に親がやることを返却形式に追加。**長時間 run のジョブは親が `run_in_background` で持つ** (完了通知が親に届く)。Stop フックが「失敗 VERDICT でも通す」のは、失敗を正しく報告して終わることを許すためで、codex も許容としているので変えない |
| M3 | 条件 3・4 が意味の認識に依存、「諮っていない (理由)」が前進禁止と結びついていない、正常に見える result の解釈が条件に無い | **採用** | 条件 3 を「plan §6 に事前に書いた参照値・許容差との比較が FAIL/判定不能 (事前に書いていない比較も含む)」、条件 4 を「§5.1・承認済み手順に無い修正・再実行へ進む前」に置換。条件 2 の「2 回」は run パスで数える。**条件 7 (result 段の解釈を確定する前。予想どおりに見えるときも)** を追加。「諮っていない」は保留の記録であって通過ではないと明記。テンプレート §5.1 に「`F` 項目の完了には『判断: 日付・結論』が要る」を追加。`check_plans.py` での機械的強制は未実装 (運用で漏れが出たら足す) |
| M4 | 「既に潰した候補」を渡し「出し直さない」と先に指示する形式が、呼び出し側の誤った除外を上位モデルに固定する | **採用** | ブリーフを「観測事実 / 期待値と出典 / 再現条件 / 実施済みの操作と結果 / 仮説」に分離し、各試行に run パス・commit・実効設定差分・判定区間・VERDICT を付ける (`AGENTS.md`)。`diagnostician` は「除外済みのラベルを信用せず証拠を確認、不足なら候補へ戻す」に変更し、返却形式に「呼び出し側の前提への異議」を追加 |
| M5 | `run-watcher` が指定外の数値設定変更 (`wallTreatmentSST: 1` の自動削除) を行い、同じファイルの「指定差分以外を変えない」と矛盾 | **採用** | codex は読めていないが、`wallTreatmentSST: 1` の不使用は `procedures/recommended-settings.md:114` にユーザ方針として書かれている。ただし指摘の本旨は正しい — **自動で落とすと壁関数用メッシュ (y⁺ 30–80) で低 Re 計算をすることになり、比較条件も黙って変わる**。`bndFirstOrder` の除去 (正本が義務づけ) だけを例外として残し、`wallTreatmentSST: 1` は投入せず `FAILED` で親に返す形に変更。`implementer` の一律禁止も `recommended-settings.md` への参照に統一 |
| M6 | 連番の自己採番と README の共有更新が並行セッションと衝突する。`implementer` が未指定の作業項目を自己選択できる | **一部採用** | 採用: run 名は親が決め、`run-watcher` は `mkdir` の失敗で衝突を検知 (番号を振り直さない)。`implementer` は項目番号が無ければ編集せず返す。plan 更新・commit/push・run 名決定の責任者は親と `AGENTS.md` に明記。**却下: README の同期を親に戻す案** — リポジトリに排他制御の仕組みは無く、親が書いても他セッションとの衝突は消えない。親 (中位) が長い表を読み直すコストだけが増える。代わりに「自分の run の 1 行だけを、編集直前に読み直して追記」に限定した |
| m1 | 費用削減と実行時モデルが実測で裏付けられていない。Fable 親を許すと往復費用が残る | **採用** | 実測を 1 つ追加: サブエージェントは `echo` 1 回でも **47,499 トークン**を消費する (AGENTS.md + メモリ索引 + ツール定義の固定コスト)。`diagnostician` は編集単位でなく判断単位でまとめて呼ぶ、と `AGENTS.md` に書いた。セッション別の実費での確認は運用開始後の残作業。既定モデルの Opus 化は `~/.claude/settings.json` (ユーザのグローバル設定) なので未変更 |
| m2 | `diagnostician` の「読み取り専用」は `Bash` を持つ以上、指示上の制約でしかない | **採用** | 「指示上の編集禁止 (Edit/Write は持たないが Bash は持つ)」に表記を改めた (`AGENTS.md`・`diagnostician.md`) |

### (a)–(f) への codex の回答のうち、記録しておくもの

- (e) 「数時間の完走責任を一回の子呼び出しに持たせる設計は推奨しない」— 採用 (M2 の対応)。
- (f) 「削減は未立証。Fable 親の継続、診断ごとの再読み込み、不足ブリーフによる往復で増える経路がある」— その通りで、
  今回の 47k 固定コストの実測がその 1 つ目の裏付け。

### 未対応で残したもの

- サブエージェント 3 本を実際の作業で呼ぶ試験 (本セッションでは定義を読み込めないため未実施)。
- `F` 項目の判断記録を `check_plans.py` で機械的に検査すること。
- セッション別の実費計測。
