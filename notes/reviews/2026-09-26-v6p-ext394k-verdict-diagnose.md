# codex 諮問 (diagnose): v6p-ext394k-verdict

- **brief**: [`notes/reviews/briefs/2026-09-26-v6p-ext394k-verdict.md`](../../notes/reviews/briefs/2026-09-26-v6p-ext394k-verdict.md)
- **plan**: [`plans/active/boundary-conjugate-heat-transfer.md`](../../plans/active/boundary-conjugate-heat-transfer.md)
- **date**: 2026-09-26
- **commit**: `136e5166` (feature/cht-phase2-fem2d)
- **codex**: effort `high`, 4.2 min, rc=0
- **結論**: **総合「判定不能」を維持し、`20/200` だけを同じ設定で追加294,000 step、累積688,000 stepまで延長して、固定211節点で再判定する。**
- **extra**: `case/58.conjugate_slot/README.md`

本文は codex の最終メッセージをそのまま転記。結論は**仮説**であり、提案 A/B で確かめる。採否・反映は plan 側 (§5.1 担当列 F の判断欄) に書く。

---

採否表。**V6′ の現時点の総合判定は「判定不能」**とする。`20/50`・`5/200` の登録条件 A 達成は採用するが、`20/200` の未静定を免除しない。

| 重大度 | 対象 | 採否・根拠・対案 |
|---|---|---|
| Major | 総合を PASS／条件つきで閉じる | **却下**。`20/200` は `Tw_m300_208`〜`212` が `TRANSIENT-UNSETTLED`。事前登録の「局所 ALL STEADY」を満たさない。「基準・比較2条件は合格、残る1条件は判別未完」と記録する。[判定記録:211](/home/sano/work/forge-cht/case/58.conjugate_slot/run_0013_v6p_df20_i200_ext394k/QUASISTEADY_band.txt:211) |
| Major | 「進行尺度＝更新数/`Df_scale`」を確認済みとする | **却下**。尺度392での最終値の近接は、過渡履歴の相似性や392の必要十分性を証明しない。全域の固定点も未確認。「今回の結果と整合する経験的目安」に留める。根拠は [plan §5.1 #102](/home/sano/work/forge-cht/plans/active/boundary-conjugate-heat-transfer.md:618) の比較条件と、下記の未収束判定。 |
| Major | 全域 G-if・流体残差を報告項目として扱う | **採用。ただし主張の範囲を限定**。4条件とも流体残差は `NOT CONVERGED (stalled/plateau)`、全域 G-if も `NOT CONVERGED`。登録された深部帯の検証と、全域CHT解の収束を分けて書く。「全域収束」「固定点の設定非依存を実証」とは書かない。 |
| Minor | 直ちに尺度392まで追加延長する | **修正採用**。延長自体は支持するが、次は **追加294,000 stepだけ**を事前登録する。現在残るのは減衰中の局所温度過渡であり、392までの一括投入を必須にする根拠はない。なお7840更新を狙う場合、初回 `warmup` 2000を含めて累積 **1,570,000**、追加 **1,176,000** step。 |
| Minor | `--fix-band` の PASS を帯の適格性確認にも使う | **修正が必要**。[`eval_v6p.py`:158](/home/sano/work/forge-cht/case/58.conjugate_slot/eval_v6p.py:158) は固定帯を座標だけで選び直し、その全行が `ok` を満たすか検査しない。次回は固定211節点の `Pe`・線形残差・勾配比も明示的に確認する。**今回の4条件は独立再計算で全て適格だったため、今回の判定は覆らない。** |

結論: **総合「判定不能」を維持し、`20/200` だけを同じ設定で追加294,000 step、累積688,000 stepまで延長して、固定211節点で再判定する。**

第 1 仮説: **`20/200` の深部帯に残る差の主因は、連成温度の緩和過渡である。** 確度: **中**

根拠: 読み取り専用で評価器の判定部分、`check_convergence.py`、`check_quasisteady.py` の判定関数を実行し、節点ログから帯内 G-if も再計算した。以下はすべて `case/58.conjugate_slot/` 配下。

| run | 準定常 VERDICT | 帯内 G-if VERDICT | (e) [%] | 最終場の基準との差 T / q [%] |
|---|---|---|---:|---:|
| `run_0012_v6p_df5_i50_nlog/` | `ALL STEADY` | `PASS` | 0.0011 | 基準 |
| `run_0013_v6p_df20_i50_ext394k/` | `ALL STEADY` | `PASS` | 0.0025 | 0.0000 / 0.0030 |
| `run_0013_v6p_df5_i200_ext394k/` | `ALL STEADY` | `PASS` | 0.0015 | 0.0000 / 0.0017 |
| `run_0013_v6p_df20_i200_ext394k/` | **`TRANSIENT-UNSETTLED`** | `PASS` | 0.0278 | 0.0149 / 0.0143 |

準定常の判定区間は、基準が local step **51,000–99,950**、延長runが **147,000–293,950／293,800**。設定は登録どおり `--tail 0.5 --drift 0.001 --osc 0.001`。帯内 G-if は末尾80更新で判定した。最終場の差は静定判定とは別の参考値である。

`20/200` の帯内上端 **i=212** では、延長後半の壁温が **394.250030 → 394.290253 → 394.309886 K** と増加し、物理残差 `r/A` は **−5.142 → −2.803 → −1.181 W/m²** と縮小している。ただし、その量の VERDICT は依然 **`TRANSIENT-UNSETTLED`**。これは緩和過渡を支持する観測であり、到達する固定点の証明ではない。

反証条件: 固定帯の適格性・局所準定常・帯内 G-if・保存性を満たした後も、基準との温度または熱流束の差が **0.5 %を超える**こと。その場合、登録許容内での「差は過渡だけ」を棄却する。

第 2・第 3 仮説: 追加しない。全域未収束による流体応答の影響は未除外だが、現時点で主因として順位を付ける証拠は不足している。

判別 A/B: 変更するのは**計算長だけ**。`run_0013_v6p_df20_i200_ext394k/` の同時刻の流体・壁温・固体状態を新しいrunへ引き継ぎ、追加294,000 stepを回す。基準は `run_0012_v6p_df5_i50_nlog/` に固定する。

- **共通前提**: 帯は −19.779487〜−4.166299 mmの211節点を維持し、帯の適格条件と (a)〜(f) を再確認する。
- **静定・釣合い**: 新しい延長区間の末尾半分、累積 **541,000〜687,800 step** で帯平均・全局所 T/q が `ALL STEADY`。最後の80更新で帯内 G-if `PASS`、(e) ≤0.1 %。
- **A**: 上記を満たし、基準との差が T/q とも ≤0.5 % → この設定範囲・固定帯・登録許容内の感度検証を合格とする。
- **B**: 静定・釣合いを満たすが差 >0.5 % → 「過渡だけ」を棄却する。ただし全域未収束なので、直ちに全域固定点の設定依存と断定しない。
- **いずれも満たさない**: 判別未完。累積688kは診断の打切り時点であり、収束を保証する長さではない。

やらない方がよいこと: 帯を縮める、準定常の閾値を緩める、`Df_scale` を変更した結果で元の `20/200` を合格扱いする、丸め表示の温度差 **0.0000 %** を厳密一致と扱うこと。

呼び出し側の前提への異議: 非静定5節点の実際の drift は **0.05099–0.06212 %/tail**。表示の「0.1 %」は丸めであり、通常閾値0.1 %超過ではなく、**末端極値かつ半閾値0.05 %超過**の条件で未静定になっている。[判定実装:297](/home/sano/work/forge-cht/solver_density_cuda/tools/check_quasisteady.py:297) また、ブリーフの代表点 **i=213、y=−4.119 mmは比較帯外**なので、帯内の根拠には上記i=212を使う。

不足情報: この追加判別を選ぶための不足情報はない。全域固定点の一意性・設定非依存性を確定する証拠は不足している。

ファイル変更・`forge` 起動は行っていない。**plan 未反映**。呼び出し側が [plan §5.1 #102](/home/sano/work/forge-cht/plans/active/boundary-conjugate-heat-transfer.md:618) と [case READMEのrun一覧](/home/sano/work/forge-cht/case/58.conjugate_slot/README.md:44) に採否・追加判別条件を反映する。
