# 超音速ノズル設計ツール (親計画)

## メタ

- **area**: `tooling / optimization`
- **status**: `draft`  <!-- ユーザレビュー・リバイズ前提の初稿 -->
- **related_docs**:
  - [`notes/investigations/nozzle-optimization-tool-survey.md`](../../notes/investigations/nozzle-optimization-tool-survey.md) (技術調査。アーキテクチャの出典。**本計画と食い違う場合は本計画が優先**)
  - `methods/design/` (新設予定。各フェーズ着手時に現在仕様を書く)
- **related_plans**:
  - [`condensation-nonequilibrium.md`](../accepted/condensation-nonequilibrium.md) (凝縮確認メニューの評価器)
  - [`thermophysics-multicomponent-tpgas.md`](../accepted/thermophysics-multicomponent-tpgas.md) / [`boundary-inlet-profile.md`](../accepted/boundary-inlet-profile.md) (入口全圧・全温・組成・分布 BC)
  - 各フェーズ着手時に変更単位の子 plan を `plans/active/` に起票し、本計画の §5 表とリンクする
- **created**: `2026-08-03`
- **owner**: `sano`

## 1. 目的

forge を粘性評価器とする超音速ノズル設計ツールを構築する。対象は 5 機種 (① 風洞ノズル軸対称 / ② 風洞ノズル矩形 / ③ スラスタベル / ④ デュアルベル / ⑤ SERN)。フロー空間設計変数 (中心線マッハ Bézier / 壁圧分布) + 帰還エンジンによる形状クロージャ + サロゲート支援多目的最適化 (随伴不採用) という基本構成はサーベイのパート B を採用する。完了時には「問題定義 → DOE → 帰還付き forge 評価 → パレート → 確認 CFD メニュー」が一気通貫で回り、最終的に問題定義そのものを AI とユーザの対話で組み立てられる状態を得る。

## 2. スコープ

- **やる**:
  - 問題定義 (設計変数・目的・制約・確認メニュー) を機械可読な設定ファイル (YAML) として最初から一級インターフェース化する
  - 区分構成ジオメトリ生成器 + MOC 初期形状、トポロジ固定メッシュ再生成、バッチ評価 CLI、目的関数ライブラリ (サーベイ B4.5 定義)
  - 帰還エンジン (Euler 帰還 + 積分 δ\* / NS 場特性線トレース帰還) と、壁更新→再メッシュ→warm restart のパイプライン (§4.3)
  - サロゲート支援 MOO ループ (co-kriging 多忠実度 + NSGA-II/III + EHVI infill; フレームワーク選定は §4.2)
  - 最適化後の**有望形状の数点抽出 + 特殊解析メニュー** (風洞: 複数入口条件の凝縮計算、スラスタ: 高度別計算 — §4.4)
  - 成果物ツール: CAD (STEP) / 3D プリンタ入力 (STL) / 報告書生成 (§4.5 の 5a)
  - 最終フェーズで **AI 対話・自動化層** — ヒアリング → 問題定義 → 試計算+承認 → 実行監視 → 成果物 (§4.5)
- **やらない**:
  - 随伴法・forge への AD 導入 (サーベイ B4 の不採用判断を踏襲。再考トリガ: ⑤ 3D FFD 変数が 30–50 超でサロゲートが回らない場合に SU2 併用を検討)
  - ソルバ内 moving mesh / ALE (帰還は run 間の外側ループであり不要。ピントル過渡等の動的メッシュは対象外)
  - ML サロゲート (DeepONet 等) の採用 (成熟度未検証。infill 補助としても当面入れない)
  - 凝縮の最適化制約化 (§4.4 の通り「確認」に格下げ。サーベイ B4.5 の制約表から変更)

## 3. 関連 docs と前提

- 技術的背景・文献・レイヤ構成 (L1–L5)・設計変数 (モード K/F)・帰還原理・メトリクス定義は全てサーベイに集約済み。本計画には**差分と決定のみ**書く。
- forge 側の前提資産: 軸対称/2D/3D RANS k-ω SST・多成分 TP・非平衡凝縮・block-DPLUR 陰解法・GPU、`interp_field.py` / `check_mesh_quality.py` / `check_convergence.py` / `check_quasisteady.py`。
- ユーザ確定事項 (2026-08-02):
  1. 固定条件 (入口全圧・全温・組成・分布) は当面 forge BC 直書きでよい。**最終的には解析条件・設計変数・目的関数・最適化フローを AI 対話で定める** (開発の最後)。
  2. 最適化は OpenMDAO を志向するが EHVI infill の可否が課題 → §4.2 で選定 (pymoo+SMT が優れるならそれでもよい)。
  3. 逆設計で壁が動くため再メッシュ機能の計画が必要 → §4.3 で提案 (**方式はユーザが判断**)。
  4. 凝縮は制約でなく確認。風洞: 設計点で最適化 → 複数入口条件で凝縮計算 → 出口マッハ数・出口動圧の性能低下を評価。スラスタ: 同様に最適化 → 有望形状のみ特定高度で計算。
- **plan 運用 (旧記述の改訂)**: サーベイ冒頭の「本格着手時は機種別 plan へ分割」は旧ルール。現行ルールでは plans は**変更単位**で起票する — 本ファイルを親計画とし、各フェーズ (§5) の着手時に `plans/active/` へ子 plan を作り `plans/README.md` に登録、完了で `accepted/` へ移動する (親計画 [`gpu-implicit-plan.md`](../accepted/gpu-implicit-plan.md) / [`discretization-median-dual.md`](../accepted/discretization-median-dual.md) と同じ運用)。

## 4. 設計方針

### 4.1 アーキテクチャと問題定義 YAML

レイヤ構成はサーベイ B1 (L1 設計変数 → L2 形状クロージャ → L3 forge 評価 → L4 目的/制約抽出 → L5 サロゲート MOO) を踏襲する。本計画での追加判断は 1 点:

**「問題定義 YAML」を Phase 0 から唯一の入力インターフェースにする。** 1 案件 = 1 YAML で、機種・設計変数 (選択・固定値化)・固定条件 (forge BC)・目的/制約 (B4.5 のメトリクス名から選択)・帰還モード (K/F, Euler/NS)・DOE/infill 予算・**確認 CFD メニュー** (§4.4) までを宣言的に記述する。理由: (i) run の再現性と記録 (case README との連携) がファイル 1 個で済む、(ii) 最終フェーズの AI 対話層 (§4.5) は「対話でこの YAML を組み立てて実行する薄い層」になり、ツール本体の改造なしに載る。

### 4.2 最適化フレームワーク選定 【決定 2026-08-03: 案 A (pymoo + SMT)。EHVI 自作が重ければ A' へ昇格】

要件: 多目的パレート、Kriging/co-kriging 多忠実度 (Euler 帰還=低 / NS 帰還=高)、**EHVI (または同等の多目的ベイズ) infill**、forge 評価の並列バッチ、全 Python。

| 案 | 構成 | 評価 |
| --- | --- | --- |
| **A (推奨)** | **pymoo + SMT を核**。サロゲート = SMT (`KRG`/`MFK` co-kriging)、探索 = pymoo NSGA-II/III (サロゲート上)、infill = EHVI を小規模自作 (2 目的は解析式 [Emmerich 系] で実装容易、3 目的以上は Monte-Carlo HVI)。評価パイプラインは素の Python 関数 (design vector → metrics dict) | 依存が軽く、**多忠実度 co-kriging (MFK) が第一級**なのが決定打。EHVI 自作分は ZDT/DTLZ ベンチで照合検証する負担を負う |
| **A'** | A のまま、EHVI acquisition だけ **BoTorch (qNEHVI)** を借用 | 自作検証負担が消え、並列バッチ提案・ノイズ耐性は既製最強。代償は torch 依存 (RTX 3060 で動作可)。SMT サロゲートと BoTorch GP の二重化をどう整理するかが設計点 |
| **B** | **BoTorch/Ax を BO エンジン一本に** | qNEHVI・多忠実度 GP とも既製だが、SMT の MFK ほど co-kriging が素直でなく、pymoo と役割重複。ツール全体が torch 色に染まる |
| **C** | **OpenMDAO を骨格に** | **標準 Driver は DOE/GA/DE/勾配系のみで EHVI・ベイズ MOO Driver が存在しない** → infill ループは結局外付け自作になる。OpenMDAO の本来の価値 (解析微分の連鎖・多分野連成 MDO) は随伴不採用・ブラックボック評価の本ツールでは使われない |

**推奨: A (必要になったら A' に昇格)。** OpenMDAO は不採用とするが、切り捨てにはしない — 評価パイプラインを「関数 1 個」に閉じて設計しておけば、将来冷却構造・システム連成など真の MDO 要件が入った時に OpenMDAO `ExplicitComponent` で包み直すのは薄い作業で済む (その時点で再評価する)。

### 4.3 形状更新と再メッシュ 【決定 2026-08-03: R1 (TFI 同一トポロジ再生成)。R2 は fallback 常備、R3 不採用】

前提の明確化: 帰還ループは **forge 実行の外側** (run と run の間) で壁を更新する。「計算中のリメッシュ」= ソルバ内 moving mesh は不要で、必要なのは**壁更新 → 新メッシュ → 前の場からの warm restart** を自動で回すパイプライン機能である。

| 案 | 方式 | 評価 |
| --- | --- | --- |
| **R1 (推奨)** | **同一トポロジ・パラメトリック再生成 + index 対応 restart**。Phase 0 のメッシュ生成器を「構造化 (i,j) トポロジ固定・壁スプラインから**代数生成 (transfinite 補間, Gmsh 非経由)**」で設計し、壁更新後も同ノード数・同接続で再生成。場は同 index でそのまま移植し、`wall_dist` は変換時に再計算 | 補間ノイズゼロ・完全に決定的 (サロゲートが要求する写像決定性に直結)・追加実装は生成器の設計方針だけ。ノズルは単連結流路なのでトポロジ固定が自然に成立する。**速度**: TFI は NumPy のみで 10 万ノード級でも数十 ms、`wall_dist` 再計算 (KD-tree) 込みでも 1 パスあたり ~1 s 未満 — forge 実行 (分オーダー) に対し無視できる。Gmsh を使うのは ⑤ SERN の 3D 非構造など TFI で張れない形状のみ |
| **R2** | **`interp_field.py` 最近傍 cross-mesh restart (既存ツール)** | トポロジ/解像度が変わる場面 (DOE 段間の解像度変更、機種切替) 用。運用実績あり。R1 の fallback として常備 |
| **R3** | **メッシュ変形 (RBF/ばね/弾性 warp)** — SU2/MACH-Aero 流 | 新規実装が要り、大変形で品質劣化・累積歪みの管理が必要。壁スプラインから毎回作り直せる本件では利点がない → **不採用** (⑤ SERN の 3D FFD 期にのみ再考) |

運用ゲート: 再生成のたび `check_mesh_quality.py` を通し (AR≤1000 / skew≤0.9)、FAIL は「評価失敗」として optimizer に返す (その設計点を実行不能扱い)。帰還 1 パスの壁変位は小さいので、R1 では restart 直後の過渡は数十 step で消える想定 — Phase 1 で実測して確認する。

### 4.4 凝縮・オフデザインは「確認 CFD メニュー」 (サーベイ B4.5 からの変更)

最適化ループ内では凝縮を**評価しない** (制約から削除)。設計点の最適化は乾き条件 (凝縮モデル OFF) で回す。$dM_c/dx$ の膨張率チェック (Daum–Gyarmathy) は CFD 前の**安価な事前フィルタとしてのみ**残す (明らかな不良候補の足切り。合否は確認メニューで判定)。

最適化完了後、パレートから**有望形状を数点抽出**し (抽出規準の既定: knee 点 + 各目的の端点 + ユーザ指名点。問題定義 YAML で指定)、各形状に対して YAML に書かれた**特殊解析メニュー (確認 CFD)** を自動実行する:

- **風洞 (①②)**: 複数の入口条件 (全圧・全温・組成/湿分のマトリクス) で凝縮 ON の計算を行い、**出口マッハ数・出口動圧の性能低下**を乾き設計値との差として表化する。評価器は検証済みの非平衡凝縮 ([condensation-nonequilibrium](../accepted/condensation-nonequilibrium.md))。
- **スラスタ (③④⑤)**: 有望形状のみ**指定高度 (背圧) のスイープ**で計算し、推力・剥離位置 (④は 2 作動点の遷移健全性) を評価する。
- 共通: 3D 最終確認・URANS 等の難解析もメニュー項目として同じ枠組みに載せる (サーベイ B8 の「最終確認」の実装形)。

### 4.5 AI 対話・自動化層 (最終フェーズ)

フロー全体が実績を持ってから着手する (Phase 5)。到達目標は「ヒアリングから報告書まで」(2026-08-03 ユーザ指示で拡張):

1. **ヒアリング**: ユーザ要望を対話で聞き取り、解析条件・設計変数・目的・制約・特殊解析メニューを問題定義 YAML (§4.1) に落とす
2. **試計算 → ユーザ承認**: 粗い試計算 (DOE 数点 + 帰還 1 パス程度) を回して結果を提示し、**承認ゲート**を通ってから本フローへ
3. **本フロー実行**: DOE → MOO → 有望形状抽出 → 特殊解析メニュー (§4.4) をジョブとして起動・監視・進捗報告
4. **成果物生成**: 選定形状の CAD (STEP) 作成 → 3D プリンタ入力 (肉厚付与ソリッド → STL) → 報告書 (パレート・選定根拠・特殊解析結果の表)

実装方針:

- **オーケストレーション = Microsoft Agent Framework (Python) を第一候補**とする。graph ベース workflow・human-in-the-loop 承認・checkpoint (長時間ジョブの中断再開)・MCP/関数ツール接続を備え、上記 1–4 の要件に直接対応する。ただし 2025-10 公開の新しいフレームワーク (Semantic Kernel + AutoGen の統合後継) なので、バージョン固定と**エージェント層を薄く保つ**こと (ツール本体は YAML と CLI しか見ない — §4.1 の設計がここで効く) で乗り換え可能性を担保する。
- **決定的な処理はエージェントに生成させない**: CAD/STL/報告書は通常の Python ツールとして実装し (CadQuery / pythonocc / FreeCAD headless — [case/37 で構築済みの環境](../../case/37.pintle_nozzle/) と `.venv-mesh` を流用)、エージェントは引数を組んで呼ぶだけ。LLM が直接書くのは要件ヒアリング・YAML 組み立て・報告書の説明文に限定する。
- 最適化ループ (時間オーダー) は会話内で待たず、ジョブ投入 + checkpoint/ポーリング監視で扱う。
- 印刷用ソリッドの注意: OCC 系の shell (肉厚付与) は複雑曲線で壊れやすいので、「壁曲線を法線オフセット → 回転体/ロフトで直接ソリッド化」する shell 非依存の専用生成にする (フランジ・分割等はテンプレートパラメータ化)。
- CAD/STL/報告書ツール自体はエージェント無しでも価値があるため、**Phase 4 と並行で前倒し実装してよい** (§5 の 5a)。

### 4.6 機種別の設計変数・目的関数 (詳細定義と実現性精査)

パラメータ化の理論的根拠 (Bézier・モード K/F・区分構成幾何) はサーベイ B2/A2.5(E)、メトリクスの数式定義 (測定面・重み・符号・無次元化) はサーベイ B4.5 を正とする。本節はそれを**実際に回す初期セット**に確定し、実現性を精査した結果を書く。

#### 共通 (a): 変数の 3 区分

問題定義 YAML では全パラメータを 3 区分で宣言する:

- **spec (仕様固定)**: 案件仕様 — 入口全圧/全温/組成、試験部径・設計マッハ (①②)、要求推力・燃焼室圧・背圧 (③④⑤)
- **derived (派生)**: spec から決まる量。ただし**スロート径 $r^*$ は、境界層排除厚さ δ\* と流量係数 $C_d$ が絡むため CFD を回す前には正確に決められない**。そこで次の「後決め」手順で閉じる (どちらも 1 変数の決定的な補正なので、最適化の探索次元 $d$ には数えない):
  - **$C_d$ (流量係数) とは**: 実際にスロートを通る質量流量 ÷ 1D 等エントロピー理論の質量流量。スロート近傍の境界層とソニックライン湾曲のぶん 1 より数 % 小さい (慣行値 ~0.98–0.995)。理論面積のまま作ると流量 (推力) が数 % 不足する。
  - **①② 風洞の後決め (出口径が強拘束の場合 — これが標準)**: 出口径 $D_e$ を守るとスロート径は**従属量**になるが、それで問題ない — 評価 1 回の内部で次の順に機械的に解消する。(1) $r^*=1$ の**無次元形状**で帰還 (§4.7(c) の逆設計ループ) を収束させ、粘性込みの「出口半径/スロート半径」比を得る → (2) 出口径が $D_e$ に一致する倍率で全体を相似スケーリング (**この倍率がそのままスロート半径** = 従属的に決まる) → (3) スケーリングで Reynolds 数が変わり δ\* の比率が僅かにずれるため、実寸で帰還を 1 パス回して壁を微修正する。壁が動くと出口径が再び僅かにずれるが二次の量であり、再スケール + 1 パスの 1–2 巡で収束する。
  - **過拘束の検出**: $D_e$ に加えて流量 (= スロート径) まで同時に強拘束すると面積比が決まってしまい、今度は設計マッハ $M_d$ が従属になる。($D_e$, $r^*$, $M_d$) のうち独立に指定できるのは 2 つ — 問題定義 YAML の読み込み時に整合チェックで弾く (3 つ指定されたらエラー、どれを従属にするか明示させる)。
  - **③④⑤ スラスタの後決め**: 守るべき仕様は「要求質量流量 (推力)」。$\dot m = C_d\,\rho^* a^* A^*$ から $r^*$ を仮決めし ($C_d$ は相関の見込み値)、**初回 CFD で実測した流量から $C_d$ を求め直して $r^*$ を 1 回補正**する ($C_d$ は $r^*$ にほとんど依存しないので 1–2 回で収束する)。
- **dv (設計変数)**: 最適化が振る。どれでも固定値化できる (B0 方針)

#### 共通 (b): 中心線マッハ Bézier の自由度勘定

CP の x 座標を等間隔固定 ($x(t)=L\,t$) にすると $M_c(x)$ は次数 $n$ の多項式となり、端点条件 1 個 = CP 1 個の拘束として消える。両端 C2 拘束 (①②: スロート側アンカー 3 + 出口一様 $M_d,\,M'=M''=0$ で 3) では**自由 CP 数 $k = n-5$**。出口一様条件が不要な推力系 (③) はスロート側拘束のみで $k = n-2$〜$n-3$。**実効 DV = 自由 CP の M 値 $k$ 個 + 軸長 $L$** とし、$k=2$–$4$ に抑える (次数 $n$ は $k$ から逆算)。単調性は「CP の M 値が単調」(単調カーブの十分条件 — B2) を bound + pymoo repair operator で強制する。壁圧 Bézier (⑤・④延長部) も同じ勘定 (端点 = 内部ノズル引き渡し圧・出口圧)。

→ **実現性: 高**。この勘定で全機種の総 DV 数 $d \le 7$ に収まり、サーベイ B4 の成立前提 (5–15 変数) の下限側で運用できる。

#### 共通 (c): 段階開放の規律

初回はパイプライン検証を優先し**最小 DV セット** (収縮 = Bell–Mehta 5 次固定、$R_u/R_d$ = 慣行値固定) で回す。$R_c$・収縮形状の DV 開放 (サーベイが新規性と位置づける $r_c$ パレート) は同一機種の **2 巡目以降**とし、一度に開くのは +2 変数までを目安にする (サロゲート再構築コストと、結果が変わったときの切り分けのため)。

#### ① 風洞ノズル (軸対称) — モード F 既定

| 項目 | 内容 |
| --- | --- |
| spec / derived | $P_0, T_0$, 組成, 試験部径 $D_e$, 設計マッハ $M_d$ / $r^*$ (**閉ループ派生** — 無次元設計→スケーリング, §4.6(a)) |
| dv 初期 ($d=5$) | $M_c$ Bézier 自由 CP $k=3$、軸長 $L/r_t$、スロート曲率 $R$ ($R_u=R_d$ 単一) |
| dv 開放候補 (2 巡目〜) | $R_u/R_d$ 分離、収縮比・収縮長 |
| 目的 (初期 3 目的) | $\varepsilon_M$ (コア質量流束重み RMS)、$\varepsilon_\theta$ (コア max 流れ角)、$L/r_t$ |
| 目的 (追加候補) | $q_{peak}$ — 下記精査の条件付き |
| 制約 | 収縮部剥離なし ($\tau_w$ 符号)。$dM_c/dx$ 凝縮フィルタは足切りのみ (§4.4) |
| 特殊解析 | 凝縮: 複数入口条件 → 出口 $M$・動圧低下の表 (§4.4) |

**精査 — $q_{peak}$ の壁解像問題 (①②共通の最大リスク)**: $q_w=k\,\partial T/\partial n$ の直接評価は $y^+\lesssim1$ 必要だが、高 Re のスロートでは $y^+\sim1$ と品質ゲート AR≤1000 が両立しないことがある (既知運用知見)。二段構えとする — **ループ内は $y^+$ 30–80 + `wallTreatmentSST=1` の壁関数 $q_w$** (絶対値は粗いが形状間ランキング用)、**採用候補の仕上げ評価 (§4.4 特殊解析メニュー) と報告値は壁解像メッシュ (可能な Re のみ) または Bartz 相関補正**。Phase 3 で壁関数 $q_w$ と壁解像 $q_w$ のランキング整合を 2–3 形状で検証し、**不整合なら $q_{peak}$ は目的から外し特殊解析メニューの量へ降格**する (目的は $\varepsilon_M,\varepsilon_\theta,L$ の 3 本で成立する)。

#### ② 風洞ノズル (矩形) — ① の 2D 平面版

dv・目的・制約は ① と同一 (2D 平面 RANS、幅 $W$ は spec)。追加要素は**側壁 δ\* 積分補正**のみ: 決定的な運動量積分で側壁排除厚さ $\delta^*_s(x)$ を推定し、$2\delta^*_s/W$ 相当を上下コンタへ転嫁する (CONTUR planar の古典実務 — B5)。
**精査**: 積分 δ\* の誤差は探索効率 (パラメータ化中心のズレ) にしか効かず、パレートにはバイアスしない (B3 と同じ理屈で、目的は常に評価した形状の CFD 解から測るため)。ただし**角部渦は積分の外** — Phase 4 の早期に 3D 確認 run を 1 本回して補正式を較正し、乖離が大きい場合も「2D で最適化 + 3D 確認必須」の運用で成立させる。

#### ③ スラスタ (ベル) — TOP 直接幾何 dv 既定 (2026-08-13 改訂)・Phase 2 の初回題材

| 項目 | 内容 |
| --- | --- |
| spec / derived | 燃焼室 $P_0,T_0$, 組成, 設計背圧 $p_a$ / $r^*$ (**閉ループ派生** — 実測 $C_d$ で 1 回補正, §4.6(a)), $C_{F,ideal}$ |
| dv 初期 ($d=3$) | **TOP 幾何**: 初期膨張角 $\theta_n(=\theta_a)$、出口壁角 $\theta_e$、軸長 $L/r_t$ ($R_u=1.5r^*$, $R_d=0.4r^*$ は慣行値固定)。下流壁 = $P_a$ に C1 接続する **TOP 放物線** (2 次 Bézier: $P_a$ で接線 $\theta_n$・出口 $(L,\sqrt\varepsilon)$ で接線 $\theta_e$)。$P_a$ の曲率不連続は Rao 慣行に忠実に許容 |
| dv 開放候補 (2 巡目〜) | $R_d$・収縮系 (§4.6(c))。**モード K ($M_c$ Bézier + アンカー継承) への切替**は任意の research question「フロー空間 dv は TOP 族を上回るか」— Kliegel–Levine 高次級数 (小 $R_d$ 遷音速解) が要るのはこのときだけ |
| 目的 (2 目的) | $\eta=C_F/C_{F,ideal}$、$L/r_t$ |
| 制約 | 設計背圧で過膨張剥離なし (Summerfield $p_w/p_a\gtrsim0.35$–$0.4$) |
| 特殊解析 | 有望形状数点 × 指定高度 (背圧) スイープ → 推力・剥離位置 |
| 検証 | **Rao TOC/TOP 照合** ([case/29] 資産) — §6 Phase 2。意味 = **評価器 + MOO ループの検証** (帰還エンジンは③に関与しないため含まない。帰還の検証は①の CONTUR 照合が担う — §4.7) |

**精査**: $C_F$ = 出口面 $(p-p_a)$ + 運動量積分 + 壁摩擦。$\tau_w$ は既存の壁サーフェス出力から取れる。軸対称積分の $2\pi r$ 重みは目的関数ライブラリ実装時に 1D 等エントロピー解で単体検証する。5 機種で最も実現性が高い — だから初回題材。**旧「モード K 既定」から変更した理由 (2026-08-13)**: ベルには TOP という実証済み低次元 (3 変数) 形状族があり、目標分布→逆設計を経由する利点が③では薄い。これにより帰還エンジン・アンカー (KL+kernel MOC)・逆マーチが③のクリティカルパスから外れ、Phase 0 完了から Phase 2 (MOO) へ直行できる。フロー空間 dv の柱は①② (中心線マッハ, モード F) と④⑤ (壁圧) で不変。

#### ④ デュアルベル

| 項目 | 内容 |
| --- | --- |
| dv ($d=4$–$5$) | 変曲位置 $x_i/L_{base}$、延長長 $L_{ext}/r_t$、延長壁圧 Bézier 自由 CP $k=2$ (CP/PP プロファイル)。**ベースベルは ③ の最適解 (または Rao) で固定** — 同時最適化しない |
| 目的 (3 目的) | $\eta_{sea}$、$\eta_{alt}$、$L_{total}/r_t$ (1 評価 = 2 作動点 = forge 2 run) |
| 制約 | 海面: 剥離位置が変曲点 ± 許容帯に固定 ($\tau_w$ 符号反転位置の帯判定)、高空: 全付着 |
| 特殊解析 | 遷移 NPR 近傍の URANS (遷移ヒステリシス・横荷重は定常 RANS の外) |

**精査**: (i) 定常 RANS の剥離位置は絶対精度に限界 → 制約は帯判定にし、遷移そのものは URANS メニュー送り。(ii) 評価コスト 2 倍は $d$ を絞って相殺。(iii) ベースベル固定は探索空間を大きく削る割り切り — ベース依存性が疑われたら 2 巡目でベース CP を 1–2 個だけ開放する。

#### ⑤ SERN — **2026-09-04 改訂: 子 plan [tooling-nozzle-sern-chain.md](tooling-nozzle-sern-chain.md) に差し替え**

| 項目 | 内容 |
| --- | --- |
| dv ($d=6$) | **平面最大推力理論の key point** $(M_c,\theta_c,\dot m_c/\dot m)$ + ランプ初期角 $\theta_{r0}$ + カウル初期角 $\theta_{c0}$ + カウル長 $L_{\rm cowl}$。壁座標・壁圧は dv にしない |
| 形状クロージャ | 燃焼器出口 starting line (超音速一様、既定) → 非対称 2 壁前進 MOC (kernel) → key point から目標 C⁻ を張り壁流線抽出 (CFD なし)。粘性は設計点 RANS 場からの δ\* 一発オフセット、帰還ループなし |
| 目的 | 設計点 $C_T$ / オフデザイン $C_T$ (作動点重み付き) / ピッチモーメント $C_M$ / $L_{\rm ramp}$ |
| 制約 | 幾何包絡、最低 NPR での剥離位置帯、(任意) $C_L$ 符号 |
| 評価 | forge 2D 平面 RANS × 作動点セット (3 ブロック: 内部 / カウル下外部流 / 下流)。低 NPR は RSS/FSS で `OSCILLATING` 統計 |
| 3D | 2D パレート数点の 3D RANS 確認 (側壁・隅 R)。FFD in-loop は乖離時のみ別 plan |

**旧方針 (壁圧 $p_w(x)$ Bézier dv + 局所 $p\to\theta$ 帰還 + 内部ノズルを $M\approx1.1$ まで対称設計 + 3D FFD in-loop) を撤回した理由**
([調査ノート](../../notes/investigations/sern-design-method-survey.md) §3): (i) ランプ上の流れは単純波でなく
(カウル角部扇の反射・カウル後縁波・せん断層)、局所帰還は入射波の到達区間で破綻する、(ii) $p_w$ の形を選ぶ理論がなく、
最適解は制御面 (最終 C⁻) 上で閉じる — ③で TOP 幾何 dv を選んだのと同じ理屈で低次元の key point が優る、
(iii) スクラムジェット SERN の入口は燃焼器出口の超音速流で遷音速解は不要、(iv) 文献は非粘性設計 → RANS 検証で
帰還ループを回した例がなく、3D MOC の利得も推力 +0.45% に留まる。壁圧規定は剥離制約の判定量と二段膨張オプションに限定する。

### 4.7 壁の所有権と帰還エンジンの具体アルゴリズム

**【2026-08-13 改訂: 適用範囲と実装段階】** 本節の逆設計・帰還機構は **①②④⑤用** (③は TOP 直接幾何 dv で帰還を使わない — §4.6③)。初陣は① (モード F) とし、次の 3 段で実装する (Euler 先行、NS フルトレースは最後の砦):

1. **v1 (MOC 逆設計)**: starting line + 軸目標 → 逆マーチで場を充填 → 質量流量法 (各横断特性線上で $\int\rho u\,dA$ = スロート流量となる点) で壁流線を抽出 → **δ\* 経験式** (Edenfield 系相関 or 運動量積分) を法線オフセット。CFD なし・ミリ秒。初期壁生成と DOE 最低忠実度層を兼ねる
2. **v2 (Euler 帰還)**: forge Euler (`visc: 0.0` + `thermCond: 0.0` — `viscMethod: 0` は定数粘性であり非粘性でない点に注意) で検算し、軸 $M(x)$ 残差を **v1 の特性線網が与える軸↔壁対応マップ (凍結)** で壁へ帰還する。(c) の全場トレース・BL 端検出は不要 — 対応マップの粗さは PM 換算と同じ「近似 Jacobian」であり、収束判定を実場で行う限りバイアスは入らない (B3 の論法)。注意: node slip 壁の市松スプリアス流 (未修正) があるため、Euler 帰還 run は cell 実行を既定に検討する
3. **v3 (NS 凍結場トレース)**: (c) のフル仕様。v2 の凍結マップで収束が停滞する場合にのみ実装するアップグレードとして保持

アンカー機構 (Kliegel–Levine + kernel MOC — 下記 (b)) が実際に必要になるのは③にモード K を持ち込む 2 巡目のみ。①②は $R_u=R_d$ 単一円弧で $R_d\gtrsim1$ が標準のため、遷音速解は**既存 Sauer 実装で足りる見込み** (KL 高次は後送、①着手時に $R_d$ 実値で要否判定)。

#### (a) 方式の明確化 — 本ツールは「目標分布による逆設計」が主。壁は直接 DV にしない

| 区間 | 壁を決めるもの | 最適化から見た扱い |
| --- | --- | --- |
| 収縮部 (入口配管 〜 $R_u$ 手前) | 直接幾何: Bell–Mehta 5 次 (収縮比・収縮長) + 円弧への曲率ブレンド | dv (初期は固定 — §4.6(c)) |
| スロート円弧 $R_u$–$R_d$–$\theta_a$ | 直接幾何: 円弧パラメータ | dv |
| 円弧終端 〜 出口 | **帰還エンジンの出力** ($M_c$ 目標 ①〜④ / $p_w$ 目標 ⑤・④延長部) | **dv ではない** (dv は $M_c$/$p_w$ Bézier の CP) |
| ⑤ 3D 段階のみ | FFD 直接変形 | dv (唯一の例外) |

旧ツールの「スロートを跨ぐ単一 5/7 次多項式壁」は**不採用** (A2.5(E) の診断: 曲率が区間内で振動し圧縮波源になる / 遷音速 starting line の曲率一定前提と不整合 / $R_u,R_d,\theta_a$ を直接制御できず Bartz 熱流束・$C_d$・ソニックラインの物理トレードと変数が対応しない)。ユーザ列挙の上流パラメータ (入口配管径・スロート径・円弧・スロート近傍の広がり角) は区分構成の区間パラメータに 1:1 対応する — 多項式を使うのは収縮壁 (Bell–Mehta 5 次) と接続ブレンドのみ。

#### (b) スロート近傍 → 中心線マッハ構成 (モード K の具体化)

「スロート近傍だけ計算 → きれいに繋がる $M_c$ を構成 → 壁角を調整」の 3 段を次で実装する:

1. **アンカーの定義と初期値 (解析)**: 「アンカー」= 目標 Bézier の始点条件 ($x_k$ での $M,M',M''$)。スロート円弧 ($R_d,\theta_a$) を決めた時点で、円弧終端から出る最終特性線が軸に当たる点 $x_k$ までの軸上マッハ分布は幾何の従属結果として決まってしまう (この円弧支配領域 = kernel)。目標 Bézier はその「続き」としてしか書けないので、$x_k$ とアンカー値を CFD なしで求めておく。手順: (i) スロート直近は遷音速で MOC が使えない → **Kliegel–Levine の解析級数** (円弧壁前提、入力は $r^*,R_d$ のみ) で少し下流の全超音速線 (starting line) 上の流れを式の代入で得る、(ii) そこから下流は超音速なので **MOC で前進計算** (壁はこの区間では既知の円弧) し、$x_k$ までの軸上 $M(x)$ を得る (ミリ秒オーダー)、(iii) $x_k$ の $M,M',M''$ に Bézier の先頭 CP を一致させる。**スロート近傍の別途 CFD は回さない** — forge は全域を解くので二重になる上、アンカーが CFD 結果に依存すると「同一 dv → 同一形状」の写像決定性が壊れサロゲートが劣化する (B2 モード K)。非粘性でよい理由: これは「目標を作る」計算であり、実流れは forge が解き、NS 帰還段でアンカーを実場値に更新するため粘性ずれは吸収される (下記 3)
2. **$M_c$ Bézier**: $x_k$ で上記微分値に接続 (①② C2 / ③⑤ C1 — B2)、下流は自由 CP (§4.6(b))
3. **壁角調整 = 帰還エンジン** (下記 (c)) と**アンカー更新 (実場)**。**更新が必要な理由**: 粘性の実場では kernel 区間の軸分布が解析予測からずれるが、円弧壁は凍結されているため**このずれは帰還では原理的に直せない** (円弧終端より下流の壁は $x_k$ より上流の軸に因果的に影響しない)。kernel 区間の分布は「目標」でなく幾何 dv の**結果**として受け入れ、NS 帰還段では目標 Bézier の始点を現在場の固定窓・固定次数 LSQ フィット値 ($M,M',M''$) に**張り替える** (B2 モード K (ii)) — これで継ぎ目の残差が構成的にゼロになり、直せない残差を追って継ぎ目直後の壁が暴れることを防ぐ。始点が動いた分は出口端条件 ($M_d$, $M'=M''=0$) を保持したまま自由 CP が吸収する。粘性 kernel の品質劣化 (小 $R_c$ のソニックライン非一様等) は実場から測る目的関数に現れ、最適化器が幾何 dv を動かして対処する (バイアスなし)。
   **パスの定義と反復構造**: 「パス」= 帰還ループの 1 巡で、順序は **(1) forge を定常収束まで計算 → (2) 残差評価・壁更新 (パス内 1 回) → (3) 再メッシュ・warm restart** (収束判定はバッチ評価 CLI の自動判定 = `check_convergence` 相当)。**壁を変えれば流れが変わるので 1 パスでは目標に一致しない — このパスを反復するのが帰還ループ**であり、次パスの forge 計算が前パスの壁変更を取り込んで残差を測り直す。Euler・NS とも**残差が収束するまでパスを反復する** ((c) の収束基準)。
   **アンカー更新のタイミング — 初回 NS パスで 1 回、以後は凍結 (監視付き)**: 初回 NS パスの収束場で解析アンカー値を実場フィット値に付け替え、**以後のパスでは凍結する**。毎パス付け直すと収束判定の目標側がパスごとに動き、フィットノイズを壁が追ってリミットサイクルに入るリスクがあるため。付け直しの必要性 (解析予測と粘性実場の差の取り込み) は初回でほぼ尽きる — kernel の流れは凍結された上流幾何で決まり、超音速核に下流からの上流影響は存在せず、境界層亜音速部経由の影響も弱い。凍結後も毎パスのフィット値は**監視**し、凍結値からのずれが閾値 (例: 収束許容誤差の半分) を超えたときのみ再更新してログに残す (固定閾値規則なので決定性は保たれる)。再更新が繰り返される場合は kernel 状態が実際に動いている病理 (スロート近傍剥離・非定常) であり、(c) のガードで評価失敗として弾く。いずれの更新・フィットも**発展途中・未収束の場に対しては行わない** (未収束場では残差もトレースもノイズで壁更新が汚れるため、「収束 → 更新」の順序は帰還成立の前提条件)。なおアンカー更新は目標始点を現在場に合わせるだけの受動操作でそれ自体は壁を動かさず、残るドリフトは固定窓 LSQ 平滑化・$x_k$ 近傍重みランプ・壁更新緩和 $\omega$ が抑える。防護は「初回更新+凍結 (監視付き) + $x_k$ 近傍の重みランプ + 円弧着地トレースのマスク ((c) ガード)」の 3 点セット

#### (c) NS 場特性線トレース帰還 — アルゴリズム仕様

入力: forge 場 ($M,\theta$, $\mu=\arcsin(1/M)$)、現在壁 $\theta_w(x)$ スプライン、目標 $M_{c,t}(x)$。1 パスは:

1. **軸分布抽出**: 固定相対 x 格子 (~200 点) へ $M_{achieved}$ を補間し、固定窓で平滑化
2. **残差**: $\Delta M(x_i)=M_t-M_a$。$|\Delta M|>$ tol の station のみ対象
3. **逆トレース**: 各軸点 $(x_i,0)$ から壁→軸波の族 ($dy/dx=\tan(\theta-\mu)$) を**凍結場の中で上流へ逆積分** (RK2・固定相対刻み) し、BL 端 (断面 95% $u_{max}$ — A2 定義) 到達点の真下の壁点 $x_w$ を得る
4. **PM 換算**: $\Delta\nu=\nu(M_t)-\nu(M_a)$、$\Delta\theta_w(x_w)=\omega\,\Delta\nu$ (緩和 $\omega=0.3$–$0.5$、正 = 開く方向)。軸対称の波増幅により PM は**近似 Jacobian に過ぎない**が、収束判定は実 CFD 場で行うため収束結果にバイアスは入らない (粗いと収束が遅いだけ — B3)
5. **基底射影**: $(x_w,\Delta\theta_w)$ 点群を $\theta_w$ スプライン係数増分へ Tikhonov 正則化付き最小二乗で射影。$x_k$ (アンカー) 近傍と出口近傍は重みランプでゼロ化。1 パスの $|\Delta\theta_w|$ はクリップ (例 0.5°)
6. **壁再構築**: $y(x)=y_a+\int\tan\theta_w\,dx$ (円弧終端の位置・角・曲率をアンカー) — 位置・勾配連続が構成的に保証される
7. **R1 再メッシュ → warm restart → 次パス** (§4.3)

運用規律 (B3 の表を実装値化):

- **収束基準 (2026-08-03 改訂: NS も収束までパス反復する)**: DOE = Euler 帰還を残差収束まで (1 パス安価。目安 5–20 パス)。NS 帰還 = **infill 評価も $\|\Delta M\|_\infty \le 0.5\%\,M_d$ に達するまでパス反復**。パス上限 (~10) を安全弁とし、上限到達で未収束なら評価失敗として optimizer に返す (剥離等の病的形状はどのみち収束しないため、ガードとして正しい挙動)。コスト: warm restart で 2 パス目以降の forge 計算は初回より大幅に安く、出発点が Euler 収束済み + δ\* 補正の壁なので数パスで収束する見込み — 旧「固定 1–2 パス」案比で ~1.5 倍に留まる試算 (Phase 2 で実測)。
  - **サーベイ B3「infill は固定 1–2 パス」からの変更 (本計画が優先)**: B3 の固定回数論の根拠は「許容誤差打ち切りはパス数の段差が写像に不連続を作り Kriging に悪い」だが、**不連続の振幅は打ち切り許容誤差のオーダーに縛られる**ため、許容誤差を十分小さく取れば無視できる。正しい教訓は「粗い許容誤差で止めるな」であり、固定回数にする必要はない。収束させる利点: dv の意味が「達成された目標分布」として明確になる (目標未達の中途半端な形状がサロゲートに混入しない)、**最終仕上げ (polish) が独立工程として不要になる** — 採用候補に追加で要るのは帰還反復でなく忠実度引き上げ (メッシュ細分化・壁解像 $q_w$ 等) であり、それは特殊解析メニュー (§4.4) 側で行う
- **ガード**: トレースが $M<1.05$ 領域・剥離域 ($\tau_w<0$) に入った station、および**凍結された円弧区間 ($x_w < P_a$) に着地した station** はマスク (そこは帰還で直せないため — (b)3)。マスク率 >20% でその設計点は評価失敗として optimizer に返す
- **決定性**: station 格子・積分刻み・平滑化窓・BL 端定義・$\omega$・正則化 $\lambda$ を全て問題定義 YAML に固定値で持つ
- **軸目標の定義域 (v1 逆マーチ)**: 壁点の依存域は軸の**両側**に足を持つ (C⁺ 足 = 上流、C⁻ 足 = 下流。壁が発する波が軸に着地するのは下流側で、壁角の情報はそこに書き込まれる)。したがって軸目標は**壁端の C⁻ 足 ($\approx x_{lip}+r_e/\tan(\mu_e-\theta_e)$) まで下流へ延長**して与える。一様出口 (①②) は $M_d$ 到達後「一定」延長で自明。壁長はマーチ軸スパンに対するセカント 1 変数で狙い値に閉じる (2026-08-13 明文化)
- **Euler 帰還版**: 同一アルゴリズムを Euler 場 (粗格子) で回し、物理壁 = 非粘性壁 + $\delta^*(x)$ 法線オフセット (決定的積分法)。BL 端検出は不要 (壁マッハを直用)。**v2 (§4.7 冒頭) では場内トレース自体を省略し、v1 特性線網の凍結対応マップを使う**
- **壁圧帰還版 (④延長部。⑤は 2026-09-04 に主線から外し [tooling-nozzle-sern-chain.md](tooling-nozzle-sern-chain.md) へ — 剥離制約の判定量と二段膨張オプションに限定)**: $\Delta p_w$ が局所壁角に直結するためトレース不要の局所帰還 (ステップ 3 を省略し、4 の換算を $p$–$\theta$ 関係に置換)

#### 計算予算の見積り (実現性の裏付け)

$d\le6$ なら LHS DOE $\approx10d=60$ 点 + infill 30–50 点。1 評価 = Euler 帰還 (分オーダー) または NS 帰還を収束までパス反復 (warm restart で 2 パス目以降は安価、axisym SST で 1 評価 15–45 分/RTX 3060 想定) → **1 機種 1 巡 ≈ 数日**で回る規模。④ の 2 作動点・⑤ 段階 2 はこの 2–10 倍で、[tooling-cloud-gpu-env](tooling-cloud-gpu-env.md) のバッチ並列が効く場面。**Phase 2 で 1 評価の実測時間を取り、本見積りと DOE/infill 予算の既定値を更新する。**

## 5. 実装ステップ (フェーズロードマップ)

各フェーズ着手時に子 plan を起票する (§3 の運用)。順序はサーベイ B8 を踏襲し、③ベルを最小構成の初回題材とする (変数最少・Rao ベルという照合解・[case/29] 資産)。各フェーズの初期 DV・目的セットは §4.6 に従う。

| Phase | 内容 | 子 plan (起票時に確定) | 規模 |
| --- | --- | --- | --- |
| **0. 基盤** | 問題定義 YAML スキーマ / 区分構成ジオメトリ生成器 + MOC 初期形状 + 事前フィルタ / トポロジ固定メッシュ生成 (①〜④: TFI 代数生成→forge h5 直書き、⑤ 3D のみ Gmsh。品質ゲート込み) / バッチ評価 CLI (収束・NaN・準定常判定を `check_*` ツールで自動化) / 目的関数ライブラリ (B4.5 実装) | `tooling-nozzle-geometry-mesh` ほか | 中 |
| **1. 帰還エンジン (2026-08-13: Phase 3 に統合)** | v1 MOC 逆設計 + δ\* 経験式 / v2 Euler 帰還 (凍結特性線マップ) / v3 NS トレース (停滞時のみ) — §4.7 改訂。③が帰還不要 (TOP 幾何 dv) になったため独立フェーズとしては解消し、**① (モード F) の一部として Phase 3 で実装する** | Phase 3 と共起票 | 中 |
| **2. 最適化ループ (③ベル)** | §4.2 の決定を実装。**TOP 幾何 dv ($\theta_n,\theta_e,L$) + TOP 放物線壁** (§4.6③)。DOE → サロゲート → NSGA-II → EHVI infill。η vs $L/r_t$ の 2 目的で **Rao 照合** (評価器+MOO の検証)。**Phase 0 完了から直行 (Phase 1 非依存)** | [`tooling-nozzle-moo-loop`](tooling-nozzle-moo-loop.md) | 中 |
| **3. ① 軸対称風洞** | **帰還エンジン初実装 (v1→v2→v3, §4.7 改訂) + CONTUR/Sivells 照合**。一様性 $\varepsilon_M,\varepsilon_\theta$ / 熱流束 / 軸長。Euler=低・NS=高の co-kriging 多忠実度。**凝縮確認メニュー初実装** (複数入口条件 → 出口 M・動圧低下表) | 起票時に確定 | 中〜大 |
| **4. 機種展開** | ② 矩形 (側壁 δ\* 積分補正) / ④ デュアルベル (2 作動点・壁圧帰還) / ⑤ SERN (**2026-09-04 改訂**: 最大推力 key point dv + 多作動点 RANS → 3D 確認、子 plan [tooling-nozzle-sern-chain.md](tooling-nozzle-sern-chain.md))。スラスタ確認メニュー (高度スイープ) | 機種ごとに起票 | 中〜大 |
| **5. AI 対話・自動化層** | §4.5。**5a**: 成果物ツール (CAD STEP / 3D プリンタ入力 STL / 報告書 — 決定的 Python。Phase 4 と並行で前倒し可) / **5b**: Microsoft Agent Framework 層 (ヒアリング → YAML 策定 → 試計算+承認ゲート → ジョブ実行監視 → 成果物ツール呼び出し) | 5a / 5b で分けて起票 | 中〜大 |

配置の提案: 新ディレクトリ `design/` (Python パッケージ、`solver_density_cuda/tools/` とは分離) に geometry / meshing / evaluate / metrics / feedback / opt / menu のサブモジュール構成。確定は Phase 0 子 plan で。

## 6. 検証

- **Phase 0**: 生成器の決定性 (同一 YAML → bit 同一メッシュ)、品質ゲート、既存 case ([case/23.axi_nozzle], [case/13.nozzle_H]) の手作りメッシュと同等品質の再現。
- **Phase 1 (→ Phase 3 に統合, 2026-08-13)**: 帰還収束性 — 既知目標分布 (Sivells/CONTUR 系コンタの中心線分布) を与えて元コンタを回復できるか。R1 restart の過渡長の実測。
- **Phase 2 (最重要マイルストーン)**: **Rao TOC/TOP ベルとの照合** — TOP 幾何 dv のパレート前線が Rao 点と整合し、かつ Rao を不当に支配しない (支配するなら評価系のバグをまず疑う)。[case/29] の Rao vs 円錐資産を照合解に使う。検証対象は評価器 + MOO ループ (帰還エンジンは含まない)。
  **→ 判定 (2026-08-13): 合格。** 77 評価 (DOE24+infill46+照合7) で確定前線 23 点 (η 0.953@L5 → 0.980@L10, 全点 η 頭打ちポリッシュ済み)。**固定 L=5.9713 の (θn,θe) 直接最適化**で max η(L) を確定させた上で、チャート忠実点 rao80 は **その +0.133% 下 = 判定帯 ±0.15% 内で前線上** (最適は θn24.5–28.5×θe8–11 の平坦台地 — チャート θn33.8° はやや過大という粘性小シフト)。θn を意図的に外した対照点は前線が +0.28% 支配。前線の θe(L) 減少傾向は Rao チャート自身の傾向と整合。詳細は子 plan [tooling-nozzle-moo-loop](../accepted/tooling-nozzle-moo-loop.md) §9。
- **Phase 3**: 出口一様性の CONTUR 設計との比較、Matsunaga 2022 の傾向 (一様性 vs 長さのトレード) との定性照合。凝縮確認メニューは検証済み凝縮モデルの範囲で運用。
- **全フェーズ共通**: AGENTS ルールのツール化 — 各評価 run で `check_mesh_quality` / `check_convergence` / `check_quasisteady` の VERDICT を自動記録し、PASS 以外の点はサロゲートに入れない (失敗扱い)。run は `case/` 配下の専用 case ディレクトリに置き README の run 一覧表を維持する。

## 7. 影響範囲

- 新規: `design/` (ツール本体)、`methods/design/` (現在仕様ドキュメント。各フェーズで更新し `methods/index.md` を同期)
- 既存ソルバ (`solver_density_cuda/`) は原則無改造 (評価器としてブラックボックス利用)。必要になった場合 (出力量の追加等) は個別の子 plan で扱う
- `procedures/` へのツール利用手順の追加は Phase 2 完了後 (実績が付いてから)
- [`notes/investigations/nozzle-optimization-tool-survey.md`](../../notes/investigations/nozzle-optimization-tool-survey.md) 冒頭注記を本計画へのポインタに改訂 (本計画起票と同時に実施)

## 8. 完了条件 (親計画)

- [x] §4.2 (最適化 FW = A: pymoo+SMT)・§4.3 (再メッシュ = R1) のユーザ決定を反映 (2026-08-03)。実装着手時に `status: in_progress` 化
- [ ] Phase 0–3 の子 plan が全て `accepted/` (③ベル Rao 照合 + ①風洞 + 凝縮確認メニューまで)
- [ ] Phase 4–5 の子 plan が全て `accepted/` (5 機種 + AI 対話層)
- [ ] `methods/design/` が現在仕様として整備され `methods/index.md` に登録済み
- [ ] 本計画を `accepted/` へ移動し `plans/README.md` を同期

## 9. 変更ログ

- `2026-08-03` — 初稿。サーベイ (パート B) を基に、ユーザ指示 (2026-08-02) を反映: 凝縮は制約→確認メニューへ変更、AI 対話による問題定義層を最終フェーズとして追加、最適化 FW (§4.2)・再メッシュ方式 (§4.3) を選択肢+推奨で提示 (ユーザ判断待ち)、「機種別 plan 分割」の旧ルール記述を現行の変更単位運用 (親+子 plan) に改訂。
- `2026-08-03` — ユーザ決定を反映: §4.2 = 案 A (pymoo+SMT, 必要時 A')、§4.3 = R1 (TFI 同一トポロジ再生成, Gmsh は ⑤ 3D のみ)。§4.4 を「有望形状を数点抽出 → 特殊解析メニュー」に明確化。§4.5 を Microsoft Agent Framework ベースの対話・自動化層 (ヒアリング → 試計算承認 → 実行監視 → CAD/3D プリンタ入力/報告書) に拡張し、Phase 5 を 5a (成果物ツール・前倒し可) / 5b (エージェント層) に分割。
- `2026-08-03` — Phase 0 子 plan ([tooling-nozzle-phase0-foundation.md](tooling-nozzle-phase0-foundation.md)) 起票・実装開始。実装上の経路調整: §4.3 R1 の「forge h5 直書き」は「msh4.1 直書き → 既存 `convertGmshToForge`」で実現 (トポロジ固定・決定性という R1 の本質は不変。wall_dist・幾何量を検証済み変換器に委譲)。詳細は子 plan §9。
- `2026-08-03` — NS 帰還の収束方針を改訂 (ユーザ指摘): 「infill は固定 1–2 パス」を廃止し、**全 NS 評価を $\|\Delta M\|_\infty \le 0.5\%\,M_d$ までパス反復** (上限 ~10、未達は評価失敗)。B3 固定回数論の根拠 (許容誤差打ち切りの写像不連続) は不連続振幅が許容誤差オーダーに縛られるため厳しい許容誤差なら無効と判断。独立工程としての最終仕上げ (polish) は廃止し、採用候補の忠実度引き上げは特殊解析メニュー側へ。§4.7(b) にパス反復構造 (壁更新→流れ変化→次パスで取り込み) を明文化。
- `2026-08-03` — §4.7 追加 (ユーザ指摘への回答): 壁の所有権の明文化 (逆設計が主・壁直接 DV は ⑤3D FFD のみ、スロート跨ぎ単一多項式壁は不採用で区分構成に確定)、モード K の具体化 (kernel アンカーは Kliegel–Levine + kernel MOC の解析関数、別途 CFD は回さない)、NS 特性線トレース帰還のアルゴリズム仕様 (7 ステップ + 収束/ガード/決定性規律 + Euler/壁圧変種)。§4.6(a) の $r^*$ を「閉ループ派生」に修正 (δ\*/$C_d$ 未知のため事前確定不能 — 無次元設計→スケーリング / $C_d$ セカントの決定的閉ループとし、探索次元には入れない)。
- `2026-08-13` — **Phase 0・Phase 2 完了** (子 plan とも `accepted/` へ)。Phase 2 は同日中に
  ステップ 1–6 を完走: TOP 放物線壁 → `opt/` (EHVI 自作 MC 照合 ≤0.1%・ZDT1 99.96%) →
  バッチドライバ (2 段起動常用・物理ゲート [ṁ/ṁ_1D, η, res_step]) → 本番 50 評価 28 分
  (49 PASS) → 前線ポリッシュ (19/19 settled) → **Rao 照合合格** (§6 Phase 2 の判定参照)。
  次フェーズ = Phase 3 (①風洞: モード F + 帰還エンジン v1–v3 + CONTUR 照合 + 凝縮メニュー)。
- `2026-08-13` — **③の dv 方式を「モード K」から「TOP 直接幾何 dv」へ変更** (ユーザ決定): dv = $(\theta_n,\theta_e,L)$ の 3 変数、下流壁 = $P_a$ C1 接続の TOP 放物線。理由: ベルには実証済み低次元族 (TOP) があり目標分布経由の利点が薄い。モード K は③の任意の 2 巡目 (「フロー空間 dv は TOP 族を上回るか」の research question) に降格し、Kliegel–Levine 高次級数はそのときだけ必要 (後送)。**帰還エンジンは①②④⑤用に確定し、初陣は① (モード F)**: v1 (MOC 逆設計 + δ\* 経験式) → v2 (forge Euler 検算 + v1 特性線網の凍結対応マップで帰還 — 全場トレース不要) → v3 (NS 凍結場フルトレース、v2 停滞時のみ) の 3 段構成 (§4.7 冒頭)。Phase 1 (帰還エンジン) は独立フェーズを解消して Phase 3 (①) に統合、Phase 2 (③ MOO) は Phase 0 から直行。v1 逆マーチの軸目標定義域 (壁端 C⁻ 足まで下流延長・壁長のセカント閉ループ) を §4.7(c) に明文化。
- `2026-08-03` — §4.6 追加: 機種別の設計変数・目的関数の詳細定義と実現性精査。spec/derived/dv 3 区分、Bézier 自由度勘定 ($k=n-5$ 等・実効 DV = 自由 CP + L)、段階開放規律 (初回は収縮・$R_u/R_d$ 固定、$r_c$ 開放は 2 巡目〜)、機種別初期セット ($d=4$–$6$)。精査結果: $q_{peak}$ は壁関数/壁解像の二段構えでランキング整合を Phase 3 検証 (不整合なら目的から降格)、④剥離制約は帯判定・ベースベル固定、⑤3D は段階 1 パレート近傍のみ探索。計算予算 1 機種 1 巡 ≈ 数日 (Phase 2 で実測更新)。
