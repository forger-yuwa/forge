# node の対流再構成: リミッタ評価点の不一致と非物理再構成のフォールバック

## メタ

- **area**: `convection / boundary`
- **status**: `draft`  <!-- 2026-09-19 起票。case/46 R4e の原因調査 (§4.15.6/§4.15.7) から分離。実装着手前 -->
- **related_docs**:
  - [`methods/boundary.md`](../../methods/boundary.md) 「node 等温壁の壁ノード温度ピン」節 (運動量 Dirichlet の 3 点セット)
  - [`methods/convection/`](../../methods/convection/) (MUSCL 再構成)
  - [`methods/limiter.md`](../../methods/limiter.md)
- **related_plans**:
  - 発見元: [`tooling-nozzle-sern-3d.md`](tooling-nozzle-sern-3d.md) §4.15.6 / §4.15.7 (機体ベースの発散)
  - 隣接: [`discretization-node-boundary-ghostless.md`](discretization-node-boundary-ghostless.md) (壁半割面を消す設計。本件はその**内部面**側)
  - 隣接: [`architecture-node-centroid-value-position.md`](architecture-node-centroid-value-position.md) (値の位置と重心のズレ)
- **created**: `2026-09-19`
- **owner**: `sano`

## 1. 目的

node (median-dual) の**対流再構成まわりで SU2 と食い違っている点**を直す。
起票のきっかけは case/46 の機体ベース発散だったが、**調査の結果それは本 plan の対象ではなくなった** (§4.5/§4.6)。
現在の主題は (a) **リミッタの評価点が再構成点とずれている**こと、(b) **再構成・更新の汎用セーフガードが無い**ことの 2 つ。

## 2. スコープ

**対象**: リミッタの評価点、再構成値の物理性検査、更新率の上限、非物理状態のロールバック。
**対象外**: 粘性流束・勾配計算そのもの・壁関数 (`wallTreatmentSST`)・cell モード。
`mesh.bndFirstOrder` は**使用禁止**なので代替案に含めない (粘性応力まで壊し、疑似 2D で全域に効く)。

## 3. 関連 docs と前提

- 壁ノードの Dirichlet は 3 点セット (状態ピン / 残差射影 / 陰解法 Jacobian 行) で
  [`methods/boundary.md`](../../methods/boundary.md) に記載済み。**本件はその 3 点とは別の層 (対流流束の再構成)**。
- 壁半割面の質量流束が 0 であることは [`discretization-node-boundary-ghostless.md`](discretization-node-boundary-ghostless.md) §4 の前提そのもの。

## 4. 設計方針

### 4.1 現行の実装 (実測で確認済み)

壁ノードまわりは 4 層に分かれている。

| 層 | 実装 | 壁ノードでの挙動 |
| --- | --- | --- |
| 状態 | `cuda_forge/nodeWallDirichlet_d.cu` `enforceWallNoSlip_d` | `roU*` と `U*` を 0 に、KE を `roe` から除去 |
| 残差 | 同 `zeroWallDirichletResiduals_d` | `res_roUx/roUy/roUz` (+SST `res_roOmega`、等温 `res_roe`) を 0 に。**`res_ro` は射影しない** |
| 境界半割面 | `convection/convectiveFlux_boundary_d.inc.cuh` | `mdot = sss*(ro_R*Vn_m)` で bvar 状態のみ・**再構成なし**。no-slip なら `Vn_m = 0` で**質量流束は厳密 0** |
| 内部面 | `convection/convectiveFlux_common_d.cuh` `interp_MUSCL_2nd` | `phif = phiC + psi*(grad . cpd)`。node では `cpd` = エッジ中点 (`convectiveFlux_d.cu` の `rem = (discretization=="node") ? 1 : 0`、**config で切れない**) |

**噛み合っていない**: 状態層が `phiC = 0` にしても、内部面の再構成は勾配を足す。
壁のすぐ外が高速なら勾配は大きく、面に有限の速度が乗る。
境界半割面は塞がっているので、内部面の正味が流出ならその CV は補充されずに空になる。

**1 次ではこの問題が起きない**: `phif = phiC = 0` なので、風上が壁ノード側のとき `rho u = 0` となり、
**壁 CV は対流で質量を失えない** (流れが入る向きなら隣接側が風上になるので、入ることはある)。
2 次はこの性質を壊す。

**forge には再構成値の物理性検査もクランプも無い** (確認済み: `convectiveFlux_slau_d.inc.cuh` で
`ro_L`/`P_L` は `interp_dispatch` の結果をそのまま使う。`contactBlend` は組成勾配向けの別機構で既定 off)。

### 4.2 症状と証拠 (case/46, 2 次元平面, node, SST, frozen_tp)

正本は [`tooling-nozzle-sern-3d.md`](tooling-nozzle-sern-3d.md) §4.15.6 / §4.15.7。要点:

- 機体ベース (厚さ 0.02 H、流れに正対する no-slip 壁) の壁ノードで、再構成が面に **457 m/s** を置く
  (節点値は 0)。実装式をそのまま再現した厳密値。
- その 9 ノードの密度が 27 step で **0.00358 → 0.00003 (120 倍減)**、圧力 1159 → 32 Pa。
  周囲のベース帯 41 ノードの平均圧力は 7 % しか落ちない = **壁 CV だけが空になる**。
- **1 次は 40 step 完全に不動**。`nodeWallDirichlet: 0` でも 40 step 完走 (ただし壁ノードが 1500 m/s で動く)。
- **CFL 律速でない** (1/10 の CFL で 6.6 倍の step)。**`implicitRelax` は 1.4 倍の延命のみ**。
- **格子を細かくすると悪化する**: 壁 CV 体積 3.3 倍減 → 落ちる step 2.8 倍早い (ほぼ体積比例)。

**測定器**: `solver_density_cuda/tools/check_wall_cv_drain.py` (本 plan で追加済み)。
**ふるいであって予測器ではない**: 風上化を含まないので偽陽性がある
(本ツールが最大と出したカウル後縁ノードは実際には密度が上がる。判定は `ro` の時系列で行う)。

### 4.3 他ケースとの比較

`case/18.backstep` (後ろ向きステップ = 同じく流れに正対する壁) の node・2 次・`nodeWallDirichlet: 1` の
**収束解**では、速度ピン壁ノード 474 個の漏れは最大 4.1 /s で、1 ms 以内に空になるノードは 0。
SERN のベースは 1.2e5 /s。**同じ機構が 5 桁弱い**。
ただし**収束解と過渡の比較なので公平ではない** (収束解では正味流束は定義上ほぼ 0)。
言えるのは「backstep はその状態に到達できている、SERN のベースは到達前に空になる」まで。

### 4.4 SU2 の実装 (`.external/su2-src`, v8.5.0 を実際に読んだ結果)

**結論: 壁ノードの扱いは forge とほぼ同じで、ここに差は無い。** 期待していた「SU2 は壁ノードを特別扱いしている」は**外れ**だった。

| 論点 | SU2 | forge |
| --- | --- | --- |
| no-slip の課し方 | **強制 Dirichlet のみ** (`CNSSolver.cpp:508-525` `SetVelocity_Old` + `LinSysRes(iPoint,iDim+1)=0`、`:577-581` `Jacobian.DeleteValsRowi`) | 同型 (状態ピン + 残差射影 + 陰解法の行単位化) |
| 壁面からの対流質量流束 | 静止格子では **0** (`Res_Conv = 0`、書き込みはエネルギー行のみ `:543`) | 0 (bvar の `Vn_m = 0`) |
| **壁ノードの連続の式** | **固定しない** (`LinSysRes(iPoint,0)` に触る処理は存在しない) | **固定しない** |
| **壁ノードでの MUSCL** | **特別扱いも除外も存在しない** (`Upwind_Residual` に境界判定なし。`GetPhysicalBoundary` は JST のセンサーにしか使われない) | 同じく特別扱いなし |
| 再構成の目標点 | **辺の中点** (`CEulerSolver.cpp:1925` `V_i + 0.5*lim_i*Project_Grad_i`) | **辺の中点** (node は `g_reconEdgeMid` 常時 1) |
| **リミッタの評価点** | **辺の中点** (`computeLimiters_impl.hpp:175-180` `dist_ij = 0.5*(coord_j-coord_i)`) = **再構成点と厳密一致** | **双対面重心** = **不一致** ← 唯一の実質的な差 |

**forge に無い SU2 の汎用セーフガード (壁専用ではない)**:

1. **再構成の物理性検査 → そのエッジを 20 反復 1 次に固定** (`CEulerSolver.cpp:1940-1962` + `CFlowVariable.hpp:80-87` `UpdateNonPhysicalEdgeCounter`)。負の p/rho/音速のみ。
2. **密度・エネルギー更新率の自動 under-relaxation** (`CFVMFlowSolverBase.inl:599-630`、既定 `MAX_UPDATE_FLOW = 0.2`) = 1 非線形反復で 20 % 以上変われない。
3. **非物理状態になった点の解を前反復値へ巻き戻す** (`CNSVariable.cpp:151-178`)。

### 4.5 診断の座標を間違えていた (2026-09-19, codex plan レビュー Critical 1)

**§4.5/§4.6 の当初の数値と結論は、測定器がソルバと違う点で再構成していたため無効**だった。

`CELLS/centCoords` は**実行時にノード座標へ置換される** (`main.cpp` の `nodeValueAtNode` → `mesh.cpp`。
`run_0208/forge_run.log` に `centCoords <- node coords for 62317 CVs (max centroid shift 0.03593721241)` と出ている)。
メッシュ HDF5 の `centCoords` をそのまま使うと別の点で再構成したことになる。訂正後:

| 診断量 | 誤 (centCoords) | 正 (ノード座標) |
| --- | --- | --- |
| 62100→62107 の `Ux_L` | 457.42 m/s | **651.77 m/s** |
| 62104→62111 の `Ux_L` | 365.77 m/s | **521.17 m/s** |
| `Ux_L / (0.5 x Ux_隣接)` | 0.70 | **0.999996** |
| 中点とのずれが辺長 1 % 超の内部面 | 58899 (47.7 %) | **48328 (39.1 %)** |

**なお成立するもの**: 再構成は片側勾配の理論値 `0.5 x u_隣接` に**厳密に一致**しており、
SU2 が出す値と同じ。**「壁ノードからの再構成が異常」という当初仮説の棄却は維持する**
(ただし「SU2 より保守的 (0.70 倍)」は誤りで、**一致**が正しい)。

**崩れたもの**: 「非物理な再構成はベース無しでも同じように出るから原因ではない」は**誤り**。
`tools/check_face_reconstruction.py` (本 plan で作り直し) でノード座標で数え直すと:

| step | ベース有り `run_0208` | ベース無し `run_0209` |
| --- | --- | --- |
| 1 | 0 面 (最小 1056.56 Pa) | 0 面 (最小 1375.90 Pa) |
| 5 | 0 面 (429.45 Pa) | 0 面 |
| **6** | **1 面 (−66.78 Pa)** | 0 面 |
| 27 / 40 | **3 面 (−296.25 Pa)** | **0 面 (40 step 通して)** |

**対照は完全にクリーンで、ベース run だけが非物理な再構成を作る。**
最初に出るのは x = −0.05 m (入口面) の 47703→47702 で、ベース自身の面も step 27 には負になる。
**SU2 の `bad_recon` 検査はベース run で発火し、対照では発火しない** → §4.4 の (1) は
「本件に無関係」ではなく**直接の候補**。

### 4.6 「1 次は壁 CV が質量を失えない」も誤り (codex Major 2)

SLAU の質量流束は `convectiveFlux_slau_d.inc.cuh:493`:

```
mdot = sss*0.5f*((ro_L*(Vn_p+Vn_hat_p_abs)+ro_R*(Vn_m-Vn_hat_m_abs)) - chi/(c_diss)*P_del)
```

**左右の速度がともに 0 でも、圧力差 `P_del` だけで質量が動く**。
したがって「1 次では壁ノード側が風上のとき `rho u = 0` なので壁 CV は質量を失えない (一方弁)」という
説明は成り立たない。**1 次が 40 step 不動だったのは観測事実として残すが、その理由づけは撤回する。**
原因は SLAU 面流束の符号付き総和・密度残差・陰的補正量で論じ直すこと。

同じ理由で、旧 `check_wall_cv_drain.py` の質量流束見積り (`rho_L u_L . S` を積むだけ) は
**座標と欠落項の二重に不正確**だったので削除し、`check_face_reconstruction.py`
(再構成状態の物理性のみを、実装と同じ式・同じ座標で検査) に置き換えた。

### 4.7 SU2 との比較で言えることの範囲 (codex Major 3)

§4.4 の比較表で引いた `CNSSolver.cpp:508` は**熱流束壁**の処理で、対象 run は**等温壁**。
SU2 の `BC_Isothermal_Wall_Generic` は温度差から熱流束を作りエネルギー残差と Jacobian に加える
(行削除は運動量のみ)。forge は `nodeWallDirichlet_d.cu` で `T/roe/P` をピンし等温壁のエネルギー残差を 0 にする。
**熱的閉包は同型ではない。**

また SU2 の Venkatakrishnan の平滑化係数は参照長さと設定係数依存 (`CLimiterDetails.hpp`)、
forge は局所 `volume` 依存で、勾配の作り方も未比較。
**「同条件なら SU2 のリミッタは 1」「SU2 より小さい」はソースの形だけからは言えない。**

言えるのは**構造上の一致に限る**: 速度 Dirichlet / 連続の式を解く / 内部辺を再構成する / 再構成の目標点は辺中点。
**同一メッシュ・対応 BC で実際に走らせるまで SU2 の成否は予測しない。**

### 4.8 W1 の設計: 再構成増分の列挙と共通化 (2026-09-19, codex Major 5 対応)

**ずれは 2 成分ある。** 「座標を揃える」だけでは足りない。

**(1) 流束側が実際に適用する増分** (`convectiveFlux_common_d.cuh` の `interp_dispatch`)。
`phi_face = phiC + psi * Δ` の Δ は `convMethod` で違う:

| `convMethod` | 関数 | 増分 Δ |
| --- | --- | --- |
| 0 / −1 | `interp_1stUp` | **0** (`return phiC`。ψ は無関係) |
| 1 | `interp_MUSCL_2nd` | `g · cpd` |
| **2** | `interp_MUSCL_3rd` | `0.5·k·(phiD − phiC) + (1−k)·(g · cpd)`, k = 1/3 ← **隣接値差を含む** |
| その他 | `interp_MINMOD` | 別式 (`f`・`dcc` を使う) |

`cpd` は目標点までのオフセットで、**node では常に `±0.5·dcc` (エッジ中点)**
(`convectiveFlux_d.cu` の `rem = (discretization=="node") ? 1 : 0`)、cell では `pc[ip] − cc[ic]` (双対面重心)。

**(2) リミッタ側が評価する増分**:

| 経路 | 評価点 | 増分 |
| --- | --- | --- |
| 通常 (`limiter_d.cu` の `limiter_r1_fused5_d` pass2) | `pc[ip] − cc[ic0]` (**双対面重心**) | `g · dcp` のみ |
| 周期 node (`limiterPeriodic_d.cuh`) | 同じく `pc[ip] − cc[ic0]` | `g · dcp` のみ (`SCALED` 版は `× inv_ref`) |

**したがって node では、点が違う (重心 vs 中点) うえに、`convMethod: 2` では形も違う**
(リミッタは隣接値差の項を**一度も見ない**)。SU2 はリミッタ側でも `umusclProjection` を適用して形まで揃えている
(`computeLimiters_impl.hpp`)。

**設計**: 増分を作る関数を 1 つにし、流束とリミッタが**同じ関数・同じ引数**で呼ぶ。

```
__device__ flow_float recon_increment(int scheme, flow_float phiC, flow_float phiD,
                                      flow_float gx, flow_float gy, flow_float gz,
                                      flow_float cpdx, flow_float cpdy, flow_float cpdz, ...);
// 流束:     phi_face = phiC + psi * recon_increment(...)
// リミッタ: delta_m  = recon_increment(...)   ← psi を求める材料
```

**必要な入力はすべて揃っている**: リミッタ pass2 で partner は pass1 と同じ一行
`ic1 = plane_cells[2*ip+0] + plane_cells[2*ip+1] - ic0` で取れる。`phiD = Q[k][ic1]` も pass1 で既読。
エッジ中点オフセットは `0.5*(cc[ic1] − cc[ic0])`。

**float32 の桁落ちも同時に直す** (codex Major 5): 現行は `Qt = qc + g·dcp` を作ってから `Qt − qc` を渡しており、
**足して引く往復で桁を落としている**。共通関数は Δ を直接返すのでこの往復が消える。
変更前後で `Δ` の相対差を float32/float64 で測って記録する。

**周期経路も同じ関数を通す** (`limiterPeriodic_d.cuh`)。`SCALED` 版のスケーリングは Δ を作った後に掛ける。
**cell は対象外** (cell の目標点は双対面重心のままで整合しているので、分岐を維持する)。

### 4.9 W1 の合否ゲート (実装前に固定, codex Major 6/7 対応)

**Barth と Venkatakrishnan で要求を分ける。**

| ゲート | 対象 | 要求 |
| --- | --- | --- |
| G1 厳密有界性 | `limiter: 1` (Barth) | 再構成した面値が**近傍の min/max を外れる面が 0** |
| G2 平滑化込みの逸脱 | `limiter: 2` (Venkatakrishnan) | 逸脱は許すが**逸脱量が改修前より減る**こと。`eps2 = volume` が正なので厳密有界は保証されない (例: `δmin=0, δm=−1, volume=1` で ψ=1/3) |
| G3 正値性 | 両方 | `check_face_reconstruction.py` の VERDICT。**W1 単独で 0 になることは要求しない** (それは W2 の役割) |
| G4 解の非退行 | 両方 | `check_field_regress.py --boundary` をノイズ床基準で。**リミッタが強く効くので解は動きうる** — 動いたら SU2 クロスチェック (`procedures/su2-cross-check.md`) で是非を決める |

**共通 IC と設定差分を固定する**: `run_0205/res_0.h5` を共通初期場に、`output.level: 2`・`outStepInterval: 1`。
差分は `space.limiter` と新キーのみ。メッシュ品質 VERDICT・全残差・`check_quasisteady.py` を毎回貼る。

**標準ケース回帰** (`procedures/verification/README.md` から具体名で指定):
`case/05.sod_shock_tube` (衝撃波)、`case/36.passive_pseudoshock_control` (擬似衝撃波・node 壁)、
`case/09.Taylor-Green` (周期 = W1 の周期経路)、`case/23.axi_nozzle` (軸対称)、
`case/44.vitiated_air_wt` (TP・凝縮)。変更範囲に応じて増やす。

### 4.10 W1 の実装と A/B の実測 (2026-09-19)

**実装**: `space.limiterMatchRecon` (既定 0)。`limiter_d.cu` の `limiter_r1_fused5_d` と
`limiterPeriodic_d.cuh` の `limiter_psi_merged_d` に分岐を足し、`=1` のとき

- 目標点を **node ならエッジ中点** (`0.5*(cc[ic1]-cc[ic0])`) にする
- `convMethod: 2` なら **隣接値差の項も含めた増分**にする (`interp_MUSCL_3rd` と同形)
- 増分 `delta` を**直接**作る (`Qt` を経由しないので float32 の足して引く桁落ちが無い)

周期経路には `plane_cells` を引数追加して partner を取れるようにした。cell は分岐を維持。

**既定パスの非退行**: 変更を外した HEAD の worktree でバイナリを作って比較した。
**ビット同一ではないが、差は run 間非決定性の水準**:

| | 同一バイナリの反復 (ノイズ床) | 変更前バイナリ vs `mr:0` |
| --- | --- | --- |
| step 1 `ro` | 1.96e-07 | 4.51e-07 |
| step 10 `ro` | 8.08e-07 | 6.52e-07 |
| step 27 `ro` | 7.90e-07 | 1.34e-06 |

(この診断 run は step 28 で発散するのでノイズがカオス的に増幅する。`limiter_P` はノイズ床自体が 0.96。)
**カーネル引数を足すとレジスタ割り当てと命令スケジューリングが変わる**ので、式が同一でもビット同一にはならない。
コード中の「ビット不変」というコメントは実測に合わせて「式は変更前と同一」に訂正した。

**A/B (共通初期場 `run_0205/res_0.h5`, cfl 1.0, 2 次)**:

| 構成 | 結果 |
| --- | --- |
| Venkatakrishnan + `mr:0` (既定) | step 28 で NaN |
| Venkatakrishnan + `mr:1` | step 31 (**3 step の延命のみ**) |
| Barth + `mr:0` | step 27 で NaN (ベース壁ノードの `ro` が **−0.001003** = 負に) |
| **Barth + `mr:1`** | **400 step 完走・NaN 無し** |

**ゲート判定**:

- **G1/G3**: Barth + `mr:1` は step 1〜400 すべてで**非物理な再構成ゼロ**
  (`check_face_reconstruction.py` VERDICT OK、再構成 P の最小は 338〜1065 Pa)。
- ベース壁ノードの密度: `mr:0` は 26 step で負になるが、`mr:1` は 0.003581 → 0.001039 で**頭打ち**
  (step 100 → 400 で 0.001156 → 0.001039 = 300 step で −10 %)。**排出が止まる**。
- **G2**: Venkatakrishnan は 3 step しか延びない。`eps2 = volume` の平滑化が残るので厳密有界にならない
  = codex Major 6 の分離がそのまま出た。

**ただし根治ではない**。全レシピ (`problem_r4e_2d_base_barth_mr1.yaml`, `run_0220_r4e_barth_mr1`) を回すと
**mid 段 step 129 で NaN** (既定は 28。4.6 倍)。NaN は同じベース近傍 (x/H 10.66–11.19, y/H 2.70–2.77 の 55 節点)。

**読み**: W1 はリミッタの正しさの問題として**独立に価値がある** (Barth の厳密有界性が実際に効くようになる)。
ベース発散も大きく緩和するが**消さない**ので、W2 (面単位の非物理フォールバック) と
SERN 側の解像度・形状の検討はどちらも残る。

### 4.11 W1b: Venkatakrishnan が効かない理由と対策案 (2026-09-19, ユーザ指摘)

**`limiter: 2` (Venkatakrishnan) は生産既定なので、そこで効かないと意味がない** (`procedures/solver-settings.md`)。
§4.10 で Barth だけが通った理由を実装から特定した。

**forge の実装** (`limiterFunctions_d.cuh`):

```
flow_float K = 1.f;
flow_float eps2 = K*K*K*volume;        // ← 次元: [m^3]。delta は変数の次元 (kg/m^3, Pa, m/s)
```

**`eps2` が変数と無関係な次元で、しかも `K = 1` 固定**。SU2 は `eps2 = (K * RefElemLength)^3` で
**K の既定が 0.05** (`CConfig.cpp`)、かつ**変数を無次元化して解く**ので `delta` が O(1)。
forge は**次元付きの変数**にそのまま当てている。

**実測 (ベース壁ノード 62104, `run_0218` step 20)**:

| 量 | 値 |
| --- | --- |
| `ro` (節点) | 1.297e-03 |
| 近傍 `ro` の min / max | 1.297e-03 (**自分が最小**) / 1.022e-02 |
| `delta_p_min = qmin − qc` | **0** (ちょうど 0) |
| `delta_m` (62103 への再構成増分) | −4.586e-05 |
| `eps2 = volume` | 1.024e-06 → **ε = 1.012e-03 = `ro` の 78 %** |
| **Barth ψ** | `min(1, 0/−4.6e-5)` = **0** (完全に 1 次化) |
| **Venkatakrishnan ψ** | `ε²/(2Δ⁻²+ε²)` = 1.024e-6/(4.21e-9+1.024e-6) = **0.9959** (ほぼ無制限) |

**平滑化の閾値 ε が変数の値そのものと同じ大きさ**なので、密度は**どこでも制限されない**。
`P` (O(1e3)) では ε=1e-3 が無視できるので ψ_P は正常に 0 まで落ちる — **変数ごとに挙動がまるで違う**のが症状。
(なお現行式は ψ > 1 を返すことがあり [実測 1.0035]、呼び出し側の `min(max(·,0),1)` で潰している。)

**対策案 (未確定・codex レビューに出す)**:

- **案 S (無次元化)**: `delta` を変数ごとの基準値で割ってから Venkatakrishnan に渡す。
  **forge には既に同じものがある** — `passiveLimiter_d.cuh` の `limiter_r1_scaled_d` が
  `phi_ref = max(|Qc|, |Qmax|, |Qmin|, phi_floor)` で無次元化している (**受動スカラー専用**)。
  流れ 5 変数に同じ扱いを入れるだけ。上の実測に当てると
  `Δ̂⁻ = 4.586e-5/1.022e-2 = 4.49e-3` → ψ = 1.02e-6/(4.03e-5+1.02e-6) = **0.0247** (Barth の 0 に近い)。
- **案 K (係数を出す)**: `K` を config キーにする (SU2 の `VENKAT_LIMITER_COEFF` 相当)。
  既定 1.0 で現行維持、0.05 (SU2 既定) を試す。`K=0.05` なら `eps2` は 8000 分の 1。
- **両方**: 無次元化したうえで K を選べるようにするのが SU2 に最も近い。

**論点**: (i) 基準値を `max(|Qc|,|Qmax|,|Qmin|)` にするか別の取り方にするか (速度は 0 を跨ぐ・`phi_floor` の決め方)、
(ii) `eps2` の**局所 volume 依存**を残すか SU2 のような大域参照長にするか、
(iii) `limiterMatchRecon` と同じキーに畳むか別キーにするか (前者は評価点、後者は平滑化閾値で**独立な欠陥**)、
(iv) 既定を変えるか opt-in に留めるか (解が動く)。

### 4.12 codex plan-2 (NO-GO, C1/M7/m1) を受けた訂正と W1b の確定設計 (2026-09-19)

**C1 (診断が NaN を見逃す) — 再現して修正済み**。`run_0218` step 31 で「非物理な面 0 / VERDICT OK」を返しながら
同じ run は `DIVERGED (NaN/Inf)` だった。`check_face_reconstruction.py` を作り直した:

- 入力 (状態・勾配・リミッタ) と面状態の **`isfinite` を必須**にし、非有限・snapshot 欠落・検査件数 0 は**終了コード 1**
- **時相を分けた** (M2): 定常陰解法は「勾配/リミッタを q^n から作る → 流束 → q^{n+1} に更新 → 出力」なので、
  `res_n.h5` は**状態が更新後・勾配が更新前**。既定 `--state-from prev` で状態を `res_{n-1}.h5` から取る
- **G1 (有界性) と G3 (物理性) を分離**。G1 は**そのノードの近傍全体**の min/max で測る
  (面の 2 セルで測ると Barth でも大量に逸脱に見える)。丸め尺度は「近傍レンジと値の大きい方」
  (レンジだけだと自由流でゼロ割、値だけだと 0 を跨ぐ速度で潰れる)

**§4.10 の主張を訂正する**:

| 旧 | 訂正後 |
| --- | --- |
| 「G1/G3 合格」 | **G3 のみ確立**。`mr:1` は両リミッタで**非物理ゼロ**、`mr:0` は入口角で 1 面 (下表)。**G1 は未確立** |
| 「ベース壁ノードの `ro` が頭打ち・排出が止まる」 | **撤回**。`check_quasisteady` は **DRIFTING** (drift 5.4 %/tail, fluct 40.4 %)、収束判定も NOT CONVERGED。言えるのは**「400 step まで有限状態を維持した」**まで |
| 「既定パスの差は run 間非決定性の水準」 | **ゲートには未達**。`check_field_regress.py --boundary` (ノイズ床×2) は step 1 で 4 量 FAIL (`roUy`/`roe` が床の 2.92 倍)、step 10 で 3 量 FAIL (`h0` 3.62 倍)、step 27 で PASS。**非退行は未確定** |

**G1/G3 の実測 (step 20, `--state-from prev`)**:

| run | G1 逸脱 | G3 非物理 | 再構成 P の最小 |
| --- | --- | --- | --- |
| Barth + `mr:1` | 11453 | **0** | 889.23 |
| Barth + `mr:0` | 11636 | **1** | **−437.96** |
| Venkat + `mr:1` | 18675 | **0** | 483.13 |
| Venkat + `mr:0` | 18334 | **1** | **−338.32** |

**G1 が Barth でも 0 にならない**。変数別では `ro` **0** / `Uz` **0** / `Ux` 1020 / `Uy` 8422 / `P` 2011。
壁ピンとの関係を疑ったが、`Uy` の逸脱のうち速度ピン壁ノードに接するのは **1.3 %** だけで棄却。
**snapshot から流束時の状態を完全には再現できていない** (M2 の指摘そのもの) ので、
**G1 の判定は流束評価直前の状態をソルバ側から出すまで保留する**。

### 4.13 W1b の確定設計 (codex M3/M4 の対案を採用)

**案 S の単純移植 (`phi_ref = max(|Qc|,|Qmax|,|Qmin|)`) は不採用** (codex M4):
速度に当てると**一様な速度加算だけで平滑化強度が変わる** (参照 0.02 → 1000.02 で ψ が 2e-6 → 0.9998)。

**採用する形**:

1. **無次元化は run 中固定の物理参照値で行う**: `rho_ref`, `p_ref` (**ゲージ差圧でなく絶対圧力**), `a_ref` (音速)。
   **速度 3 成分は共通の `a_ref`** で割る (成分ごとの局所絶対値を使わない)。
2. **ε̂² = (K · h_i / L_ref)³** とする。`h_i` は**平面・軸対称では半径重み前の面積の平方根**、**3D では体積の立方根**。
   周期 node では合併 CV に対応させる。<br>
   理由 (codex M3): 現行の `volume` は共通の長さ尺度でない — 平面 2D は面積×単位厚み、
   **軸対称はさらに半径を掛ける** (同じ増分でも半径で平滑化が変わる)、周期 node は合併体積。
3. **`K` を config キーにする**。ただし **SU2 の 0.05 をそのまま移植しない**:
   SU2 は `RefElemLength = 1.0` で閾値 1.25e-4、対象ノードで forge の K だけ合わせると約 1.28e-10 で **約 100 万倍違う**。
   forge の式に合わせた値を実測で決める。
4. **式の整理**: 現行 `venkata_limiter` は分子分母に余分な `delta_m` 因子を持つ。約分する。
   **`K=0, δp=0, δm=1e-19` で `0/0 → NaN`** を codex が float32 で再現しており、これも同時に潰す。
5. **`limiterMatchRecon` とは別キー**にする (評価点と平滑化閾値は**独立な欠陥**)。既定は現行維持の opt-in から始める。

**必須の試験**: 単位変換 (SI ↔ 別単位)、相似拡大 (メッシュを一律スケール)、格子細分。
**軸対称**で半径依存が消えることを確認する。

**§4.11 の「密度はどこでも制限されない」も過大**だった (codex M4): `run_0218` step 20 で
`limiter_ro < 0.9` が **4661 / 62317 節点**、最小 **0.002031**。正しくは
**「局所状態と尺度によって効き方が不適切になる」**。

### 4.14 W1 の適用範囲が設計と合っていない (codex M7)

増分式は今も 3 箇所に別々に在る (`limiter_d.cu` / `limiterPeriodic_d.cuh` / `convectiveFlux_common_d.cuh`) —
**共通関数への集約は未実施**。さらに周期側の wrapper は `limiterMatchRecon` を**化学種・受動スカラーにも渡す**一方、
非周期側の `limiter_r1_d` / `limiter_r1_scaled_d` は旧経路のまま。cell も `mr:1` なら算術が変わる。
**「流れ 5 変数の node のみ」という宣言と一致していない。**
**W1b の前に共有増分関数と対象変数を確定し、他経路への波及を明示的に遮断する**こと。
周期の単体試験 (`test_periodic_limiter.cu`) も `mr:0` 固定なので `mr:1` を足す。

### 4.15 W1 の共通化と範囲限定を実装 (2026-09-19, codex M7 対応) + **A/B の土台が不適切と判明**

**実装**:

- **`cuda_forge/reconIncrement_d.cuh` を新設** — 再構成増分 Δ の**唯一の定義**。
  流束 (`interp_MUSCL_2nd` / `interp_MUSCL_3rd`) とリミッタ (`limiter_d.cu` / `limiterPeriodic_d.cuh`) が
  **同じ関数を同じ引数で**呼ぶ。`convMethod` 0/1/2 に対応 (MINMOD は別式なので対象外)。
- **範囲を限定** (M7): 周期の `limiter_periodic_merged` に `matchRecon` 引数を足し、
  **流れ 5 変数だけ `cfg.limiterMatchRecon` を渡す**。化学種・受動スカラーは 0 固定。
  config 検証で `limiterMatchRecon: 1` は **node かつ convMethod 0/1/2** のみ受理 (cell と MINMOD は起動時エラー)。

**ところが、リファクタで A/B の結果が変わった**:

| | リファクタ前 | 後 |
| --- | --- | --- |
| Barth + `mr:1` | 400 step 完走 | **step 217 で NaN** |
| Venkat + `mr:0` (既定) | step 28 | step 28 (不変) |

差の出どころを追うと、**`limiter_ro` が 1 節点で 5.4e-5 変わっただけ** (共有関数化による FMA 畳み込みの差)。
それが step 1 のうちに別の節点 (x/H 0.165, y/H 1.044 = ランプ角部) の `ro` を **0.1414 → 0.1334 (6 %)** 動かしている。
step 1 で >1e-3 違う節点は 5 個、>1e-7 でも 102 個。

**結論: この診断ケースは float レベルでカオス的で、「何 step 生き延びたか」は A/B の指標にならない。**
「Barth + `mr:1` は 400 step 完走」という §4.10 の記述は**この意味で無効** (217〜400 は同じもののばらつき)。
codex M8 の「各方式の発散直前を比較すると状態差が交絡する」はこのこと。

**A/B の土台を作り直す (W1c)**:

- **安定に収束するケースで測る**。発散寸前の run で生存 step を比べない。
- 指標は**生存 step でなく直接測れる量**にする: G3 の非物理面数、G1 の逸脱数と逸脱量、
  収束解での目的量の差。いずれも `--state-from prev` で時相を揃えて測る。
- ベースの 2D 診断は**原因調査には使える**が、**W1/W1b の可否判定には使わない**。

**既定パスは不変 (ノイズ水準)**: リファクタ前後で `ro` の差は 8.7e-7 (反復ノイズ床 7.9e-7 と同オーダー)、
`limiter_P` は完全一致。

### 4.16 W1c: 安定ケースでの A/B (2026-09-19)

**土台**: ベース**無し**の SERN 2D 設計 A (`problem_rb_prop_A.yaml` 系)。生産レシピで 4 本とも完走し
**GATES PASS**。発散寸前の生存 step でなく、**収束後の目的量**と**直接測れる量**で比べる。
run は `case/46.sern_design/run_0225`–`0232` (後半 4 本は収束場からの level 2 継続)。

**目的量**:

| 構成 | `C_T_with_shear` | 生産既定との差 |
| --- | --- | --- |
| Venkat / `mr:0` (**生産既定**) | 0.91540876 | — |
| **Venkat / `mr:1`** | 0.92032665 | **+0.54 %** |
| Barth / `mr:0` | 0.91653520 | +0.12 % |
| Barth / `mr:1` | 0.91670852 | +0.14 % |

**G1 (有界性, 変数別・収束場からの継続 step 5)** — **§4.20 で無効と判明 (時相ズレのアーチファクト)。カーネル内計測の表を見ること**:

| 構成 | `ro` | `Ux` | `Uy` | `Uz` | `P` |
| --- | --- | --- | --- | --- | --- |
| Venkat / `mr:0` | 6664 | 78 | 18965 | 0 | 173 |
| Venkat / `mr:1` | 6595 | 124 | 19151 | 0 | 290 |
| Barth / `mr:0` | 585 | 378 | 16149 | 0 | 992 |
| **Barth / `mr:1`** | **0** | 856 | 16604 | 0 | 1597 |

**G3 (物理性)**: 4 構成とも **0** — 安定ケースでは判別しない (ベース有りでのみ差が出た)。

**読み方 (診断の限界込み)**: snapshot から流束時の状態を再現できるのは、更新後に**書き換えられない変数**だけ。
`Ux/Uy/P` は壁ピン・BC 適用・EOS 再計算で書き換わるので、その G1 は診断の産物を含む
(`Uy` が全構成で 16–19k と一様に大きいのがその指紋)。**信頼できるのは `ro`**。

- **`ro` で Barth + `mr:1` だけが厳密有界 (0)** を達成。Barth `mr:0` は 585、Venkat は両方 ~6600。
  → **W1 (評価点と形を揃える) は Barth では設計どおり効く**。
- **Venkatakrishnan は `mr:1` にしても変わらない** (6664 → 6595)。**束縛しているのは平滑化項**であって評価点ではない
  → **W1 単独では生産既定は直らない。W1b が必須** (ユーザ指摘のとおり)。
- **目的量が Venkat + `mr:1` で 0.54 % 動く**。MOO が区別する設計間の差 (2–3 %) の 2 割に当たるので無視できない。
  §4.9 の G4 のとおり、**動いた解の是非は SU2 クロスチェックで決める**。

**config 検証は「警告して無効化」に変更**: 例外を投げると、同じ `solverConfig.yaml` を読む
メッシュ品質チェック (`convertGmshToForge` の cell 変換) が落ちる。実際に `run_0226`/`0228` が
`sern_qc.h5` を作れず失敗した。

### 4.17 W1b の実装と結果: **Venkatakrishnan で厳密有界を達成** (2026-09-19)

**実装** (§4.13 の確定設計どおり):

- `space.limiterScaled` (0/1, 既定 0)、`space.venkatK` (既定 1.0)、`space.limiterRefLength` (0 = 自動)。
- **基準は run 中固定**。起動時に初期場から体積加重平均で `ro_ref` / `p_ref` (絶対圧) / `a_ref` (音速) を取り、
  **速度 3 成分は共通の `a_ref`** で割る (局所絶対値スケーリングは codex M4 で棄却済み)。
  基準長は未指定ならメッシュ境界箱の対角 (格子細分で変わらない長さ)。
- **ε̂² = (K · h_i / L_ref)³**。`h_i` は **2D / 軸対称では `sqrt(A_planar)`** (半径重み**前**の面積)、3D では `cbrt(volume)`。
  2D 判定は**境界箱が 1 方向に潰れているか**で行う (`ieleType` は node の双対 CV では primal の型を持たないので使えない)。
- `venkata_limiter_scaled` を新設し、**余分な `delta_m` 因子を約分**。現行の 3 次積は
  `K=0, δp=0, δm=1e-19` で `0/0 → NaN` になる (codex が float32 で再現) — 約分形にはこの穴が無い。

**結果** (安定ケース、収束場からの継続 step 5、`--state-from prev`):

| 構成 | `ro` | `Ux` | `Uy` | `Uz` | `P` | G3 | `C_T_with_shear` | 既定との差 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Venkat / `mr0` (**生産既定**) | 6664 | 78 | 18965 | 0 | 173 | 0 | 0.91540876 | — |
| Venkat / `mr1` | 6595 | 124 | 19151 | 0 | 290 | 0 | 0.92032665 | +0.54 % |
| Venkat / `mr1`+scaled (`cbrt` = 2D 判定前の誤り) | 2710 | 1895 | 22400 | 0 | 2554 | 0 | 0.91583282 | +0.046 % |
| **Venkat / `mr1`+scaled (`sqrt`)** | **0** | 238 | **13718** | 0 | 640 | 0 | **0.91563312** | **+0.025 %** |
| Barth / `mr1` (参考) | 0 | 856 | 16604 | 0 | 1597 | 0 | 0.91670852 | +0.14 % |

**結論**:

- **生産既定のリミッタ (Venkatakrishnan) で `ro` の厳密有界を達成**した。他の変数の逸脱も Barth より少ない
  (`Uy` 13718 vs 16604、`P` 640 vs 1597)。
- **目的量の変化は +0.025 %** — `mr1` 単独の +0.54 % から 20 分の 1 に収まった。
  MOO が区別する設計間差 (2–3 %) の 1 % なので実用上無視できる水準。
- **長さの取り方が効く**: `cbrt(volume)` (2D で誤り) だと `ro` 2710 で止まり、他の変数はむしろ悪化した。
  `sqrt(A_planar)` にして初めて 0 になる。**codex M3 の指摘 (h_i の定義) が判定を分けた。**
- `K` は 1 と 10 で目的量が 0.91583282 / 0.91583787 とほぼ変わらない (無次元化後は ε̂² が十分小さく、
  K の効きが鈍い)。既定 1.0 を維持する。

**残り**: 単位変換・相似拡大・格子細分・軸対称の半径非依存の試験 (§4.13 の必須試験)、標準ケース回帰、既定化の判断。

### 4.18 リミッタの形を 3 つ比べた: **比の形が最良** (2026-09-19, ユーザ問い「これしかないのか」)

**きっかけ**: `limiterFunctions_d.cuh` の現行実装の**1 行上に**、比の形がコメントアウトで残っていた。

```c
flow_float eps2 = K*K*K*volume;
//return (x*x + 2.0*x + eps*eps)/(x*x + x + 2.0 + eps*eps);   ← x = delta_p/delta_m (無次元)
```

ファイル冒頭が引用している Michalak の流儀そのもの。**比で書けば単位の問題は最初から起きない**
(基準値 `ro_ref`/`p_ref`/`a_ref` も長さ尺度 `L_ref` も要らない)。

**`space.limiterScaled` を 3 値にした**: 0 = 現行 / 1 = 差を無次元化 (§4.13) / **2 = 比の形**。
比の形では `venkatK` を**無次元の eps** として使う。

**実測** (安定ケース、`mr:1`、収束場からの継続 step 5) — **G1 列は §4.20 で無効と判明。目的量と残差は有効**:

| 形 | `ro` | `Ux` | `Uy` | `P` | `C_T_with_shear` | 既定差 | `rms_ro` 低下 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ① 現行 (**生産既定**) | 6664 | 78 | 18965 | 173 | 0.91540876 | — | 0.1 dec |
| ② 差を無次元化 (K=1) | **0** | 238 | 13718 | 640 | 0.91563312 | +0.025 % | 0.1 dec |
| **③ 比の形 eps=1e-3** | **0** | **213** | **13604** | **554** | 0.91564662 | +0.026 % | 0.1 dec |
| ③ 比の形 eps=0.1 | 1091 | 410 | 16601 | 1694 | 0.91565915 | +0.027 % | 0.1 dec |
| ③ 比の形 eps=1.0 | 5211 | 3757 | 20932 | 5220 | 0.91559506 | +0.020 % | 0.1 dec |
| (参考) Barth | 0 | 856 | 16604 | 1597 | 0.91670852 | +0.142 % | 0.1 dec |

**結論: ③ 比の形 (eps=1e-3) を推す**。

- `ro` が 0 なのは ② と同じだが、**他の全変数でも逸脱が最少** (213 / 13604 / 554 < ② の 238 / 13718 / 640)。
- **基準値の設計問題が丸ごと消える** — ② は `ro_ref`/`p_ref`/`a_ref` を初期場の体積加重平均から取っており、
  **初期値に依存する**・**広い外部流領域に引っ張られる** (SERN で `p_ref` 6230 Pa は入口 101 kPa でも自由流 2851 Pa でもない)
  という弱点があった (ユーザ指摘)。比の形にはこれが無い。
- `eps` の効きは素直: 1e-3 → 0.1 → 1.0 で `ro` の逸脱が 0 → 1091 → 5211。**1e-3 が既定候補**。
- 目的量は ② と ③ でほぼ同じ (+0.025 / +0.026 %)。

**収束の代償は実測した (2026-09-19)**。本段末尾 20 % の残差平均:

| 形 | `rms_ro` | `rms_roUx` | `rms_roe` | `rms_roK` |
| --- | --- | --- | --- | --- |
| ① 現行 (生産既定) | 4.93e-05 | 0.116 | 405 | 0.721 |
| ② 無次元化 | 1.06e-04 | 0.191 | 457 | 1.016 |
| ③ 比の形 eps=1e-3 | 1.06e-04 | 0.192 | 458 | 1.011 |
| ③ 比の形 eps=0.1 | 1.06e-04 | 0.191 | 455 | 1.016 |
| (参考) Barth | 1.16e-04 | 0.209 | 458 | 0.996 |

**正しく制限する 3 形はいずれもプラトーが約 2.2 倍高い**。`eps` を 1e-3 → 0.1 にしても変わらないので、
**平滑化パラメータの問題ではなく「実際に制限すること自体の代償」**。② と ③ はこの指標では区別できない。
目的量の変化が +0.025〜0.026 % に収まっていることと併せて判断する。

**収束「速度」の試験は別途要る**。`case/08.bump/run_regr_single` で試みたが、
**無改造の ① を含む 3 形すべてが現行バイナリで step 82 に発散**した (保存されている旧 run は 5.9e-3 まで収束している)。
= **この回帰ケースは現行バイナリで再現しない** (私の変更とは無関係。別途調査が要る)。深く収束するケースで別途確認が要る。

### 4.19 MLP は実装可能だが別仕事 (2026-09-19, ユーザ関心)

**forge に MLP は無い。** ただし `limiter_d.cu` の `limiter_r1_d` の直上に

```c
// Modified multi-dimensional limiting process with enhanced shock stability on unstructured grids
```

と**論文題名だけが引用**されており (実装は 1-ring の Barth/Venkatakrishnan)、
§4.18 の比の形のコメントと合わせて**このファイルには実装されていない手法への引用が 2 つ**ある。

**MLP に要る 3 つの接続の在庫**:

| 必要なもの | 状態 |
| --- | --- |
| primal 要素 → 節点 (vertex neighbourhood の素) | **solver の host に在る** — `msh.vizCONNE` (`/VIZMESH/CONNE`)。ただし可視化専用で **device には一切上がっていない** (`cuda_forge/` に `vizCONNE` のヒット 0) |
| device の広いステンシル | **無い**。device は 1-ring のみ (`map_plane_cells_d` / `map_cell_planes_*_d`)。**LSQ 勾配も同じ 1-ring** なので流用できない |
| 双対 CV の頂点座標 (primal セル重心・面重心) | **host にも device にも無い**。エッジ中点だけ device で再構成可能。`PLANES/centCoords` は双対**面**の重心で別物 |

**2-ring は代用にならない (実測)**:

| | 1-ring 次数 | vertex nbhd 次数 | `1∪2-ring` から漏れる vertex nbhd |
| --- | --- | --- | --- |
| 2D quad (`case/46` sern, 62070 節点) | 3.97 | 7.90 | **0** (ただし軸方向 2 つ先が ~3.93 個余分に入り**リミッタが緩くなる**) |
| **3D hex** (`case/18` backstep, 98973 節点) | 5.73 | 23.58 | **6.93 個 (最大 8)** ← 六面体の体対角は primal エッジで 3 ホップ |

**コスト**: vertex-neighbour CSR は 2D sern で ~2.2 MB、3D backstep で ~9.7 MB、
**3D hex 2.37 M 節点で ~234 MB**。pass1 の gather は 1-ring 比 **約 4.1 倍**。
リミッタは既に「3D 2.37 M 節点で 9.5 ms/step」と記録のある工程なので無視できない。

**実装の足がかりはある**: CSR 構築は変換器の `renumberNodesRCM` (`gmshReader.hpp:202-225`) が
**まさに vertex neighbourhood グラフ**を作っており流用できる。device 上の 2-ring 走査の先例も
`ransBoundary_d.cu` の `compute_wall_y_eff_d` にある。

**落とし穴** (コードで確認済み): 周期 node では CV が union-find で合併されるので、MLP の min/max も
`periodicGatherMax/Min` で group 合併しないと周期対で ψ が食い違う (既存 2 段構造と同じ手当てが要る)。

**判断: 本 plan では扱わない。** ③ 比の形で `ro` の逸脱は既に 0 になっており、MLP が追加で改善するのは
`Ux`/`Uy`/`P` の逸脱 (現状 213 / 13604 / 554) だが、その多くは診断の時相アーチファクトを含んでいて
**現状の測定器では改善を判定できない**。MLP は
**(a) device への `vizCONNE` 配線と ~234 MB / 4.1 倍コストの是非、(b) 判定できる測定器**が揃ってからの別 plan とする。

### 4.20 測定器をカーネル内に移した — **後処理の G1 はほぼ全部アーチファクトだった** (2026-09-19)

**なぜ移したか**: 後処理は `res_*.h5` から再構成を組み直すが、1 step の中で
「① 境界条件 → ② 勾配・リミッタ → ③ 流束 → ④ 更新 → ⑤ P/T を作り直す → ⑥ 出力」の順に進むため、
出力ファイルは**状態が⑤の後・勾配が②のもの**で 1 step ずれている。`ro` は①でも⑤でも書き換わらないので
「1 つ前のファイルの状態 + 今のファイルの勾配」で②を厳密に再現できるが、
`Ux`/`Uy`/`P` は壁速度ピン・壁温由来の P・EOS 再計算で書き換わるため**再現できない**。

**実装**: `limiter_r1_fused5_d` に **pass3** を足した (`space.limiterDiag: 1`、既定 0 で atomicAdd は一切走らない)。
psi 確定後に**流束と同じ共有関数 `recon_increment`** で再構成値を作り、そのノードの近傍 min/max を外れた
face-side を変数ごとに数える。丸め許容は「近傍レンジ」と「値の大きさ」の大きい方。

**結果** (安定ケース、面側 246196):

**⚠ 最初の 4 run (`run_0246`–`run_0249`) は交絡していた**: ① だけ `limiterMatchRecon: 0` で、②③ Barth は `1` だった。
`matchRecon == 0` のとき kernel は旧経路に入り **`limiterScaled` を読まない** (`limiter_d.cu:300-311`)。
つまりあの比較は「無次元化の効果」ではなく「評価点一致 + 無次元化」の合算だった。**`mr` を揃えて測り直した** (`run_0256`–`run_0260`):

**⚠ この節の ② (C) の数値は §4.32 で無効と判明** (平面 2D で `A_planar` が未初期化 → ε²=0 で `venkatK` が効いていなかった)。**§4.32 の表を見ること**。以下は評価点の修正そのものの記録として残す。

**測定器自体にもう 1 つ欠陥があった** (codex plan-3 Critical 2): pass3 は `matchRecon != 0 && edgeMid != 0` のときだけ
辺中点を使っていたが、**node の流束は `matchRecon` に関係なく常に辺中点**である
(`convectiveFlux_d.cu:69` で `g_reconEdgeMid` は node なら無条件 1、`convectiveFlux_slau_d.inc.cuh:134`)。
つまり `mr0` の構成だけ双対面重心を測っており、**A/B が別々の点の比較**になっていた。
さらに `qf` が NaN だと大小比較が両方 false になり「逸脱なし」に数えられ、検査件数 0 も合格扱いだった。
**評価点を流束側の条件だけで決め、非有限を別枠で数え、`sides==0` を `VERDICT=NO-CHECKS(FAIL)` にし、
許容幅に対する最大逸脱倍率も記録するよう修正**して測り直した (`run_0273`–`run_0277_g1fix_{A..E}`):

| 構成 | `mr` | `scaled` | `ro` | `Ux` | `Uy` | `Uz` | `P` | 最大逸脱 (許容幅比) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A ① **生産既定** | 0 | 0 | **8146** | **94** | **20626** | 0 | **141** | `Uy` 47186× |  <!-- 以下 §4.32 で置換 -->
| B 評価点だけ一致 (W1) | 1 | 0 | **8131** | **0** | **20512** | 0 | **0** | `Uy` 47189× |
| C ② 無次元化 | 1 | 1 | **0** | 0 | **0** | 0 | 0 | 0 |
| D ③ 比の形 (eps=1e-3) | 1 | 2 | **0** | 0 | **0** | 0 | 0 | 0 |
| E (参考) Barth | 1 | — | **0** | 0 | **0** | 0 | 0 | 0 |

(非有限は全構成 0 件。`sides` は全構成 246196 で `VERDICT` は A/B が `VIOLATIONS`、C/D/E が `CLEAN`)

**W1 と W1b は別の変数を直している**:

- **W1 (評価点の一致) は `Ux` と `P` を直す** (94→0, 141→0)。これらはリミッタと流束が**別の点を見ていた**ことだけが原因で、
  正しい点で評価すれば旧 Venkatakrishnan のままでも逸脱しない。
- **W1b (無次元化) は `ro` と `Uy` を直す** (8146→0, 20626→0)。W1 だけでは `ro` 0.2 % / `Uy` 0.6 % しか減らない。
  こちらは ε² の次元不整合そのもので、評価点とは無関係。
- したがって **W1 と W1b は束で入れる**。どちらか片方では 5 変数すべてを満たせない。
- 逸脱は周辺的でない: `Uy` は許容幅の **47000 倍**外れている。

**§4.16 / §4.18 の G1 の表は無効**。後処理が出していた `Ux` 78〜856 / `Uy` 13604〜19151 / `P` 173〜1597 は
**ほぼ全部が時相ズレのアーチファクト**だった (実際は ① 以外すべて 0、① も `Ux`/`P` は 0)。
**Barth が `Uy` 16604 と出ていたのも артефакт** — 厳密有界なので 0 が正しい。

### 4.21 スケール不変性 (相似拡大 ×1024) — 旧 Venkatakrishnan はメッシュを大きくすると**リミッタが消える**

**設計**: メッシュ座標を **2 の冪 (×1024)** で拡大すれば float32 で丸めが一切出ない
(`COORD`/`centCoords`/`surfArea`/`surfVect`/`wall_dist` ×S、平面 2D なので `volume` (=面積·奥行 1) ×S²)。
状態場 (ρ,u,P) は示強量なので不変。**スケール不変なリミッタなら ψ はビット同一になる**はずで、
差が出ればそれは 100 % リミッタのスケール依存性である。1 step・`output.level 2` の `limiter_*` を比較
(`run_0261`–`run_0268`)。**物理的な相似は不要** — ψ は状態と幾何だけで決まり粘性項を通らない。

| 構成 | ψ の最大差 | ψ が変わったノード | ψ_ro 平均 (ref → ×1024) |
| --- | --- | --- | --- |
| A ① 生産既定 | **9.98e-01** | **58761 / 62069** | 0.9718 → **0.99998** |
| B 評価点一致 (旧 Venkat) | **9.98e-01** | **58919 / 62069** | 0.9715 → **0.99998** |
| C ② 無次元化 | **0** | **0** | 0.7101 → 0.7101 |
| D ③ 比の形 | **0** | **0** | 0.7101 → 0.7101 |

**旧 Venkatakrishnan は ×1024 で実質 OFF になる** (ψ→1)。`eps2 = K³·V` が面積 (2D) と共に ×2²⁰ 増え、
分子分母を支配して常に ψ≈1 を返すため。**②③ はビット同一** = 厳密にスケール不変。
**格子細分は別に測る** (codex plan-3 Major 5 を採用): ×1024 が示したのは「この入力・この 1 回の評価で
スケール依存性を分離できた」ことだけで、格子細分は (a) 解像度が上がって場そのものが変わる、
(b) h_i と L_ref の比が変わる、の 2 つが同時に効くので同じ軸ではない。**§5.1 の残作業に残す**。
なお ②③ は ψ_ro 平均 0.710 と旧式 0.972 より**大幅に強く効いている** — 目的量への影響は §4.17/§4.18 の通り +0.026 %。

**結論 (確定)**:

- **生産既定のリミッタは `ro` と `Uy` で有界性を破っている** (8478 / 21460 面側)。
- **② と ③ と Barth はいずれも全変数で逸脱 0** (許容幅 1e-5×scale の下で)。**②と③は有界性でもスケール不変性でも区別できない**。
  **ただし「② が厳密有界性を保証する」は数学的に誤り** (codex plan-3 Major 3): 局所極値で δp=0 のとき
  ψ = ε²/(2δm²+ε²) > 0 なので δm<0 なら下限を割る。実例 `qc=qmin=0.01, δm=-0.1, ε²=0.01` → ψ=0.3333, 面値 **−0.0233**。
  上の 0 は「**この入力で許容幅を超える逸脱が無かった**」という実測であって、普遍的な保証ではない。
- **W1 は `Ux`/`P` を、W1b は `ro`/`Uy` を直す**。単独ではどちらも 5 変数を満たせない (束で入れる)。
- したがって **②と③の選択は「基準値が要るかどうか」で決まる → ③ 比の形を採る** (§4.18)。

**MLP (§4.19) の位置づけも変わる**: ③ で全変数が既に 0 なので、**MLP が改善する余地は有界性には無い**。
MLP の利点があるとすれば衝撃波近傍の挙動や収束性であって、本 plan の G1 では測れない。**W2b は据え置き**。

### 4.22 標準ケース回帰 — **② 採用 / ③ 棄却** (2026-09-19)

生産既定が `space: {convMethod: 1, limiter: 2}` ([`procedures/recommended-settings.md`](../../procedures/recommended-settings.md) §本段) である以上、
既定化の可否は標準ケースで決める。node + `limiter: 2` の既存 run を種に **A (= 生産既定 `mr0/scaled0`)** と
**C (= `mr1/scaled1` 無次元化)** を同一種から走らせて比べた (③ は case/44 で落ちたので以降は外した)。

| case | 内容 | run | 結果 |
| --- | --- | --- | --- |
| **05** Sod 衝撃管 | node 3D periodic, Euler, `cm2`, 200 step, **厳密 Riemann 解と比較** | `run_0020_limA_prod` / `run_0021_limC` / `run_0022_limD` | **C は A より良い**: L1 誤差 ro 7.048e-3→6.908e-3, P 3.212e-3→3.062e-3, u 6.048e-3→5.909e-3。衝撃後 ρ は 1.9913→**2.0517** (厳密 2.2240) と**鈍りが減る**。厳密解の上限超え 0 個・P 範囲外 0 個で**新たな振動は出ない** |
| **09** Taylor-Green | node periodic, 受動スカラー + FCT, unsteady 100 step | `run_0165_limA_tgv` / `run_0166_limC_tgv` | **差なし**: ρ/P/Ux は一様流のまま**ビット同一**、受動 ξ の最大相対差 2.0e-6、有界性 [0, 0.999995] は両者同一 |
| **36** 擬似衝撃波 | node SST, 定常陰解法, 収束場から継続。**同一 step 数 (32000)・基準値固定** (`limiterRoRef/PRef/ARef` を C の値に pin。ログで `(fixed)` を確認) | `run_0033_fairA` / `run_0034_fairC` | **衝撃位置は完全に同一**: `check_quasisteady --quantity shock` が両者 **STEADY**, tail mean **241.7**, drift 0.0 %。残差は**どちらが上とも言えない** — C が良いのは `rms_roUx` (9.76e-3→**2.79e-3**, 3.5 倍) だけで、`ro` 2.08e-5→5.21e-5, `roUy` 2.88e-3→6.47e-3, `roe` 6.91→1.48e1, `roOmega` 8.53→7.11e1 は **C が悪い**。両者とも 32000 step で **NOT CONVERGED (still converging)**。**前版の「C が 5/6 列で上」「7 倍・10 倍改善」は、C だけ 24000 step 長く回した不公平比較だったので撤回する** (codex plan-3 Major 7)。衝撃位置 240.9 vs 241.7 の差も run 長の違いで、リミッタの差ではなかった |
| **44** 汚染空気ノズル | node **軸対称** TP + 多成分 + 非平衡凝縮, 定常陰解法 | `run_0490_limA_va3` / `run_0491_limC_va3` / `run_0492_limD_va3` / `run_0493_limD_va3_long` | **C は A の残差床に戻る** (`rms_ro` 5.45e-7 vs **5.26e-7**, `roUx` 2.97e-4 vs 2.96e-4, `roUy` 4.08e-4 vs 4.10e-4, `roe` 4.15e-1 vs 4.04e-1)。**D は戻らない**: 4000 step で 3〜4 倍高く、さらに 12000 step 伸ばしても `ro` 1.52e-6 / `roUx` 6.37e-4 / `roUy` 2.15e-3 / `roe` 1.14 で**床そのものが 2.1〜5.3 倍高い** |

**③ 比の形を棄却する理由 (§4.18 の推奨を撤回)**: 比の形 ψ(y) は y=Δ₊/Δ₋ だけの関数なので、**滑らか域でリミッタが切れない**。
Venkatakrishnan の ε² は「Δ が十分小さければ ψ→1 にして定常収束を妨げない」ためにあるのであって、それを捨てると
Barth と同じ非平滑性が残り、**定常残差の床が上がる** (case/44 で実測)。「基準値が要らない」という ③ の利点は
この代償に見合わない。**§4.18 の「③ が最良」は撤回**する。

**SU2 との対応 — 構造だけが共通で、閾値は同じでない** (codex plan-3 Major 4 を採用・前版の記述は誤り):
SU2 は `eps2 = (L·K)³`, `L = REF_ELEM_LENGTH` = **1.0** (`Common/src/CConfig.cpp:5021`。前版が書いた 0.1 は誤り),
`K = VENKAT_LIMITER_COEFF` = 0.05 (`CConfig.cpp:2006`) なので **ε² = 1.25e-4、√ε² ≈ 1.118e-2**。
前版の「SU2 既定は Δ_無次元 ≲ 3.5e-4 で ②(K=1) の 1.9e-4 と同程度」は**両方とも誤り**なので撤回する。
さらに SU2 の ε² は**大域定数**だが forge の ② は**局所 h_i³ 依存** (`limiter_d.cu:341`) で、格子依存性も同じではない。

**言えるのはここまで**: 「**無次元の増分に平滑化項 ε² を加える**」という構造が共通で、SU2 が SI 次元のまま
同じ式を使っても壊れないのは**解を無次元化して解いているから**である。forge は SI 次元のままなので
同じ式が壊れた — これが ① の欠陥の説明になる。**② の妥当性は forge 自身の精度・収束・格子感度で判断し、
「SU2 と閾値が同程度」を採用理由には使わない**。

**⚠ 上の表の 05 / 09 は最初の実施時点では ② を実行していなかった** (codex plan-3 Critical 1):
周期 node は `limiter_d.cu:460` から `limiter_periodic_merged` に入り、その先 (`limiterPeriodic_d.cuh:115`) は
旧 `venkata_limiter(..., volume)` を呼ぶだけで **`limiterScaled` を読まなかった**。`case/05` で 3417 CV、
`case/09` で 3169 CV の周期合併があるので、**run 全体**が旧式で回っていた (`matchRecon` は渡っていたので
差が出たのは W1 の効果)。**周期経路に ② / ③ を配線して再実施した** (`run_0025`–`run_0027_per{A,C,D}`,
`run_0167`/`run_0168_per{A,C}_tgv`)。上の表はその再実施値。配線前後で `case/05` は ρ 9.9e-4 / P 677 Pa 動いており、
配線が実際に効いていることを確認した。

**「標準ケース回帰完了」とは書けない** (codex plan-3 Major 7 を採用): [`procedures/verification/README.md`](../../procedures/verification/README.md)
の標準検証ケースは **08 / 20 / 13 / 48** であって 05/09/36/44 ではない。本節は「node + `limiter: 2` の
既存 run がある 4 ケースで退行を見た」までであり、標準ケース回帰は**別途 §5.1 の残作業**とする。

**既定化の判断はまだ保留**。残るゲート:
1. **G4 (目的量の SU2 クロスチェック)** — §4.16 のとおり `mr:1` で case/46 の目的量が **+0.54 %** 動く。
   「A と C のどちらが真値に近いか」は forge 内では決められない。
2. **基準値の restart 継承** (§4.25)。
3. **格子細分** と **標準ケース (08/20/13/48) 回帰**。

### 4.25 ② の基準値は restart ごとに変わっていた (codex plan-3 Major 6) — 明示指定できるようにした

`main.cpp` は起動のたびに**現在場**から `ro_ref`/`p_ref`/`a_ref` を体積加重平均で決めていた。case/36 の継続で実測:

| run | `ro_ref` | `p_ref` | `a_ref` |
| --- | ---: | ---: | ---: |
| `run_0031_limC_pshock` | 16.45741351 | 1207044.245 | 312.4399249 |
| `run_0032_limC_pshock_long` (同設定の継続) | 16.44493307 | 1201651.814 | 311.6185214 |

② ではこれらが実効平滑化強度 (ε²·q_ref²) を決めるので、**「同じ設定で追加 24000 step」が同じ数値作用素ではない**。
分割実行の回数や開始場が最終解に効きうる。**既定化する前に必ず潰す必要がある。**

**処置 (実装済み)**: `space.limiterRoRef` / `limiterPRef` / `limiterARef` を config キーにした。
**`>0` を書けばそれが優先され、起動時の自動決定は上書きしない**。0 (既定) なら従来どおり初期場から決めるが、
そのときはログに `(auto)` と**警告**を出し「この run は開始場に依存する作用素である」と明示する。
ログの `[limiter] scaled: ro_ref=...(auto|fixed)` がどちらで回ったかの一次情報。

**残作業** (§5.1 W1f): (a) 自動決定した値を checkpoint / 段階設定記録に保存して restart で継承する、
(b) **連続実行と分割実行の一致を必須ゲートにする**。現状は「手で固定値を書けば一致させられる」段階。

### 4.23 W2 設計: 面単位の非物理フォールバック (SU2 `bad_recon` 相当)

**位置づけ**: リミッタ (§4.13/§4.22) が保証するのは**近傍に対する有界性**であって**物理性ではない**。
有界性の前提は「ノード自身の値が健全」であること — 更新が負圧を作り EOS 床が洗浄した直後のような局面では
ψ がいくら正しくても守れない。そこを**面単位**で止めるのが本節。§4.22 の ② と**重複しない**。

**SU2 の実物** (`CEulerSolver.cpp:1946-1965`, `CFlowVariable.hpp:72-87`):

```cpp
bool bad_recon = neg_sound_speed || neg_pres_or_rho_i || neg_pres_or_rho_j;
bad_recon = nodes->UpdateNonPhysicalEdgeCounter(iEdge, bad_recon);
numerics->SetPrimitive(bad_recon ? V_i : Primitive_i, bad_recon ? V_j : Primitive_j);
// UpdateNonPhysicalEdgeCounter: 真なら counter=21、毎回 max(0, counter-1)、counter>0 を返す
```

- `neg_sound_speed` は **Roe 平均**の全エンタルピーと速度から `(γ-1)(H_roe - ½|u_roe|²) < 0` で判定する。
- 復帰先は**両側とも**セル平均値 `V_i`/`V_j` (片側だけ戻すのではない)。
- カウンタは `int8_t` の**辺ごと**。**辺を訪れるたびに減る**ので、陰解法の内部反復でも自然に期限切れする。

**forge 側の設計**:

| 項目 | 方針 | 理由 |
| --- | --- | --- |
| 判定 | `ro_L,ro_R ≤ roMin` / `P_L,P_R ≤ pMin` (既存の EOS 床キー [[eos-pressure-floor-config]]) + ρ/P の非有限。**速度の非有限は未検査 (残作業)** | forge は SI 次元なので固定 0 でなく設定済みの床を使う。**節点状態そのものが不正なら 1 次に戻しても不正なまま** — W2 は更新による状態破壊の代替にはならない (codex plan-3 Major 8) |
| 音速判定 | **第 1 段では入れない**。ρ/P で SERN ベースが捕まるかを先に見る | TP・多成分で Roe 平均エンタルピーを作るのは高く、効果が確認できてから |
| 復帰 | その面の **`conv_scheme` を局所的に −1** にして流れ 5 変数をセル値に戻す | **⚠ 「全変数が同じ `interp_dispatch` を通るので自動で整合する」は誤り** (codex plan-3 Major 8)。化学種の面値共有は S3 経路 (`speciesTransport_d.cu:802`)、凝縮モーメントは `condensationTransport_d.cu:300` で受動種経路と従来 1 次移流に分かれる。ΣY の保証は各側の正規化にも依存し、「片側だけ戻すと必ず ΣY≠1」も誤り。**第 1 段は node + SLAU/SLAU2 に限定**し、対応する S2/S3・受動種・凝縮設定を列挙してから広げる |
| ヒステリシス | 面ごと `int8_t` カウンタ。発火で `N+1`、訪問ごと `max(0, c-1)`、`c>0` の間 1 次 | SU2 と同一。毎 step 2次↔1次が振れるのを防ぐ |
| config | `space.badReconFallback: 0` (既定 OFF) / `N` = ヒステリシス回数 (SU2 相当は 20) | **既定 OFF の opt-in で入れる**。A/B が済むまで生産既定を動かさない |
| 診断 | 発火した face-side 数を既存の `g_*` 診断と同じ形で間隔出力 | 「発火しているのか」を推測でなく数で言うため |

**適用範囲**: 内部面のみ (`convectiveFlux_slau_d.inc.cuh` の主ループ)。境界半割面は再構成していない
([[node-boundary-flux-structure]]) ので対象外。

**⚠ 前提条件 (実装前に確認すること)**: §4.5 で「ベース run でのみ発火し対照では発火しない」と書いたのは
**削除済みの `check_wall_cv_drain.py` による後処理**の結果で、§4.20 で後処理の再構成判定は信用できないと
分かっている。**まず `limiterDiag` と同じ形でカーネル内に発火カウンタだけを入れ、ベース run と対照で
本当に差が出るかを測ってから**フォールバック本体を入れる。差が出ないなら W2 は本件の候補から外す。

### 6.4 W2 の検証計画 (codex plan-3 Major 9 を受けて改訂)

**前版のゲートは 3 つとも不適切だった**ので差し替える。(a) V1 の「対照でも発火したら中止」は汎用セーフガードの
有用性を否定しない、(b) V2 の「ビット同一」は §4.10–§4.15 で確認した演算順・反復非決定性と矛盾する、
(c) V3 の「発散 step が伸びる」は **§4.15 で自分が棄却した指標そのもの**である。

| ゲート | 内容 | 合格条件 |
| --- | --- | --- |
| **V1 発火の記録** | カーネル内カウンタのみ。**最初の発火時刻・面・復帰前後の値・両側の節点状態の健全性**を記録する | 発火が**発散に先行する**こと、かつ節点状態が健全なまま面状態だけが不正になっている面が在ること。対照の発火有無は中止条件にしない |
| **V2 既定不変** | `badReconFallback: 0` で標準ケースを回す | **同一バイナリ 2 反復のノイズ床を両側で測り** ([[noise-floor-both-sides]])、旧新差がその内側。**ビット同一は要求しない** |
| **V3 介入の効果** | `badReconFallback: N` で複数反復 | **非物理面の数が減る / 有限状態を維持する / 全残差が下がる / 目的量が定常化する**で判定。**生存 step の延長だけでは合格にしない** |
| **V4 退行なし** | V3 が有効なら標準ケースで有効化 | 目的量・残差床がノイズ床内 |
| **V5 カウンタの契約** | 単位は**流束評価回数**。block-DPLUR は固定残差の線形 sweep (`main.cpp:1456`) なので `nStepInner` ごとに流束を再評価しない = 実質**外部 step** 単位 | 再発火・期限切れ・restart 時の初期化・周期面の扱いを試験する |
| **V6 人工入力** | 正常面 / 負 ρ・P / 速度 NaN / 節点不正 / 多成分面 | 質量流束と化学種流束の整合を確認。節点不正は**別の失敗分類**にする |

### 4.24 W2 の結果 — **発火は実在したが、フォールバックしても発散は変わらない** (2026-09-19)

§4.23 の設計どおり実装した (`space.badReconDiag: N` で計測、`space.badReconFallback: N` で本体。**既定はどちらも 0**)。

**V1 発火の記録 — PASS** (対照の発火有無は中止条件にしない、§6.4 改訂): `run_0269_v1_base` (ベース有り) / `run_0270_v1_nobase` (対照)。5 flux 呼び出しごとの窓:

| 窓 (call) | ベース有り | 対照 (ベース無し) |
| --- | --- | --- |
| 5 | 0 / 617930 | 0 / 615490 |
| 10 | **5** (`P` 5, `ro` 0) | 0 |
| 15 / 20 | **5** (`P` 5, `ro` 0) | 0 |
| 25 | **6** (`P` 6, `ro` 1) | 0 |
| 30 / 35 | (step 28 で NaN 停止) | 0 |

**発火は NaN (step 28) より先行する** (call 10 ≒ step 2 付近) ので、V1 の合格条件「発火が発散に先行する」を満たす。
**ベースでのみ発火し対照は 40 step 全窓でゼロ**。§4.5 の後処理による同趣旨の主張は §4.20 で無効になっていたが、
**カーネル内で測り直して同じ結論が立った**。あわせて **`P` が先 (call 10)・`ro` は 18 step 遅れて (call 25)** と分かり、
detectNaN が `ro` を指すのは終端症状だという [[detectnan-reports-symptom-not-cause]] の見立てと整合する。

**V2 既定不変 — PASS** (ノイズ床基準。§6.4 改訂後の判定): 既定 (`0`) のまま同一入力を新旧バイナリで走らせ、**同一バイナリ 2 回走のノイズ床と比べた**
([[noise-floor-both-sides]])。`case/05` `run_0023/0024_v2_bitcheck`: 旧 vs 新 1.19e-4 < ノイズ床 **1.51e-4**。
`case/44` `run_0494/0495_v2_bitcheck`: 旧 vs 新 2.81e-4 < ノイズ床 **3.38e-4** (状態量は ρ 4.6e-6 / P 6.0e-7 / T 4.3e-6 で
いずれもノイズと同桁)。**「ビット同一」という §6.4 の文言は誤り** — `case/05` の node 3D periodic は
同一バイナリでも 1.5e-4 再現しない。判定はノイズ床比較が正しい。

**V3 介入の効果 — 負の結果**: `run_0271_v3_base_fb20` (`badReconFallback: 20`, SU2 相当) / `run_0272_v3_base_fb5` (`5`)。
`active1st` は 5→6 と V1 で数えた面とちょうど同数が 1 次に落ちており **機構は設計どおり動いている**。
**⚠ 「非物理面の数が減らない」は測定になっていない** (codex 2026-09-20 result レビュー Major 8):
カウンタは `convectiveFlux_slau_d.inc.cuh:205` で**フォールバック前**に数えており、復帰は 223 行以降である。
したがって `faces=6, active1st=6` から「フォールバック後も 6 面残った」とは言えない。
節点状態の健全性も復帰後の状態も記録していない。**言えるのは「この介入では run 全体の発散を防げなかった」まで**。
NaN は `run_0208`/`run_0269` と同じ step 28 で出る。
(生存 step の一致だけを根拠にはしない — §4.15 でその指標は棄却済み。ここでは**非物理面の数が減らない**ことが主根拠。)
**カウンタの単位は流束評価回数**だが、block-DPLUR は固定残差の線形 sweep (`main.cpp:1456`) で `nStepInner` ごとに
流束を再評価しないので**実質は外部 step 単位**であり、28 step の run では `N=20` の期限切れは一度も起きていない。

**結論**: **非物理な再構成はベース発散の駆動要因ではなく、NaN の 18 step 前に現れる症状**である。
W2 は本件の候補から外す。コードは **opt-in (既定 0) のまま残置**する — V1 の計測器は
「発火しているのか」を推測でなく数で言える唯一の手段で、他ケースの切り分けに使える
([[optin-retained-is-not-rejected]] のとおり opt-in 残置は削除対象ではない)。V4 は V3 が負なので実施しない。

**次にどこを見るか** → **§4.28 で決着した** (真因は (b) の系: ベース後流の解像度不足)。

### 4.26 周期経路にも G1 を入れたら、Sod で **② が ① より悪かった** — K を振って決着 (2026-09-19)

**W1h 実装**: 周期 node は ψ が `periodicGatherMinArray_d_wrapper` の**後**に確定するので、通常経路の pass3
(`limiter_r1_fused5_d` 内) が使えない。確定した ψ を受け取る別カーネル `limiter_g1_check_periodic_d` を足した。
評価点・増分は**流束側の規則だけ**で決める (§4.20 と同じ)。

**測定器にもう 1 つ穴があった**: 許容幅 `max(近傍レンジ, 値の大きさ)×1e-5` は、**恒等的に 0 の変数**
(疑似 1D の `Uy`/`Uz` など) では 0 になり、**float ノイズが全部「逸脱」に化けていた** (`Uy` 129280 件、
最大逸脱 66667 倍という非現実的な値で気づいた)。**構成によらない絶対床 `1e-6·q_ref` を敷いた**。
そのために `main.cpp` の基準値計算を `limiterScaled == 1 || limiterDiag > 0` に広げた
(こうしないと ① と ② で許容幅が変わり比較にならない)。

**結果 — Sod (`case/05`, node periodic, 200 step, 面側 3611520)**: ① と ② の**優劣が逆転する**。

| 構成 | `ro` | `Ux` | `Uy` | `Uz` | `P` | 最大逸脱 (許容幅比) |
| --- | --- | --- | --- | --- | --- | --- |
| ① 生産既定 (`run_0034_g1perA200`) | **49712** | 0 | 0 | 0 | 0 | ρ **22.7×** |
| B 評価点一致・旧式 (`run_0035_g1perB200`) | 52524 | 0 | 0 | 0 | 0 | ρ 22.6× |
| ② K=1.0 (`run_0030_ksweep_1.0`) | **86674** | **153157** | **128688** | **128664** | **95885** | 速度 **308×** |
| ② K=0.3 | 30959 | 87791 | 79336 | 79285 | 38637 | — |
| ② K=0.1 | 3071 | 40371 | 37822 | 38447 | 8890 | 速度 9.7× |
| ② **K=0.05** (SU2 既定) | **0** | 20502 | 19022 | 19230 | 4402 | 速度 **3.4×**, P 1.9× |

**現行の `venkatK: 1.0` は Sod で全変数を悪化させる**。K を下げると ρ は単調に 0 へ向かい、
**K=0.05 で ρ 逸脱 0**。残る速度・P の逸脱は最大でも許容幅の 3.4 倍で、**① の ρ 逸脱 22.7 倍より 7 倍小さい**。
L1 精度は K に対してほぼ平坦 (K=1.0 の 6.914e-3 → K=0.05 の 6.905e-3) なので、**K を下げても精度は落ちない**。

**case/46 では K=1.0 / 0.1 / 0.05 のいずれも `VERDICT=CLEAN`** (`run_0275_g1fix_C`, `run_0278_k0.05`, `run_0279_k0.1`)
なので、K を下げる副作用は見えない。

**判断 (§4.22 の推奨を更新)**:

- **② を採るなら K は 0.05 (SU2 既定) にする**。現行の `venkatK` 既定 1.0 は Sod で ① より悪い。
- **② は ① の厳密な上位互換ではない**。① と ② は「ε² を全変数共通の生の体積で与えるか、変数ごとの
  基準で無次元化して与えるか」の違いなので、**変数間の厳しさの配分が変わる** — ρ に厳しく、速度・P に緩くなる
  (Sod) / 全変数で厳しくなる (SERN)。どちらが良いかは場に依存する。
- したがって **「② は有界性を直す」と単純化しない**。言えるのは「**K=0.05 の ② は、ρ で ① を明確に上回り、
  速度・P で ① よりわずかに劣るが、逸脱量は ① の ρ より 1 桁小さい**」まで。

### 4.27 W1f 実測 — 基準値の restart ドリフトは**実在するが桁が小さい** (2026-09-19)

§4.25 で「基準値が restart のたびに変わる」ことは示したが、**それがどれだけ解を動かすか**は測っていなかった。
`case/44` (軸対称 TP + 凝縮, `venkatK: 0.05`) で 2 つ測った。

**(1) 連続 vs 分割** (`run_0497_w1f_cont_pin` = 連続 4000 / `run_0498`+`run_0501_w1f_half*_pin` = 2000+2000。
auto 版は `run_0499`/`run_0500`/`run_0502`):

| | 連続 4000 vs 分割 2000+2000 の最大相対差 |
| --- | --- |
| 基準値 **固定** | 1.995e-5 (`Uy`) |
| 基準値 **自動** | 1.670e-5 (`Uy`) |

**固定しても縮まない**。分割時の auto 基準値のドリフトは ρ_ref 0.7786498524 → 0.7787124601 = **相対 8e-5** しかなく、
差は**forge 自身の restart 非忠実性** ([[forge-sst-restart-nonfidelity]]) に埋もれている。

**(2) 基準値そのものの感度** (同一開始場・連続 4000 step なので restart ノイズ無し。`run_0503_w1f_ref_x2` / `run_0504_w1f_ref_x05`):

| 基準値 | 解の最大相対差 |
| --- | --- |
| **×2** | 2.106e-4 (`Uy`) |
| **×0.5** | 6.062e-5 (`Uy`) |

**基準値を 2 倍にしても解は 2e-4 しか動かない**。実際に観測されたドリフト (8e-5) はこれよりさらに 4 桁小さい。

**⚠ 主張を限定する** (codex 2026-09-20 result レビュー Major 5): 上の比較で見ているのは**流れ変数の最大差**である。
(a) 分割時に実際に動いたのは ρ_ref の 8e-5 だけでなく **`a_ref` が 398.7246823→399.2868456 = 相対 1.41e-3**、
(b) 凝縮モーメントを入れると固定・連続 vs 分割の `Q0_0` は **3.892e-4** で流れ変数より 1 桁大きい、
(c) このケースは `model: none` なので **SST 由来の restart 非忠実性という説明は未確認**、
(d) ×2 の有限差分から 8e-5 の微小摂動を線形外挿する根拠は無い。
**言えるのは「このケース・4000 step・流れ変数では、自動基準の追加影響を分離できなかった」まで**。

**判断**: codex plan-3 Major 6 は**構造的には正しい** (auto のままでは分割実行が同じ作用素でない)。
現時点の処置は以下だが、**② を既定化するなら基準値の保存・継承を必須に戻す** (§5.1 W1f):

- `limiterRoRef`/`limiterPRef`/`limiterARef` の明示指定 (実装済み, §4.25)。
- auto のとき**そのまま貼れる YAML 行をログに出す** (`[limiter]   limiterRoRef: ..., limiterPRef: ..., limiterARef: ...`)。
- **checkpoint への自動継承は実装しない**: 効果が restart 非忠実性より 4 桁小さく、
  「重要な run では固定値を書く」で足りる。ここに工数を掛けるより ② の K (§4.26) と G4 の方が効く。
- **既定化するなら基準値の保存・継承は必須に戻す** (前版の「ブロッカーから外す」は撤回)。当面は固定値を手で書く。

### 4.28 ベース発散の真因 — **メッシュがベース後流を 1 セルで跨いでいる** (2026-09-19, 決着)

これまで「壁の再構成」「リミッタ」「非物理フォールバック」を疑って全部外した。**状態を 1 step 刻みで直接見た**ら
即座に分かった (`run_0269_v1_base`, `outStepInterval: 1`)。

**ベース節点 (id 62103, x=1.07732, y=0.27182) の履歴**:

| step | P [Pa] | ρ | T [K] | \|U\| |
| --- | --- | --- | --- | --- |
| 0 | 1475.9 | 4.462e-3 | 1000 | 0 |
| 6 | 1186.9 | 3.429e-3 | 1000 | 0 |
| 12 | 836.0 | 2.365e-3 | 1000 | 0 |
| 18 | 484.7 | 1.308e-3 | 1000 | 0 |
| 23 | 218.5 | 5.042e-4 | 1000 | 0 |
| 24 | 164.8 | 6.211e-5 | 1000 | 0 |
| 27 | 32.7 | **−4.47e-5** | 1000 | 0 |
| 28 | 0 | NaN | — | 0 |

**ρ が毎 step 約 5 % ずつ単調に抜けていく**。T は等温壁で 1000 K に固定、u は `nodeWallDirichlet` で 0 に固定なので、
**ρ だけが自由**で、連続の式が抜き続ける。P=ρRT なので P も道連れ。振動でも衝撃でもなく**純粋な排出**である。

**なぜ抜けるか — 幾何を見れば明白**:

| 量 | 値 |
| --- | --- |
| ベース (no-slip) の節点数 / 厚さ | **9 節点 / 2.0 mm** (壁に沿う間隔 0.25 mm) |
| **ベース直後の後流第 1 節点までの距離** | **8.19 mm** (= ベース厚さの **4.1 倍**) |
| その節点の \|U\| | **1030〜1230 m/s** |
| ベース節点の \|U\| | 0 (ピン) |

**ベース後流を 1 セルで跨いでいる**。壁節点 (u=0) から 8.19 mm 先 (u≈1100 m/s) へ向かうエッジの中点は壁から
4.1 mm の位置にあり、2 次再構成はそこに **u_face ≈ 550 m/s** を与える。これは近傍レンジ [0, 1100] の内側なので
**どんなリミッタでも止められない** (Barth も ② も ③ も効かなかった理由)。面積 0.25 mm × 体積 ~1e-6 m³ の
薄い双対 CV から ρ·u_f·A/V ≈ 600 kg/m³/s で質量が出ていき、**実測の 5 %/step と桁が合う**。

**A/B が全部この説明で揃う**:

| 変更 | 結果 | 説明 |
| --- | --- | --- |
| `solver: ROE` (`run_0280`) | NaN step **26** | 流束スキームは無関係 (どのスキームも同じ面速度を使う) |
| `convMethod: 0` 1 次 (`run_0282`) | **200 step 完走** | 風上 1 次は壁側の u=0 をそのまま使う → 流出しない |
| `nodeWallDirichlet: 0` (`run_0283`) | 完走 | 速度ピンが無ければ壁節点も流れに乗る |
| **`slip`** (`run_0287`) | **200 step 完走・非物理再構成 0** | no-slip でなければ起きない |
| `wall` 断熱 (`run_0285`) | NaN step **22** | **熱条件は無関係** |
| `wall_isothermal` Ts=300 (`run_0286`) | NaN step **24** | 同上 |
| `lowMachPrecond: 2` (`run_0281`/`run_0284`) | 28 → **160** step | 遅らせるだけ。根治せず |
| リミッタ (Barth / ② / ③) §4.15/§4.16 | 効かない | 面値は有界 — 有界性の問題ではない |
| 面単位フォールバック §4.24 | 効かない | 面状態は物理的 (ρ>0, P>0) — 物理性の問題でもない |
| 後縁 station クラスタ `run_0213` (Δx 0.082→0.025) | **悪化** (step 10) | 0.025 m は依然ベース厚さの **12 倍**。全く足りていない |

**結論**: これは**スキームの欠陥ではなく解像度不足**である。ベース背後の剪断層/再循環を 1 セルで跨いでいるので、
線形再構成が「壁から 4 mm で 550 m/s」という**格子上は正しいが物理的に無意味な**面状態を作る。
**本 plan (`convection-node-wall-reconstruction`) の守備範囲はここで終わり** — 対策はメッシュ側
([`tooling-nozzle-sern-3d.md`](tooling-nozzle-sern-3d.md)) に移す。

**要求仕様**: ベース直後の第 1 station 間隔を**絶対値で** `t_base / 5` 以下にする (現状 8.19 mm → **0.4 mm 以下**、約 20 倍の細分)。
壁法線方向に `first_wall_frac` があるのと同じ役割を、ベースでは**流れ方向**に持たせる必要がある。
`check_mesh_quality.py` はこれを捕まえない (AR 33 で合格してしまう) ので、**設計チェーン側のゲート**にする。

**W4 の位置づけ**: §4.5 で「壁からの再構成は片側勾配の理論値に厳密一致 = 異常でない」として撤回したが、
**それ自体は今も正しい**。異常なのは再構成ではなく、**その理論値が物理と無関係になるほど格子が粗い**こと。
「壁ノードの再構成を特別扱いする」対策は依然として不要。

### 4.29 §4.28 の検証 — **排出は消えたが、収束はしない** (2026-09-19)

メッシャに**絶対値の後流第一間隔** `first_wake_frac` を足し (`mesh_sern.py` / `mesh_sern3d.py`、
`t_base > 0` なら必須・`t_base/5` を超えたら生成を失敗させる)、`t_base > 0` のときは後縁で**必ず** station を
分割するようにした (分割しないと `first_wake_frac` が効かない — 最初これを見落として同じ 8.19 mm を再生成した)。

生成結果 (`run_0288_r4e_wake`): ベース直後の第一間隔 **8.19 mm → 0.400 mm** (ベース厚さの 4.1 倍 → **0.20 倍**)、
節点数 62069 → 62324 (**+0.4 %**)、`check_mesh_quality.py` **VERDICT: PASS** (AR max 677 / skew max 0.439)。

**2×2 (メッシュ × CFL、本段 config、40 step、ベース節点 id 62103 の Δρ/ρ)**:

| | cfl 1.0 | cfl 5.0 |
| --- | --- | --- |
| **粗** (8.19 mm) | NaN step **28** / 排出 **−3.94, −4.03, −4.16, −4.32, −4.53 %/step** (`run_0293`) | NaN step **28** / 排出 **同じ数値** (`run_0269`) |
| **細** (0.40 mm) | **40 step 完走** / 排出 **−0.00 %/step** (`run_0291`) | NaN step **34** / 排出 **±0.00〜0.02 %/step** (`run_0292`) |

**§4.28 の診断は確認できた**: 後流を細分すると**ベース節点の排出が完全に消える** (5 %/step → 0.00 %/step)。
CFL を 1/5 にしても粗メッシュでは**何も変わらない** — `dt_local` が両者とも 3.638e-7 で同一、排出率も同一値で、
**cfl は当該節点の dt を律速していない**。つまりこれは時間刻みの不安定ではなく**空間離散の流束不均衡**である。

**ただし収束はしない**。細メッシュでも cfl 5 で step 34 に NaN が出る。**場所が変わっている**:

| run | NaN の範囲 | ベース面 (x=1.07732, y=0.27082–0.27282) との関係 |
| --- | --- | --- |
| `run_0293` 粗 cfl1 | x 1.0773–1.1120, y 0.2706–0.2728 | **ベース面から下流** = §4.28 の排出そのもの |
| `run_0292` 細 cfl5 | x **1.0702**–1.0808, y **0.2694**–**0.2791** | **ベース面より上流**にも、y もベース幅の外へ広がる = **肩 (shoulder) 領域の別現象** |

**結論**: ベース面の排出は解像度で解決した。**残るのは肩の問題**で、これは本 plan の対象 (壁からの再構成) ではない。
⑤ の R4e を進めるには、次はこの肩を見る必要がある ([`tooling-nozzle-sern-3d.md`](tooling-nozzle-sern-3d.md) へ)。

**メッシャ側の成果** (⑤ チェーンに恒久的に残るもの):

- `SernMeshParams.first_wake_frac` / `SernMesh3DParams.first_wake_frac` (絶対値 /H)。
- `t_base > 0` で未設定または `> t_base/5` なら **`ValueError` で生成を失敗させる** (house style: 粗ければ黙って通さない [[geometry-must-not-follow-mesh-spacing]])。
- `runner_sern.py` / `runner_sern3d.py` が `mesh.first_wake_frac` / `mesh3d.first_wake_frac` を読む。
- **`check_mesh_quality.py` はこれを捕まえない** (AR 33 で PASS してしまう)。ベース後流の解像度は**設計チェーン側のゲート**である。

### 4.30 W1g 格子細分 — **② は解像度に対して頑健** (2026-09-20)

codex plan-3 Major 5 の採用分。`case/05` Sod を **100 / 200 / 400 セル**で生成し直し (`.geo` の
`Transfinite Line` を変更 → gmsh → `convertGmshToForge` → `gen_sod3d_ic.py`)、
**CFL を固定するため dt と step 数を h に比例させた** (5.0e-7×100 / 2.5e-7×200 / 1.25e-7×400、いずれも t=5e-5 s)。
① は生産既定 (`mr0/scaled0/K=1`)、② は §4.26 の推奨 (`mr1/scaled1/K=0.05`)。

| 格子 | 構成 | L1(ro) | L1(P) | L1(u) | 衝撃後 ρ | G1 `ro` | G1 速度 | 面側 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 100 | ① | 1.192e-2 | 6.571e-3 | 1.150e-2 | 1.7378 | **27335** | 0 | 4.53e6 |
| 100 | **②** | 1.217e-2 | 6.459e-3 | 1.117e-2 | 1.7631 | **0** | 13162 | 4.53e6 |
| 200 | ① | 7.048e-3 | 3.212e-3 | 6.048e-3 | 1.9913 | **49712** | 0 | 1.81e7 |
| 200 | **②** | 6.905e-3 | 3.062e-3 | 5.909e-3 | 2.0517 | **0** | 20507 | 1.81e7 |
| 400 | ① | 3.520e-3 | 1.545e-3 | 2.887e-3 | 2.1972 | **88433** | 0 | 7.21e7 |
| 400 | **②** | 3.349e-3 | 1.483e-3 | 2.855e-3 | 2.2121 | **0** | 33508 | 7.21e7 |

(厳密 Riemann の衝撃後 ρ = 2.2240)

**収束次数 (L1(ro))**: ① **0.76 / 1.00**、② **0.82 / 1.04**。衝撃を含む L1 として妥当な 1 次で、**② がわずかに上**。

**⚠ G1 の数字は「窓」である** (codex 2026-09-20 result レビュー Major 6): 印字は**直前 `interval` 回の呼び出し**の集計で、
出力ごとにリセットされ**終了時の flush が無い**。RK3・200 step = 600 呼び出しに対し最後の 199 回は未出力だった。
**累計出力 (`CUM ...`) を追加して修正済み**だが、上表の値は全期間集計ではない。
また格子は **x 方向だけ**の細分 (101→201→401 × 9 × 9) なので `ε²∝volume` は 1/2 ずつであり、
3 方向一様細分の 1/8 とは異なる。**本試験の合格範囲は「Sod の流れ方向細分に対する有限時刻 L1 誤差」に限定する**。
滑らかな極値を含む平面・軸対称の格子感度は別途必要。

**有界性は解像度に対して頑健** (上の窓の範囲で): ② は **3 解像度すべてで `ro` 逸脱 0**。① は絶対数が 27335 → 49712 → 88433 と増えるが、
面側あたりの率では 0.60 % → 0.28 % → 0.12 % と下がる。② の速度逸脱率は 0.29 % → 0.11 % → **0.046 %** で、
**① の `ro` 逸脱率より速く落ちる**。細分で悪化する病理は無い。

**W1g の格子細分は合格**。残るのは標準ケース (08/20/13/48) の回帰と G4。

### 4.31 標準ケース回帰は **case/08 が現行バイナリで壊れていて実施できない** (2026-09-20)

codex plan-3 Major 7 の「標準ケース (08/20/13/48) で回帰せよ」に着手した結果:

**case/20 と case/13 は全 run が cell**。本変更は config 検証で **node 以外では `limiterScaled` を無効化**するので
(`solverConfig.cpp`)、構造上**効きようがない**。[[user-prefers-node-base]] のとおり cell は使わない方針でもあるので、
この 2 ケースは「対象外」と記録する (回帰を回しても情報が無い)。

**case/08 は node + `limiter: 2` の run があるが、現行バイナリで再現しない**:

| run | バイナリ | 結果 |
| --- | --- | --- |
| `run_dual_hiM_node_m3imp` (**保管**) | 当時 | **PASS (converged)**、`rms_ro` 4.0 桁低下 |
| `run_0020_std8_A` (同入力・生産既定) | 今日のビルド | **DIVERGED**、最初の NaN step **217** |
| `run_0021_std8_C` (同入力・② K=0.05) | 今日のビルド | **DIVERGED**、step **240** |
| `run_0022_std8_bisect_new` (診断キー無し) | 今日のビルド | **DIVERGED**、step **228** |
| **`run_0023_std8_bisect_old`** (同上) | **`build-native` (09-19 10:16, 本セッションの変更前)** | **DIVERGED**、step **243** |

**私の変更ではない** — 本セッション以前のバイナリでも落ちる。切り分けとして確認済み:

- ~~step 0 の残差が保管 run とビット一致~~ **誤り** (codex 2026-09-20 result レビュー Major 7)。
  一致するのは `rms_ro` と `rms_roe` だけで、**`rms_roUy` は 21.70760 → 0.000200996 と 10⁵ 倍違う**
  (`rms_roUx` も 38.25486 → 37.70877)。**`rms_ro` だけ見て「一致」と書いた** — AGENTS.md が禁じている判断そのもの。
  メッシュ・BC ファイルの SHA-256 は同一なので、**差は step 0 の y 運動量残差の作り方**にある。
  bump は **slip 壁**なので、node の slip 境界の扱いが変わった痕跡とみるのが自然。
- **複製時に禁止フラグ `mesh.bndFirstOrder: 1` を落とし忘れていた** (保管 run に入っていたものをそのまま複製した)。
  外して再実行しても (`run_0025`/`run_0026_std8_nobnd_*`) **step 0 残差も発散も変わらない** ので結論は動かないが、
  [[bndfirstorder-banned]] の手順違反なので記録する。
- 旧キーの移行 `LESorRANS: 0 / LESmodel: 0` → `model: "none"` は**正しい**
  (`LESorRANS` 1=LES / 2=RANS。[`turbulence-sigma-model.md`](../accepted/turbulence-sigma-model.md),
  [`architecture-axisym-sst.md`](../accepted/architecture-axisym-sst.md))。
- 保管 run のログと現行ログで**有効設定値に差は無い**。旧のみに出るのは削除済みキー
  (`mesh.meshFormat`, `time.last.control`, `physProp.isCompressible`, `physProp.ro`) だけ。
- 228 vs 243 の差は §4.15 で棄却した「生存 step」の float カオス帯で、意味を持たせない。
- **さらに遡れない**: `build-passive` (09-18) は削除済みの `meshFormat` キーを**要求する**ので同じ config で走らない。
  本格的な bisect は旧コミットを旧 config でビルドし直す必要があり、別作業。

**結論** (→ §4.33 で更新): 標準ケースでの ② の回帰は**依然できない**が、理由は「case/08 の退行」ではなく
**node + 2 次で収束する標準ケースが無い**ことである。
ただし **case/20・13 を「cell だから恒久的に対象外」とはしない** (codex Major 7): node 入力を用意すれば使える。
case/48 も別セッションの占有が解ければ使える。**恒久免除ではなく「現時点で手持ちの node 基準が無い」**が正しい。
これは ② とは独立の退行で、**標準検証ケースが 1 本死んでいる**という事実自体が報告対象である。
§5.1 に W1i として残す。

### 4.32 **平面 2D の ② はこれまで一度も動いていなかった** (codex 2026-09-20 result レビュー Critical 1)

`A_planar` は `variables.cpp:610` の **`isAxisymmetric == 1` の中でしか device へ転送されない**。
一方 `limiter_d.cu` は**配列の存在だけで選んで**いた (`var.c_d.count("A_planar") ? ... : volume`)。
`A_planar` は常時確保されゼロ初期化もされないので、**平面 2D (非軸対称) では未初期化メモリを `sqrt` していた**。
実質 0 だったため `ε² = (K/L_ref)³·h_i³ = 0` となり、**`venkatK` が一切効いていなかった**。

**証拠**: `run_0275_g1fix_C` (K=1.0) / `run_0278_k0.05` / `run_0279_k0.1` の `res_1.h5` は
**5 変数の `limiter_*` がビット同一** (最大差 0.0)。「K を下げても SERN は CLEAN のまま = 副作用なし」と
書いたのは**誤読**で、正しくは「K が何もしていなかった」。

**影響範囲**: **平面 2D × `limiterScaled: 1` の測定はすべて無効**。
`case/46` (平面 2D) の §4.20 / §4.21 / §4.26 の ② の数値が該当する。
**無効でないもの**: `case/05` Sod (3D なので `lenArea=0` → `cbrt(volume)`)、`case/09` TGV (3D)、
`case/44` (軸対称なので `A_planar` は転送済み)。**§4.26 の K スイープと §4.30 の格子細分は Sod なので有効**。

**修正** (実装済み): 面積配列の選択を **`isAxisymmetric` で決める** (`cfg.isAxisymmetric == 1 ? A_planar : volume`。
平面 2D の `volume` は奥行 1 の面積そのもの)。`.at()` にして存在しなければ落ちるようにした。
再発防止として、`limiterDiag` が ON のとき **h_i ≤ 0 / 非有限を数え、`LIMG1 WARNING: 幾何尺度 h_i が 0/非有限 … = eps^2 が潰れて venkatK が効いていない` と印字**する。

**再測定 (`run_0309`–`run_0314_g1fix2_*`, 面側 246196)** — §4.20 の表を置き換える:

| 構成 | `mr` | `scaled` | K | `ro` | `Ux` | `Uy` | `Uz` | `P` | 最大逸脱 (許容幅比) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A ① **生産既定** | 0 | 0 | 1.0 | **8146** | **94** | **919** | 0 | **141** | `Uy` **10651×** |
| B 評価点だけ一致 | 1 | 0 | 1.0 | **8131** | 0 | **805** | 0 | 0 | `Uy` 11.7× |
| C1 ② 無次元化 | 1 | 1 | **1.0** | **797** | **133** | **1777** | 0 | **758** | `Uy` 194× |
| **C005 ② 無次元化** | 1 | 1 | **0.05** | **0** | **0** | **133** | 0 | **0** | `Uy` **3.5×** |
| D ③ 比の形 | 1 | 2 | — | **0** | 0 | **0** | 0 | 0 | CLEAN |
| E Barth | 1 | — | — | **0** | 0 | **0** | 0 | 0 | CLEAN |

(`Uy` の絶対数が §4.20 の 20626 から 919 に落ちたのは、その後 §4.26 で入れた**許容幅の絶対床 `1e-6·q_ref`** が
恒等 0 近傍の float ノイズを除いたため。A/B は `limiterScaled` を使わないので挙動自体は不変。)

**結論が強まった**: K は SERN でも決定的に効き、**K=1.0 は `ro` 797・`Uy` 1777 と悪いが、K=0.05 は
`ro`/`Ux`/`P` が 0・`Uy` だけ 133 (許容幅の 3.5 倍)** で Barth/比の形にほぼ並ぶ。
**§4.26 の「K=0.05 にすべき」は Sod と SERN の 2 ケースで独立に裏づけられた**。

**スケール不変性の再測定 (`run_0315`–`run_0322_sc2_*`)**: §4.21 の結論は変わらない。

| 構成 | ψ の最大差 | ψ が変わったノード | ψ_ro 平均 (ref → ×1024) |
| --- | --- | --- | --- |
| A ① 生産既定 | 9.98e-01 | 58761 / 62069 | 0.9718 → **0.99998** |
| B 評価点一致 (旧 Venkat) | 9.98e-01 | 58919 / 62069 | 0.9715 → **0.99998** |
| **C ② K=0.05** | **0** | **0** | 0.8107 → 0.8107 |
| D ③ 比の形 | **0** | **0** | 0.7101 → 0.7101 |

**② と ③ が初めて区別できる** (ψ_ro 平均 0.8107 vs 0.7101)。ε² が実際に効いている証拠でもある。
前版で「②と③は有界性でもスケール不変性でも区別できない」と書いたのは、**②が動いていなかったから**であり撤回する。

### 4.33 W1i case/08 の追跡 — **「退行」ではなく自由流保存の修正だった** (2026-09-20)

§4.31 で「保管 run は PASS、現行は DIVERGED」と書いたが、**step 0 の `rms_roUy` の差 (21.708 vs 0.000201) は退行ではない**。

**IC は完全に一様**である (`run_0025_std8_nobnd_new/res_0.h5`: ρ 0.400721 std 2.98e-8、
P 21839.5 std 0.00195、Ux 455.772 std 3.05e-5、**Uy std 厳密 0**)。
一様状態なら閉じた CV で $\oint p\,\mathbf{n}\,dS = 0$ なので、**`rms_roUy \approx 0` が正しい**。
上下は `slip` 壁なので y 方向には何も残らないはずで、**保管 run の 21.708 の方がスプリアスな壁力**である。
`rms_roUx` が両者とも大きい (37.7〜38.3) のは入口 `inlet_Pressure` と出口 `outflow` が x 方向に本物の残差を作るため。

**`space.pRef: 21839.5` を足すとさらに 13 倍良くなる** (`rms_roUy` 2.01e-4 → **1.54e-5**)。
[[forge-freestream-nonorthogonal]] の pRef ゲージが効いている = **間に入ったのは修正であって退行ではない**。
**保管 run の PASS は妥当な目標ではない**ので、§4.31 の「標準検証ケースが 1 本死んでいる」は
「**保管 run が古い (スプリアスな壁力を含む) 解で回っていた**」に訂正する。

**健全な node 基準は作れた**: `run_0031_std8_cm0` (`convMethod: 0` + `pRef`) は
**`PASS (converged)`、全列 5.1〜5.3 桁低下**。

**ただし 2 次以上は落ちる**:

| 構成 | `convMethod` | `cfl_pseudo` | リミッタ | 結果 |
| --- | --- | --- | --- | --- |
| `run_0031_std8_cm0` | **0** | 3 | — | **PASS (5.1〜5.3 桁)** |
| `run_0028_std8_cm1` | 1 | 3 | 既定 | DIVERGED |
| `run_0030_std8_cm1cfl1` | 1 | **1** | 既定 | DIVERGED |
| `run_0029_std8_cm2cfl1` | 2 | **1** | 既定 | DIVERGED |
| `run_0032`/`run_0033_lim8_{A1,C1}` | 1 | 3 | ① / **② K=0.05** | **両方 DIVERGED** |
| `run_0034`/`run_0035_lim8_{A2,C2}` | 2 | 3 | ① / **② K=0.05** | **両方 DIVERGED** (cm2 は step 182) |

**リミッタ修正では直らない**。

**⚠ slip 壁説も棄却 (2026-09-20)**: 上下を `slip` → `wall` (no-slip) に変えても**同じく DIVERGED**
(`run_0036_slip8_noslip` vs `run_0037_slip8_slipref`)。さらに **`case/05` Sod は slip 壁 2 枚・`convMethod: 2` で完走**する。
したがって [[node-slip-spurious-flow]] は本命ではなく、**case/08 の 2 次発散の原因は未特定**である。

**「2 次は動かない」ではない** — 他のケースでは動いている:

| ケース | `convMethod` | slip 壁 | 判定 |
| --- | --- | --- | --- |
| `case/46` SERN 有限ベース | 1 | 0 | NOT CONVERGED (plateau) だが**完走** |
| `case/05` Sod | **2** | **2** | 完走 |
| `case/09` TGV | 1 | 0 | 完走 |
| `case/44` 汚染空気ノズル | 1 | 1 | 完走 |
| `case/36` 擬似衝撃波 | 1 | 1 | 完走 (still converging) |
| **`case/08` bump** | **1 も 2 も** | 2 | **DIVERGED** |
| `case/08` bump (1 次) | 0 | 2 | **PASS (5.1〜5.3 桁)** |

**6 ケース中 5 つで 2 次は動く**。case/08 だけが落ち、**理由は分かっていない**。

**W1g への影響**: リミッタは `convMethod ≥ 1` でしか働くので、**1 次でしか回らない case/08 は
リミッタ回帰には使えない**。標準ケース回帰は依然ブロックされている。
必要なのは「**node + 2 次で収束する標準ケース**」であり、それを作るには node slip 壁の問題を先に潰す必要がある。

### 4.34 case/08 の 2 次発散の真因 — **超音速入口に亜音速用 BC を使っていた** (2026-09-20)

§4.33 で「原因未特定」としたものを、SERN で効いた方法 (**節点の状態を 1 step 刻みで見る**) で特定した。

**発散は壁ではなく入口の 1 節点から始まる** (`run_0039_diag2nd_dense`, `outStepStart: 150, outStepInterval: 1`):

- step 195 まで場は健全で**収束方向**に進んでいる (`ro_min` 0.1528 @ (1.9768, 0.0094) で安定、`P_min` 6548)。
- step 202 の NaN は **6076 節点中たった 1 つ**、座標 **(0.0000, 0.5208)** = **入口境界 `x=0` 上の節点**。
  壁でも角でもない (y は領域の中ほど)。

**その節点 (id 317) の履歴** — 隣接する入口列のノードは 0.39 前後で健全なまま:

| step | 189 | 193 | 196 | 199 | 200 | 201 | 202 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ρ | 0.2604 | 0.2101 | 0.1526 | 0.0500 | **0.0068** | **−0.0314** | NaN |
| P | 1.24e4 | 9469 | 6406 | 2046 | **70.95** | **1 (床)** | 1 |
| Ux | 511.8 | 532.9 | 557.3 | 599.2 | **628.8** | **856.5** | −2.3e5 |

**ρ↓ → Ux↑ → Ps↓ → ρ↓ の正帰還**で 13 step かけて枯れる。

**原因**: この入口は `kind: inlet_Pressure` (Pt=100 kPa, Tt=293.15 K) だが、
**IC の入口 Mach は 1.650** (ρ 0.400721, P 21839.5, Ux 455.772 → 音速 276.23 m/s) = **超音速**である。
`inlet_Pressure` の閉包は**内点の Mach から等エントロピーで Ps・ρ を決める**
(`boundaryCond_d.cu:1058` `mach_c = Un_c/sonic[ic]`、`rf=0` で速度参照のみ) **亜音速入口用**であり、
超音速入口では全特性が流入なので内点に依存させてはいけない。
[`procedures/divergence-and-startup.md`](../../procedures/divergence-and-startup.md) §(B) の
「超音速向けの境界条件を亜音速に使う」の**裏返しの誤り**である。

**検証**: 入口だけ `inlet_uniformVelocity` (ρ/U/Ps 固定 = 超音速入口用) に替えると
(`run_0040_inletuni`) **5000 step 完走・NaN なし** (`NOT CONVERGED (stalled/plateau)`, 1.2〜1.4 桁低下)。
`convMethod: 1` + `limiter: 2` のままである。

**つまり case/08 の 2 次発散は forge の欠陥ではなく case の config の誤り**だった。
1 次で通っていたのは、風上 1 次が正帰還を減衰させていたため。
§4.33 の「原因は未特定」「[[node-slip-spurious-flow]] が本命」は**どちらも撤回**する。

**残る課題**: 入口を直しても**残差はプラトー** (1.2〜1.4 桁) で `PASS` しない。
`convMethod: 0` は 5.1〜5.3 桁通るので、**2 次固有の頭打ちが別にある**。
これは ② の回帰ケースとして使う前に潰す必要がある (§5.1 W1k)。

## 5. 実装ステップ

1. 設計確定 (§4.5 の未決を codex レビューで決める)
2. 実装 (1 案。opt-in の config キーで既定 off から始め、A/B が取れる形にする)
3. §6 の検証
4. 既定化の可否を判断

### 5.1 残作業 (優先順)

| # | 項目 | 内容 |
| --- | --- | --- |
| W0 | ~~測定器の座標~~ **完了 (2026-09-19)**: `check_wall_cv_drain.py` を削除し `check_face_reconstruction.py` に置換 (ノード座標・再構成状態の物理性のみ)。§4.5/§4.6 の結論を訂正 |
| W1 | **共通化と範囲限定まで完了 (2026-09-19) → §4.8 設計 / §4.15 実装**。`reconIncrement_d.cuh` に増分の唯一の定義を置き、流束とリミッタが同じ関数を呼ぶ。周期・化学種・受動スカラーへの波及を遮断し、config で node + convMethod 0/1/2 に限定。**可否判定は保留** — A/B に使っていた 2D ベース診断は float レベルでカオス的で生存 step が指標にならないと判明 (§4.15)。**W1c が前提** |
| W1e | ~~有界性を判定できる測定器~~ **完了 (2026-09-19) → §4.20**。`space.limiterDiag: 1` でリミッタ カーネル内に pass3 を足し、流束と同じ共有関数で再構成した値の逸脱を変数ごとに数える。**後処理の G1 はほぼ全部アーチファクトだった** — 生産既定は `ro` 8478 / `Uy` 21460 で破っており、②③ Barth はいずれも**全変数 0** |
| W1f | ~~② の基準値を restart で継承する~~ **完了 (2026-09-19) → §4.25 / §4.27**。明示指定キー + 貼り付け用ログ行を実装。**一致ゲートを実測した結果、固定しても縮まない** (連続 vs 分割 1.995e-5 / auto 1.670e-5) = ドリフトは forge の restart 非忠実性に埋もれる。基準値 ×2 でも解は 2.1e-4 しか動かない。**既定化のブロッカーから外す** (checkpoint 継承は不実装) |
| W1g | ~~格子細分~~ **完了 (2026-09-20) → §4.30**: Sod 100/200/400 で ② は全解像度 `ro` 逸脱 0・収束次数 0.82/1.04 (① は 0.76/1.00)。**標準ケース回帰は §4.31 で実施不能と判明** (case/08 が現行バイナリで退行、case/20・13 は cell のみで対象外)。§4.22 の 4 ケースは「node + `limiter: 2` の既存 run がある case」であって [`procedures/verification/README.md`](../../procedures/verification/README.md) の標準検証ケースではない |
| W1h | ~~周期経路の pass3 診断が無い~~ **完了 (2026-09-19) → §4.26**。`limiter_g1_check_periodic_d` を追加し、許容幅に絶対床 `1e-6·q_ref` を敷いた (恒等 0 の変数で float ノイズを逸脱と数えていた)。**副産物: Sod で ② が ① より悪いと判明 → `venkatK` は 0.05 にすべき** |
| W1i | ~~case/08.bump の退行を直す~~ **追跡完了 (2026-09-20) → §4.33**。**退行ではなく自由流保存の修正**だった (IC は厳密に一様なので `rms_roUy≈0` が正しく、保管 run の 21.708 がスプリアス。`pRef` でさらに 13 倍改善)。`convMethod: 0` + `pRef` なら **PASS (5.1〜5.3 桁)**。**ただし 2 次以上は ①/② どちらでも発散** → [[node-slip-spurious-flow]] が本命で本 plan の対象外 |
| G4 | **目的量の SU2 クロスチェック** (① vs ② `mr1/scaled1/K=0.05`)。§4.32 修正後に、同一メッシュ・対応する物性/組成/SST/熱的 BC で比較する。**許容差・反復ノイズ・格子誤差を先に固定**し、主張する ~0.025 % を判別できる不確かさまで下げる。全残差の収束判定と、推力・質量収支・壁圧・摩擦の定常性判定を併記する。**SU2 に近いことだけを真値の証明にしない** (codex 2026-09-20 result M4) |
| W1j | ~~現在仕様の同期~~ **完了 (2026-09-20)**。[`methods/limiter.md`](../../methods/limiter.md) に 「既定の ε² は次元が合っていない」「無次元化 Venkatakrishnan (`limiterScaled: 1`, K=0.05 推奨, h_i の軸対称/平面の選択)」「比の形は棄却」「評価点 `limiterMatchRecon`」「有界性の診断 `limiterDiag` (絶対床・非有限・NO-CHECKS)」「面単位フォールバック (カウンタは復帰前に数える)」を追記 |
| W2c | **W2 の未完了項目** (codex 2026-09-20 result M8)。復帰前/復帰後/節点自体が不正、を別々に計数する。速度の非有限検査。対応設定 (node + SLAU/SLAU2、S2/S3・受動種・凝縮) の明示。カウンタ期限切れ試験 |
| W1k | **case/08 の 2 次プラトーを潰す** (§4.34)。入口 BC を直すと発散は止まるが残差が 1.2〜1.4 桁で頭打ちする (1 次は 5.1〜5.3 桁)。**node + 2 次で収束する標準ケース**が要るので、ここを通さないと ② の標準回帰が開かない |
| W2b | **MLP (vertex neighbourhood ステンシル) は別 plan** → §4.19。接続は host の `vizCONNE` に在るが device 未配線、**3D では 2-ring が代用にならない** (体対角 6.93 個が漏れる)、コストは ~234 MB / pass1 4.1 倍 (2.37 M 節点)。③ で `ro` は既に 0 なので、残る `Ux`/`Uy`/`P` の改善を**判定できる測定器**ができてから |
| W1c | ~~A/B の土台を作り直す~~ **完了 (2026-09-19) → §4.16**。ベース無しの安定ケースで 4 構成とも GATES PASS。**`ro` で Barth + `mr:1` のみ厳密有界 (0)**、Venkat は `mr` を変えても不変 (~6600) = **W1 単独では生産既定は直らない**。目的量は Venkat + `mr:1` で **+0.54 %** 動くので G4 の SU2 クロスチェックが要る |
| W1b | **② `limiterScaled: 1` + `limiterMatchRecon: 1` + `venkatK: 0.05` が唯一の採用候補**。根拠: Sod の K スイープ (§4.26)・格子細分 (§4.30)・SERN の再測定 (§4.32)。**③ 比の形は棄却** (§4.22: case/44 で残差床が 2〜5 倍)。**既定化はしない** — 下の G4 / W1f / W1g が未了 |
| W2 | ~~面単位の非物理フォールバック~~ **完了・負の結果 (2026-09-19) → §4.24**。V1 発火の実在は PASS (ベースのみ 5〜6 面、対照 0)、V2 既定不変も PASS (ノイズ床以下) だが、**V3 で `badReconFallback` 20/5 とも step 28 NaN = フォールバック無しと同じ step**。非物理な再構成は駆動要因でなく症状。コードは **opt-in (既定 0) で残置** |
| W3 | **更新率制限と点ロールバックは別 plan へ**。forge には既に `update_d.cu` の `updateGuardScale` があり、[`accepted/time_integration-update-positivity-geard.md`](../accepted/time_integration-update-positivity-guard.md) に「試験した CFL 上限を改善しなかった」実測がある。SU2 の `MAX_UPDATE_FLOW` をそのまま移植する設計では、流れ 5 変数の後に別途更新される SST・化学種・凝縮との整合 (ΣρY=ρ、組成依存 EOS、周期共有 DOF、TP のエネルギー基準) を失う。**3 件一括の既定 on は推奨されない** |
| W4 | ~~壁ノードからの速度再構成を制限する~~ **撤回 (§4.5)**。再構成は片側勾配の理論値に厳密一致しており異常でない |
| W5 | **タイトル・索引・§7 の同期** (codex m1)。`plans/README.md` の説明が旧方針のまま。`update_d.cu` が §7 に無い |

**ベース発散の原因は未確定のまま残す** (codex Major 7)。「未解像が主因」と断定するのは早い —
§4.5 で SU2 の検査が発火することが分かった以上、W2 の A/B を局所格子試験より**先に**行う。
[`tooling-nozzle-sern-3d.md`](tooling-nozzle-sern-3d.md) R4e にもそう書き戻す。

## 6. 検証

**W1 (リミッタ評価点)** — 主眼は「有界性が実際に効くようになること」と「解が壊れないこと」:

1. `tools/check_wall_cv_drain.py` 系の面再構成診断で、**辺中点での再構成が近傍 min/max を外れる面が 0** になること
   (現行はカウル後縁で毎 step 負圧。`case/46` の 2D で測る)。
2. 既存 run の非退行: `check_field_regress.py --boundary` をノイズ床基準で。
   **リミッタが強く効くようになるので解は動きうる** — 動いた場合は「どちらが正しいか」を
   SU2 クロスチェック (`procedures/su2-cross-check.md`) で決める。
3. 標準ケース回帰 (`procedures/verification/README.md`)。特に衝撃波を持つケース (`case/05` Sod、`case/36`)。

**W2 (セーフギヤード)** — 主眼は「起動が頑健になること」と「収束解を動かさないこと」:

4. 発動回数を出力する (SU2 の `counter_local` 相当)。**収束後は 0 に落ちること**を確認する
   — 収束解で発動し続けるなら解を歪めている。
5. 既存の起動困難ケースで段数・step 数が減るか (`case/46` の梯子、`case/16`)。

許容差は実装案を決めてから数値で宣言する。

### 6.1 レビュー記録 (codex)

| stage | 日付 | 記録 | 判定 | 採否 |
| --- | --- | --- | --- | --- |
| plan | 2026-09-19 | [2026-09-19-convection-node-wall-reconstruction-plan.md](../../notes/reviews/2026-09-19-convection-node-wall-reconstruction-plan.md) | NO-GO, C1/M6/m1 | **全件採用**。C1 → 測定器の座標を修正し §4.5/§4.6 の結論を訂正 (対照はクリーン・ベースのみ非物理再構成 = SU2 の検査は発火する)、M2 → 「1 次は一方弁」を撤回 (SLAU の質量流束に圧力差項)、M3 → SU2 比較を構造一致に限定 (等温壁・勾配・リミッタ係数は未比較)、M4 → W3 を別 plan へ (既存 `updateGuardScale` と連成状態)、M5 → W1 に周期経路と 3 次を含める、M6 → 有界性ゲートを Barth と Venkat で分ける、M7 → ベース原因は未確定のまま残し W2 を格子試験より先に、m1 → 索引と §7 を同期 |
| plan | 2026-09-19 | [2026-09-19-convection-node-wall-reconstruction-plan-2.md](../../notes/reviews/2026-09-19-convection-node-wall-reconstruction-plan-2.md) | NO-GO, C1/M7/m1 | **全件採用** (§4.12–§4.14)。C1 → 診断が NaN を見逃す件を再現して修正 (有限性必須・欠落と検査 0 を不合格・終了コード)、M2 → 時相を分離 (`--state-from prev`) し G1/G3 を分離、**G1 は未確立として保留**、M3 → ε̂²=(K h_i/L_ref)³ (h_i は半径重み前) を採用・SU2 の K をそのまま移植しない、M4 → 局所絶対値スケーリングを不採用にし固定物理参照 (ρ_ref, p_ref 絶対圧, a_ref) へ・式の約分と 0/0 NaN も潰す、M5 → 「排出が止まる」を撤回 (DRIFTING)、M6 → 非退行を「未確定」に、M7 → 共有増分関数と対象変数を W1b の前に確定、m1 → 検証の数値基準を先に固定 |
| plan | 2026-09-19 | [2026-09-19-convection-node-wall-reconstruction-plan-3.md](../../notes/reviews/2026-09-19-convection-node-wall-reconstruction-plan-3.md) | NO-GO, C2/M7/m1 | **全件採用** (根拠は自分で `ファイル:行` を確認した)。**C1** → 周期 node は `limiterPeriodic_d.cuh:115` が旧 `venkata_limiter(...,volume)` を呼び `limiterScaled` を読んでいなかった → **周期経路に ②/③ を配線し** (`limiterPeriodic_d.cuh` + `limiter_d.cu` の wrapper) case/05・case/09 を再実施 (§4.22)。**C2** → pass3 の評価点が `matchRecon` に依存し `mr0` だけ双対面重心を測っていた (node 流束は `convectiveFlux_d.cu:69` で無条件に辺中点) → **評価点を流束側の条件だけで決め、非有限を別枠で計数、`sides==0` を `NO-CHECKS(FAIL)`、最大逸脱倍率も記録**して A–E を再測定 (§4.20)。結論は変わらず、むしろ **W1 が `Ux`/`P`・W1b が `ro`/`Uy` を直す**と分離できた。**M3** → 「② が厳密有界」を撤回 (局所極値 δp=0 で ψ=ε²/(2δm²+ε²)>0、反例を §4.20 に明記)。**M4** → SU2 `RefElemLength`=**1.0** (`CConfig.cpp:5021`) を確認し §4.22 の `L=0.1`・閾値 3.5e-4 を撤回、SU2 対応は「無次元増分に平滑化項を足す構造が共通」に限定し採用理由から外した。**M5** → 格子細分の省略を撤回 → §5.1 W1g。**M6** → 基準値が restart ごとに変わる件を §4.25 に記録し、`limiterRoRef/PRef/ARef` を**明示指定できるよう実装** (>0 が優先、自動時は警告)。継承と一致ゲートは W1f。**M7** → 「標準ケース回帰完了」を撤回 (標準は 08/20/13/48)、case/36 を**同一 step 数・基準値固定**で再実施 → §4.22。**M8** → W2 の「全変数が自動で整合」を撤回 (S3 は `speciesTransport_d.cu:802`、凝縮は `condensationTransport_d.cu:300` で別経路)、速度の非有限未検査と「節点不正には効かない」を §4.23 に明記。**M9** → §6.4 の V1/V2/V3 を差し替え (生存 step を合格条件から外し、非物理面数・有限性・全残差・定常性へ)、カウンタ単位を流束評価回数と明記 → §4.24 の結論も非物理面数ベースに書き直した。**m1** → §5.1 の W1b 行を現行契約に統一 |
| **result** | 2026-09-20 | [2026-09-20-convection-node-wall-reconstruction-result.md](../../notes/reviews/2026-09-20-convection-node-wall-reconstruction-result.md) | NO-GO, C1/M8/m1 | **全件採用** (根拠は自分で確認した)。**C1 (実バグ)** → `A_planar` は `variables.cpp:610` の `isAxisymmetric == 1` の中でしか転送されないのに、`limiter_d.cu` は**配列の存在だけで選んで**いた。平面 2D では未初期化メモリを読み **ε²=0 → `venkatK` が一切効いていなかった** (`run_0275`/`0278`/`0279` の `limiter_*` がビット同一で確認)。**選択を `isAxisymmetric` で決めるよう修正**し、h_i≤0/非有限を数えて警告を出すようにした。**§4.20/§4.21/§4.26 の SERN の ② の数値を §4.32 で全部取り直した** — 結論はむしろ強化 (K=0.05 で `ro`/`Ux`/`P` が 0)。**M2** → `limiterScaled > 0` に `limiterMatchRecon: 1` を必須化 (通常経路は mr0 で無視するのに周期経路は適用しており契約が割れていた)。**M3** → `test_periodic_limiter.cu` を現行シグネチャに追従させ、mr1/scaled1 × convMethod 1/2 の試験を追加。**ビルド・実行して PASS**。**M4** → G4 を「① vs ② (mr1/scaled1/K=0.05)」として §5.1 に独立行で起票。許容差・ノイズ・格子誤差を先に固定する要求も明記。**M5** → W1f の主張を「このケース・4000 step・流れ変数では分離できなかった」に限定 (`a_ref` は 1.41e-3 動き、`Q0_0` は 3.892e-4、`model: none` なので SST 説明は未確認)。既定化時の継承必須を復活。**M6** → 診断が**窓集計で終了 flush が無い**のを修正 (`CUM ...` を追加)。§4.30 の合格範囲を「Sod の流れ方向細分」に限定。**M7** → 「step 0 ビット一致」を撤回 (`rms_roUy` 21.708 vs 0.000201。`rms_ro` だけ見た)。**複製時に禁止 `bndFirstOrder: 1` を落とし忘れていた**ことも記録 (外しても結論は不変)。case/20・13 の恒久免除も撤回。**M8** → W2 のカウンタは**フォールバック前**に数えていると明記し、主張を「run 全体の発散を防げなかった」に限定。未完了項目を W2c に起票。**M9** → §5.1 を現行判断だけに整理、`methods/limiter.md` 同期を W1j に起票。**m1** → 貼り付け用ログを 1 キー 1 行の有効な YAML に修正 |

## 7. 影響範囲

`cuda_forge/convection/convectiveFlux_*.inc.cuh` / `convectiveFlux_common_d.cuh`、
`cuda_forge/limiter_d.cu` + `limiterPeriodic_d.cuh` (W1 は周期経路も対象)、
`cuda_forge/update_d.cu` (W3 を別 plan に出すまでの参照先: 既存 `updateGuardScale`)、
`input/solverConfig.cpp` (新キー)、`solver_density_cuda/tools/check_face_reconstruction.py`。
**node の 2 次以上の全計算に影響する** — SERN に閉じない。

## 8. 完了条件

- W1: リミッタの評価点が再構成点と一致し (通常・周期・次数すべて)、解の変化が宣言した許容内
- W2: 面単位フォールバックの発火回数が収束後 0 に落ち、ベース最小再現への効果が測れている
- 標準ケース回帰が宣言した許容内 (対象ケースは実装前に具体名で固定する)
- 既定化するかどうかの判断が plan に書かれている

## 9. 変更ログ

- `2026-09-19` — 起票。case/46 R4e の原因調査から分離。測定器 `check_wall_cv_drain.py` を追加。
- `2026-09-19` — **codex plan レビュー NO-GO (C1/M6/m1) を全件採用**。**測定器が `CELLS/centCoords` を使っていたが
  実行時はノード座標に置換される**ため、§4.5/§4.6 の数値と結論が無効だった。ツールを
  `check_face_reconstruction.py` に置換して訂正。**対照 (ベース無し) は 40 step 非物理な再構成ゼロ、
  ベース run は step 6 から発生** = SU2 の `bad_recon` はベースで発火する → W2 を格子試験より先に。
  「1 次は一方弁」も SLAU の圧力差項で成り立たないので撤回。
- `2026-09-19` — SU2 v8.5.0 のソースを読み、当初の仮説を棄却 (§4.4)。壁ノードの扱い
  (強制 Dirichlet・連続の式は自由・MUSCL の特別扱い無し) は SU2 も forge と同型で差が無い。
  forge の再構成値は隣接の半分の 0.70 倍 = **SU2 が出す値より小さく異常でない**。
  SU2 の 3 つのセーフガードもこの排出を止めない (20 % 上限に掛かるのは 27 step 中 5 step だけ)。
  **確認された唯一の実質差はリミッタの評価点**。plan の主題をそちらへ移し、ベース発散は
  `tooling-nozzle-sern-3d.md` R4e (未解像) に戻した。
