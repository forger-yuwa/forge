# 影アキュムレータの横展開と、残差側の丸めの帰属

## メタ

- **area**: `time_integration`
- **status**: `draft`
- **related_docs**:
  - [`methods/time_integration/implementation.md`](../../methods/time_integration/implementation.md) の「commit の丸め — 定常解の到達限界を決める」
- **related_plans**:
  - [`plans/accepted/time_integration-fp64-accumulator.md`](../accepted/time_integration-fp64-accumulator.md) (v1a。本計画の発注元。2026-09-24 に `accepted/` へ移動)
  - [`plans/active/case-hypersonic-gap-heating-validation.md`](case-hypersonic-gap-heating-validation.md) (発見の経緯)
- **created**: `2026-09-24`
- **owner**: gap-heating セッション

## 1. 目的

v1a ([`time_integration-fp64-accumulator.md`](time_integration-fp64-accumulator.md)) は
**定常陰解法 (`tI=11`, `unsteady=0`)・node・軸対称なし・周期なし・`sstEnergyIncludesK=0`** に
範囲を絞って閉じた。本計画はそこで**意図的に外した 3 つ**を引き取る:

1. **拒否したままの経路**を外す (軸対称・SST 交換エネルギー・周期・陽解法・dual-time)
2. **残差側の丸めの帰属**を決める (v1a で運動量だけ未解決のまま残った)
3. **深部の量の定常化** (v1a の合否から外したもの。ユーザ決定、v1a §6.0a)

## 2. スコープ

- **やる**: 上の 1〜3。および v1a で保留にした double-float の再評価条件の判定。
- **やらない**: 面流束**評価**そのものの精度向上 (相殺の構造を変える話で、SLAU の定式化に踏み込む)。
  本計画は「総和を FP64 にすれば済むのか」までを決める。

## 3. 関連 docs と前提

- v1a の §4.3 (影アキュムレータの設計)・§4.4 (拒否リストと理由)・§6.1 (正本ゲート表)。
- v1a の実測: §5.2 (4 残差の `E_order`/`E_eval`)・§6.1 の G2/G3 実測表。
- **前提**: v1a の実装 (`cuda_forge/qAccumulator_d.cuh`、両 commit の FP64 分岐、
  `updateVariablesOuter_d` への reconcile 融合、起動時の拒否リスト) が入っていること。

## 4. 設計方針

### 4.1 拒否したままの経路 (v1a の S1b-①〜④)

**いずれも「原理的に無理」ではなく「Qacc をまだ扱っていない」だけ**である (v1a §4.4)。

| 経路 | v1a で拒否した理由 | 要る作業 |
| --- | --- | --- |
| node 軸対称 | `axisymmetricSource_d.cu:312-323` が commit の**基準** `roeN`/`roUyN` を射影し、呼出が commit より前 (`main.cpp:1387`) なので `Q` 側の射影は上書きされて消える。<br>**2026-09-24 に再確認**: 軸ピンの呼出は `main.cpp:1387` の**1 箇所だけ** (`:1687` と `:1773` はコメントアウト)。`applyBlockImplicitCorrection_d` の axis 分岐は**今も `nullptr` で inert** (`update_d.cu:335`。有効化すると block-DPLUR の線形 solve と不整合で Mach~1000 に発散する、と註にある) | `enforceAxisSymmetry_d` に `Qacc` 5 本を渡し、軸セルで `Qacc_roUy=0` と `Qacc_roe -= 0.5·Qacc_roUy²/Qacc_ro` を当てる。`applyBlockImplicitCorrection_d:253` の axis 分岐は現状 `nullptr` で inert なので、有効化するなら同時に対応 |
| `sstEnergyIncludesK=1` | `ransTransport_d.cu:175` の `roe -= (roK-roKref)` は**増分**。陽解法は毎段 `roK−roKN` (`main.cpp:1764`, `fromN=1`) | ⚠ **「陽解法は最終段のみ」では足りない。積分器ごとに補正式を定義する** (codex rollout plan M4)。<br>**定常**: 増分を 1 回 `Qacc_roe -= (double)Δ`。<br>**RK4**: `Qacc` への加算は最終段だけでよいが、**中間段の FP32 補正は省略しない** (`timeIntegration_d.cu:395` が暫定状態を作り `main.cpp:1764` が毎段補正する。省略したり `roe=(flow_float)Qacc_roe` で置換すると**次段の状態を壊す**)。<br>**RK3**: 基準が $a\,Q_N+b\,Q_M$ なので**交換量の基準も $a\,roK_N+b\,roK_M$ に対応させる**。`ransTransport_d.cu:167` の「各 stage が N から組み直す」は**RK3 の式には成立しない**。総エネルギー `roe+roK` の stage 保存式から導出する。<br>**dual-time**: 上の行のとおり**移植しない**。<br>⚠ **周期ミラーは交換補正後の状態も同期する必要がある**。**単独経路の解除で組合せ全体を許可せず、周期 × SST × 時間積分の試験を登録する** |
| node 周期 | **経路が 2 つあり、要る作業が違う** (2026-09-24 にソースで確認し設計を精密化):<br>**陰解法**: ループ中は**状態でなく `dq` を root→member にミラー**する (`periodicMirrorDq_d_wrapper`, `main.cpp:1567`)。状態ミラー (`periodicMirrorNSState`) は**初期化時 `:1222` にしか走らない**。<br>**陽解法**: 保存量更新の直後に**状態をミラー**する (`main.cpp:1762`)。ここは FP32 値だけを配るので member の `Qacc` の下位ビットが root と食い違う | **陰解法は追加のミラーが要らない見込み**: 初期化の順序が `periodicMirrorNSState` (:1222) → `initQAccumulatorFromQ` (:1256) なので **`Qacc` は root/member で同一値から始まり**、同じ `dq` を足すので同一のまま (正値性ガードも `Q_N=(float)Qacc` で同一)。→ **要るのは「本当に一致しているか」を測る不一致カウンタだけ**。<br>**陽解法は `periodicMirrorQacc` が要る** (状態ミラーと一組)。<br>⚠ **「見込み」であって測っていない**。不一致カウンタを入れて **0 であることを実測してから**、陰解法の周期を拒否リストから外す |
| 陽解法 `tI=1`/`tI=4` | 未配線なだけ | `tI=1` は `Q_N + res·dt/v` で commit と同型。`tI=4` は**最終段 (`loop==3`) のみ** `Q_N + res_m` が commit で、中間段は捨てる暫定値 |
| 陽解法 `tI=3` | commit の基準が**別配列の FP32 値** (`Q_N`/`Q_M`) の**凸結合** | ⚠ **「FP64 影を shift するだけ」は誤り** (2026-09-24, codex rollout plan M2 を自分で再現)。<br>RK3 の係数は **float32 に格納**されており (`solverConfig.hpp:268` が `flow_float`)、`coef_N[2]=1/3`・`coef_M[2]=2/3` を FP64 に昇格すると**和が `1.0000000298023224`** になる。残差ゼロ・初期値 1 で 100 step 回すと **1.0000029802366353**、FP32 ミラーが **25 ULP** 動く = **定数状態が保存されない**。<br>**要る設計**: (i) **係数を FP64 で持つ** (ii) **定数状態を保存する差分形** $Q_{\rm stage} = Q_{acc,N} + b\,(Q_{acc,M}-Q_{acc,N}) + c\,dq$ にする。`N` は外側 step・`M` は stage の履歴で、**単なる同時 shift ではない**。<br>**必須試験**: 残差ゼロでの定数保存、sub-ULP 増分、時間収束次数 |
| dual-time | commit が**独立の FP32 `Q += dq`** (`update_d.cu:501`)、BDF 項も FP32 の現在値と履歴を読む (`:460`)、しかも **`updateVariablesOuter` を通らない** (`main.cpp:1838`) ので**今の reconcile が効かない** | ⚠ **履歴の影を足すだけでは機能しない** (codex rollout plan M3)。<br>**要る設計**: 現在反復値・物理履歴・FP32 ミラーの役割、各 subiteration の writer と reconcile の順序、**BDF 時間差分をどの精度で評価するか** (影を読むかミラーを読むかで**解く離散時間方程式が変わる**) を明文化する。checkpoint (`output.cpp:185`) も `flow_float` 履歴なので再開契約が未定。<br>⚠ **`sstEnergyIncludesK=1` の dual-time は既に BDF に `roK` を含み交換補正を呼ばない** (`main.cpp:1929`)。**定常用の補正を移植してはいけない**。<br>**必須試験**: BDF1/2、再開、subiteration 数依存 |

**実使用の分布** (全 3172 run、v1a §4.4): `tI=11 unsteady=0` 2131 / `tI=11 dual-time` 456 /
**`tI=3` 516** / `tI=4` 49 / `tI=1` 14。**`tI=3` (516) と dual-time (456) で 972 run** なので、
「陽解法に対応する」と言うなら `tI=3` を含めないと誤解を生む。

### 4.1a 累積していない保存量 (**v1a はここを扱っていない**)

v1a の `Qacc` は**流れの 5 本だけ** (`ro, roUx, roUy, roUz, roe`)。次は float32 のまま:
**乱流 `roK`/`roOmega`・化学種 `roY_s`・凝縮モーメント・受動トレーサ `roXi`・遷移モデル `roGamma`/`roReth`**。
v1a はこれらを**拒否せず**、起動時に列挙して警告するだけにした (`case/56` では
全域 FP64 ビルドと物理量が 0.00–3.77 % で一致しており実害が見えなかったため)。

**決めるべきこと**:
1. **吸収が起きているのか** — 深部 z/W>10 の 1 step で `roK` が動く CV は 0.28 % だが、
   **出力の Δ は丸めた後の値 (0 か 1 ULP 以上) なので意図増分が分からず判定できない**。
   **意図増分 (`res_roK·dt/D` 等) を診断出力に足す**のが先。
2. **混在が悪さをするか** — 流れだけ収束が進んで乱流が止まると不整合が出るのか、
   それとも `case/56` のように無害なのか。**乱流が効くケース** (`case/26.flat_plate_sst`,
   `case/48`) で ON/OFF と全域 FP64 の 3 者比較をする。
3. **横展開する場合の設計** — `roK`/`roOmega` は `applySSTPointImplicit` で別カーネル、
   化学種・凝縮・トレーサも別。**commit の形が `Q = Q_N + dq` かを経路ごとに確認**してから配線する
   (v1a で陽解法 `tI=3` が凸結合で別物だった前例がある)。

### 4.2 残差側の丸めの帰属 (v1a で未解決)

v1a §5.2 の実測 (深部 z/W>10、16607 CV):

| 残差 | `E_order` (再実行の変動) | `E_eval` (FP32/FP64 の評価差) | 比 |
| --- | --- | --- | --- |
| `res_ro` | 1.111e-17 | 2.915e-11 | 3.8e-07 |
| **`res_roUx`** | 6.728e-09 | 2.133e-08 | **0.315** |
| **`res_roUy`** | 5.583e-10 | 1.743e-09 | **0.320** |
| `res_roe` | 3.640e-12 | 1.747e-05 | 2.1e-07 |

~~質量・エネルギーは並べ替え上界で総和丸めを除外できる~~ **撤回 (codex result-2 M4)**。
再実行差の最大値は共通誤差の上界にならない (反例: `[1e-3, 1e-11, -1e-3, 1e-17]` は順序で 0 と 1e-17 だが
**どちらも真値から ~1e-11 ずれる**)。**全保存量について総和丸めは未除外**であり、
**固定面流束の FP32/FP64 総和比較は 5 本すべてで行う**。

⚠ **`E_order` は再実行の変動しか測らない**ので、同じ順序で毎回出る総和の丸めは現れない
(反例: `[1e8, 1, -1e8]` と `[-1e8, 1, 1e8]` は**どちらも FP32 で 0**、厳密和 1)。
**だから比 0.32 は「誤差寄与率」ではない**。

**決める方法** (2026-09-24 に設計を直した。codex rollout plan M5):

⚠ **「FP64 総和で `E_order` 級の差が消えるか」では決まらない**。**固定順なら再実行差は FP32 総和でも消える**ので、
それは総和精度の判定になっていない。また**対流以外を固定しないと粘性残差の `atomicAdd` が交絡**する。

⚠ **既存の `massflux` は再利用できない**: `convectiveFlux_roe_d.inc.cuh:399` の `massflux` は**散逸項を含まず**、
直後に加算する質量残差と**違う量**。`convectiveFlux_keep_d.inc.cuh:574` も `advGauge` 使用時は異なる。

**正しい設計**: `atomicAdd` に渡す**直前の実際の符号つき寄与を 5 本とも保存**し、
**同じ状態・幾何・面集合**について次を**別々に**測る:

| 測る量 | 意味 |
| --- | --- |
| $S_{32}(F_{32}) - S_{64}(F_{32})$ | **同じ FP32 面流束**を FP32 で足すか FP64 で足すかの差 = **総和の精度** |
| $S_{64}(F_{32}) - S_{64}(F_{64})$ | 面流束の値そのものの精度 |
| 固定順 $S_{32}$ と atomic $S_{32}$ の差 | 順序依存の成分 |

**5 保存量すべてで行う** (質量・エネルギーを除外する根拠だった並べ替え上界は撤回済)。
**粘性・境界・周期の寄与も固定する**か、対流だけを分離できる構成で測る。

### 4.3 gather の選択肢と費用

| 案 | 中身 | 得るもの | 代償 |
| --- | --- | --- | --- |
| FP64 `atomicAdd` | 残差配列を double に (sm_60 以降ハード対応) | 総和誤差が相対 1e-16 級へ | 残差配列 2 倍。**決定論にはならない** |
| 面流束保存 + 固定順 gather | `cell.iPlanes`/`iPlanesDir` (`mesh.hpp:41-42`) と device CSR (`mesh.cpp:872`) が既にある | **ビット再現性**。run 間ノイズが消える | 対流 5 変数で概ね 16 B/面 + 1 パス。面並列の連続読みが散在読みに変わる |
| 圧力の基準差分 | 面の圧力力から局所基準を引く。現状 `space.pRef` は**既定 0 で実効的に何も引いていない** | 総和以前の相殺が減る | 面の反対符号共有・閉性・軸対称ソースとの整合を壊しうる |

⚠ **対流だけ決定論化しても run 間ノイズは消えない**: `viscousFlux_d.cu:379` と
`periodicNode_d.cu:27` にも残差への `atomicAdd` がある。

### 4.4 double-float の再評価条件

v1a §4.3a で**見送った**。理由はメモリ半分が成立しないこと (検出のために上位を別に持つと
FP64 影と同じ 8 B/変数、`hi==Q` にすると `dependentVariables` の commit 前書き込みで黙って壊れる)。
**再評価する条件は「速度の実測で FP64 加算が効いていると分かったとき」だけ**。
v1a の G5 は `case/56` (65k CV) で差が区別できなかったので、**代表的 3D 規模で測り直したとき**が判断点。
試作 (`cuda_forge/qAccumulatorDF_d.cuh`) と試験 (`tests/unit/test_qacc_df.cu`) は残してある。

## 5. 実装ステップ

### 5.1 残作業 (優先順)

| # | 担当 | 項目 | 内容 |
| --- | --- | --- | --- |
| 0 | F | **累積していない保存量の扱い** (§4.1a) | **最優先**。(a) 意図増分を診断出力に足して `roK` 等が吸収領域にいるか判定 (b) 乱流が効くケースで「v1a ON / OFF / 全域 FP64」の 3 者比較 (c) **横展開の条件は「目的量への影響とその帰属」**にする (~~吸収していれば横展開~~ は #6c 自身の結論と矛盾する。**吸収だけでは問題と判断できない**)。必要なら「流れのみ FP64」と「流れ + 対象スカラー FP64」の対照を足す。<br>**`case/56` で無害だったことは「乱流を含む全ケースで無害」の証拠ではない** — このケースは深部がほぼ静止しており乱流が効いていない可能性がある。**平板 SST の結果から化学種・凝縮・遷移モデルまで「無害」と確定してはいけない** (codex rollout plan M6)。<br>⚠ **診断では「更新前値・緩和後増分・丸め後値・ピン/clamp 理由」を分けて出す**。SST の実際の増分は `update_d.cu:385` の対角・緩和を含み、さらに壁ピンと非負化がある。**「意図増分」だけではそれらによる停止と丸め吸収を混同する** |
| 1 | F | **残差側の帰属を決める A/B** | §4.2 のとおり面流束を固定して FP32/FP64 総和を比較。**5 保存量すべてを測る** (質量・エネルギーを除外する根拠だった並べ替え上界は誤りだったので撤回)。<br>**事前登録**: FP64 総和で `res_roU*` の `E_order` 級の差が消えれば **A** (gather の精度が効く) / 消えなければ **B** (面流束値の評価が効く)。⚠ ~~実用影響が無いことは v1a G2 で確認済み~~ **一般化しすぎ** (codex rollout plan M8): G2 は**対象量・領域を限定した**結果である。`U_rms_deep` は両者 `STEADY` で **4.02 倍差**があり、これが工学的に許容できるかは**絶対許容値なしには決まらない** |
| 2 | O | **軸対称** (v1a S1b-①) | §4.1。合格条件: v1a の反例 (ρ=1, ρu_r=0.125, ρe=2, dq=0 → `Q` と `Qacc` の**両方**で ρu_r=0, ρe=1.9921875) を単体試験に追加して PASS。`case/44` で採用カウンタ 0、ON/OFF の場差が事前登録の許容内 |
| 3 | O | **SST 交換エネルギー** (S1b-②) | §4.1。合格条件: RK 反例 (roKN=8、各段 9/10/11/12 → 正しい補正 −4、全段累積の −10 でないこと) の単体試験 |
| 4 | O | **周期** (S1b-③) | §4.1。**陰解法と陽解法で要る作業が違う** (陰解法は不一致カウンタだけ、陽解法は `periodicMirrorQacc`)。<br>**合格条件**: (i) 陰解法で**不一致カウンタが全 step 0** — これが 0 なら「同じ初期値 + 同じ dq」の理屈が実証される。0 でなければ理屈が間違っているので**ミラーを入れる** (ii) 陽解法も同様に 0 (iii) `case/09` と**翼列 (`case/53`)** で確認。<br>**これが済むと翼 CHT の ON/OFF 判別ができるようになる** (#6c が待っている) |
| 5 | O | **陽解法 `tI=1`/`tI=4`** (S1b-④) | §4.1。`unsteady=1` も許可 (陽解法に BDF 履歴は無い)。合格条件: `case/09 run_0046` 設定で ON/OFF の場距離 ≤ OFF の run-to-run ノイズ床 (**両側測定**)、採用カウンタ 0 |
| 6 | F | **`tI=3` と dual-time** | §4.1 の「履歴配列と並べて FP64 影を shift」。**970 run が該当**するので優先度は低くない。費用は履歴 1 段につき +40 B/CV |
| 6b | F | **`U_rms_deep` の 4 倍差** | 「ノイズ床」と説明したのは撤回済 (codex result-3 M2)。**深さ別に分解すると全域 FP64 でも速度二乗和の 99.78 % が $4<z/W\le5$ に集中**し、両系列ともその帯で `STEADY`。**正味流束ゼロは速度 RMS ゼロを意味しない** (往復流)。→ **深さ別の定常流と数値誤差を分離する**。運動量 gather (§4.2) が候補 |
| 6d | O | ~~実測との一致が変わるか~~ **済 (2026-09-25)。変わらなかった** | ユーザ指摘「試験結果があるケースでやってくれてたんじゃないのか」。**それまでの「一致」は全部コード同士** (全域 FP64 ビルド / SU2) で、**実測とは一度も比べていなかった**。<br>参照値が数表である唯一のケース `case/50` ([W70] Table IV) で ON/OFF を 100k step 回した (`run_0050_acc_{off,on}`、seed md5 同一)。<br>**結果: 変わらない**。後壁 $q_w/q_{fp}$ の深部 ($x/d\ge0.15$) は **OFF/ON とも 1e-4 級で符号が振れ**、実験値 0.02–0.07 の **2〜3 桁下**のまま。開口面積平均 **0.582 → 0.581** (実験 1.07)。<br>→ **`case/50` 深部の食い違いは commit の丸めではない**。深い帯の吸収率が **7.4 %** しかなく (`case/56` は 99.8 %)、床に当たっていなかった。<br>⚠ **`case/56` (TP-1187) は 2D では実測と定量比較できない**と 2026-09-22 に決定済み (衝突支配で 3D 必須、発端 plan #47)。**したがって本計画は実測に対する改善を一度も示していない**。示せたのは「倍精度ビルドという独立な基準との一致」と「場が収束するようになったこと」まで |
| 6c | O | ~~過去の生産 run の棚卸し~~ **一次調査 済 (2026-09-24)。結論は「問いの立て方が違った」** | ユーザ質問「昔の結果も桁落ちしていないか」。道具を作って 6 case で測った (`solver_density_cuda/tools/check_commit_absorption.py`。収束済みの場から 1 step 回し `dq_block_old_*` を `extraFields` で出して $\lvert dq\rvert/\mathrm{ULP}(Q)$ を見る。`run_*/run_0900_absorb`)。<br>**最悪の帯で $\lvert dq_\rho\rvert<\tfrac12$ULP だった CV の割合**:<br>`case/56` TP-1187 すきま **99.8 %** (中央値 0.146) / `case/53` C3X 翼 CHT **93.5 %** (0.177) / **`case/48` 冷却平板 92.2 %** (0.189) / `case/55` M5 乱流すきま 52.4 % (0.473) / `case/50` W70 深キャビティ 22.6 % (4.537)。<br>**⚠ 健全なはずの `case/48` も 92.2 %**。→ **吸収は例外でなく、float32 で「収束した」run の normal な姿**である (収束すれば定義上 $\lvert dq\rvert$ は小さくなる)。<br>**したがって「吸収しているか」は問いとして弱い**。正しい問いは「**吸収する前に物理の答えに着いていたか**」で、判別には **ON/OFF で答えが動くかを見るしかない**。`case/56` は動いた (深部の絵が変わった)、`case/48` は ON/OFF 差がノイズ床の約 2 倍 (G3 の `d(B,C)`)。<br>**⚠ 翼 CHT (`case/53`/`54`) は判別できない**: **周期境界を使うので v1a が ON を拒否する** (S1b-③ 待ち)。→ **#4 (周期) が済むまで保留**。<br>**残**: (i) ON/OFF 判別を case ごとに回す (安い) (ii) 壁近傍の吸収率も出す (`case/53` は壁距離 1e-5 未満で 80.2 %) (iii) `check_commit_absorption.py` の VERDICT は「吸収の有無」でなく**ON/OFF 判別への入口**だと文言を直す |
| 7 | F | **深部の量の定常化** | v1a が合否から外したもの (§6.0a)。全域 FP64 でも `DRIFTING` なので、**まず「到達可能な量なのか」を決める**。到達不能なら指標を変える |
| 8 | O | ~~`mdot_decay.py` の量の混在~~ **済 (2026-09-24)** | 保存量 `roUy` を直接積分する形に修正。`run_0026/res_400000` で 5.903983e-10 → 5.880652e-10 (**0.40 %**)、減衰率の結論は不変。`signed=True` も足した (床付近は符号が零をまたぐので絶対値だけ見ると「張り付いている」ように見える) |
| 9 | O | ~~3D 規模での G5~~ **済 (v1a 側で実施)** | `case/49` hex 100 万節点、5 対、片側 95 % 上限 **+2.05 %**。G6 の 3D は未実施 |
| 9b | O | (旧 #9 の G6 部分) | v1a は `case/56` (65k CV) のみ。**代表的 3D 規模**で速度とメモリを測る。これが §4.4 の double-float 再評価の判断点になる |
| 10 | F | codex レビュー (plan 段) | §4 の設計方針と §6 の検証計画が書けた時点、実装着手前 |

## 6. 検証

### 6.0 検証表 — **実装前に固定する** (2026-09-24、codex rollout plan M1)

**2 種類のゲートを分ける**。混ぜると「意図的に答えを変える機能」の正しさと後方互換が区別できなくなる。

- **R (回帰)**: 導入前バイナリ vs 導入後 **OFF**。**答えが変わっていないこと**を見る。
- **A (改善)**: 導入後 **OFF vs ON**。**精度と保存性**を見る。ON は設計上答えを変えるので、R とは別条件。

| 項目 | 対象 run | 観測量 | 比較区間 | 合格条件 (**絶対値で**) | 反復 | 判定不能条件 |
| --- | --- | --- | --- | --- | --- | --- |
| **#0 診断** | `case/26.flat_plate_sst` (乱流が効く)、`case/48` | `roK`,`roOmega` の**更新前値・緩和後増分・丸め後値・ピン/clamp 理由**を分けて出力 | 1 step | 「丸めで消えた」「緩和で小さい」「ピンで止まった」を**別々に数えられること** | — | 出力が揃わない |
| **#0 対照** | 同上 | 壁熱流束・$y^+$・`k`/`omega` のピーク | 収束端 | 「流れのみ FP64」vs「流れ + 乱流 FP64」で**壁熱流束の相対差 $\le$5 %** | 各 3 本 | run 間ノイズが 5 % を超える |
| **#1 帰属** | `case/56` (既存の場) | §4.2 の 3 つの差 | 1 step | 総和精度と面流束精度の**寄与が 3 倍以上離れていること** (離れなければ帰属しない) | 3 本 | 3 つとも同オーダー |
| **#2 軸対称 R** | `case/44` | 保存量 5 本の maxabs/scale | 2000 step | 導入前 vs OFF $\le$ 同一バイナリ内のばらつき (平均+3σ、**各群 4 本**) | 4+4 | within の変動係数 >0.5 |
| **#2 軸対称 A** | `case/44` | 軸上 CV の `roUy` | 収束端 | **厳密に 0** (射影が効いていること)。採用カウンタ 0 | 1 本 | — |
| **#3 SST R/A** | `case/26`, `case/48` | R: 保存量 / A: $\Sigma(roe+roK)$ の変化 | 2000 step | R は #2 と同じ規則。A は**総和の変化が FP64 残差増分和と丸め桁で一致** | 4+4 / 1 | 同上 |
| **#4 周期 R/A** | `case/09`, `case/53` | R: 保存量 / A: **root/member 不一致カウンタ** | 2000 step | R は #2 と同じ。A は**全 step で 0** | 4+4 / 1 | — |
| **#5 陽解法 R** | `case/09 run_0046` (tI=4) | 保存量 | 2000 step | #2 と同じ規則。⚠ **ON/OFF 差をノイズ床と比べるのは R ではない** (A に回す) | 4+4 | 同上 |
| **#6 RK3 A** | 解析解のある case | **残差ゼロでの定数保存** / 時間収束次数 | — | 定数状態が **0 ULP** 動かない。収束次数が 3 次を保つ | 1 本 | — |
| **#6 dual-time A** | `case/09` (BDF2) | 物理時間精度・保存量・subiter 内部収束 | 物理 10 step | BDF2 の時間 2 次を保つ。subiter 数依存が OFF と同等 | 各 3 本 | — |
| **#7 深部** | `case/56` | `zW844`/`zW1411`/`U_rms_deep` | 400k | **絶対許容値・観測期間・未達時の扱いを先に決める** (⚠ v1a で「到達不能と見えてから指標を変えた」ので、今回は先に固定する) | — | 先に決めた期間で未達なら**未達のまま記録** |

**全項目に共通する固定**: 同一 seed (md5 で確認)・**同一 SHA のバイナリ**・実効設定の diff を貼る・
メッシュ品質 VERDICT・`check_convergence` と `check_quasisteady` の VERDICT を貼る。
**CPU (`gpu != 1`) と cell は拒否を維持する** — **「拒否リストが空」を完了条件にしない**。

### 6.1 レビュー記録 (codex)

| stage | 日付 | 記録 | 判定 | 対応 |
| --- | --- | --- | --- | --- |
| plan | `2026-09-24` | [2026-09-24-time_integration-fp64-accumulator-rollout-plan-2.md](../../notes/reviews/2026-09-24-time_integration-fp64-accumulator-rollout-plan-2.md) | **NO-GO**, C0/M6/m2 | **全件採用。実装にはまだ入らない。**<br>**M6 診断ツールの baseline** → **直した**。更新後の `Q` に `dq` を足し直しており「今回消えたか」でなく「もう一度足すと消えるか」を測っていた (反例: 更新前 $Q=1-2^{-24}$, $dq=2^{-24}$ の実更新は $Q_{new}=1$ で吸収していないが誤判定する。自分で再現)。<br>⚠ **`roN` も baseline にならないと実測で分かった**: 出力の直前に `updateVariablesOuter` が `roN = ro` で上書きするので `res_1.h5` では **`roN == ro` が全 CV で成立**。→ **`--baseline res_0.h5`** (前 step 末尾の出力) を使う形にした。直しても数字はほぼ不変 (`case/48` 92.2 %)。<br>**M1 検証表がまだ未完成** → #7 の許容値が未記入、RK3 の対象が「解析解のある case」で刻み幅・誤差ノルム・次数許容が未定、dual-time が BDF2 のみで BDF1/restart が無い、`tI=1` の試験と `tI=4` の A と RK3/dual-time の R が無い、周期×SST×積分器の組合せが未登録、R の集計方法とゼロばらつき時の扱いが未定。**「全項目で同一 SHA」は R と矛盾** (R は旧・新の 2 つの SHA が要る)。<br>**M2 dual-time** → 問題を列挙した段階で設計が決まっていない。残作業 #6 も旧来の「影を shift」のまま。**BDF2 の差分形・reconcile の呼出順・再開仕様**を固定する必要がある。<br>**M3 RK3 の SST 契約** → 交換式が「導出する」のままで式が無い。`K_stage − [K_N + b(K_M−K_N)]` と明記し、Qacc の stage・FP32 暫定値・履歴確定・周期同期の順を固定する。検証表の `Σ(roe+roK)` は**体積重みを欠く**。<br>**M4 #0 の 5 %** → 影響調査の結論を先に合格条件にしている (乱流累積で目的量が正しく大きく変わった場合に完了できない)。**分岐ゲート**にする。`case/26` の断熱壁は `qwall` が**厳密に 0** (200 点すべて 0 を実測) なので熱流束の相対差は使えない。$y^+$ も出力 `ypls` でなく第一内部ノード基準にする。<br>**M5 帰属実験** → §4.2 で否定した判定が残作業 #1 に残っている。「3 倍以上離れること」も成功条件にならない (同程度なら**それが結果**)。結論を「総和優勢/評価優勢/混在/測定不成立」に分ける。**既存の全域 FP64 ビルドは幾何精度も変えるので流用すると交絡**。<br>**m1/m2** → gather の費用表の本文が修正記録と不一致 (まだ「相対 1e-16」「16 B/面」)。<br>**codex が支持した点**: R/A の分離、RK3 の差分形 (CPU 再現で残差ゼロ 100 step 後も 1.0、一定増分 $2^{-27}$ で $100\times2^{-27}$ を保持)、`sstEnergyIncludesK` の dual-time に定常用補正を移植しない修正 |
| plan | `2026-09-24` | [2026-09-24-time_integration-fp64-accumulator-rollout-plan.md](../../notes/reviews/2026-09-24-time_integration-fp64-accumulator-rollout-plan.md) | **NO-GO**, C0/M8/m2 | **全件採用**。<br>**M1 検証計画が無い** → **§6.0 に検証表を新設**。R (回帰: 導入前 vs OFF) と A (改善: OFF vs ON) を**別ゲート**にし、経路ごとに対象 run・観測量・区間・**絶対値の合格条件**・反復数・判定不能条件を固定。完了条件から「拒否リストが空」を外し **CPU/cell は拒否維持**と明記。<br>**M2 RK3** → **設計が誤りだった**。係数は float32 格納で `1/3+2/3` を FP64 昇格すると **1.0000000298**、残差ゼロ 100 step で **25 ULP** ずれる (自分で再現)。**FP64 係数 + 定数保存する差分形**に変更。<br>**M3 dual-time** → commit が独立 (`update_d.cu:501`)、BDF が FP32 を読む (`:460`)、**`updateVariablesOuter` を通らない** (`main.cpp:1838`) ので reconcile が効かない。**一体で設計**すると明記。`sstEnergyIncludesK=1` の dual-time に定常用補正を**移植しない**。<br>**M4 SST** → **積分器ごとに補正式を定義**。RK4 は中間段の FP32 補正を省略しない、RK3 は基準が $a\,roK_N+b\,roK_M$ で「各 stage が N から組み直す」は成立しない。周期 × SST × 時間積分の試験を登録。<br>**M5 帰属実験** → 「`E_order` 級の差が消えるか」では決まらない (固定順なら FP32 でも消える)。**3 つの差を別々に測る**形に変更。**`massflux` は Roe/KEEP で質量残差と違う量**なので再利用しない。<br>**M6 #0 の分岐** → 「吸収していれば横展開」は #6c の結論と矛盾。**「目的量への影響とその帰属」**に変更。診断は**更新前値・緩和後増分・丸め後値・ピン/clamp 理由を分けて**出す。<br>**M7 診断ツールの欠陥** → **直した**。ULP 比でなく**実際に float32 で足して値が変わるかの直接検査**に (`Q=1, dq=-4e-8` は比 0.336 だが値は変わる。2 のべき乗境界で下向き ULP が半分)。増分ゼロを別列に分離。取り直しても順序は不変 (`case/50` 22.6→19.9 %、`case/53` 93.5→89.2 %)。<br>**M8 「実用影響なし」** → 一般化しすぎ。ON/OFF は**感度検査**であって物理解への到達を保証しない。`U_rms_deep` の **4.02 倍差**は絶対許容値なしには判断できない。#7 は**指標を先に固定**する。<br>**m1 gather の費用** → 「相対 1e-16 級」は相殺後の保証にならない。5 本で 20 B/面。**CV ごとに FP64 レジスタで gather** すれば残差配列全体の FP64 化は不要。圧力基準差分は既存の圧力ゲージ計画へ接続。<br>**m2** → v1a のリンクを `accepted/` へ、run 数を **972** に訂正、3D 速度は実施済みに同期 |

## 7. 影響範囲

- `solver_density_cuda/cuda_forge/` の `axisymmetricSource_d.cu` / `ransTransport_d.cu` /
  `periodicNode_d.cu` / `timeIntegration_d.cu` / `update_d.cu`
- `solver_density_cuda/main.cpp` の起動時拒否リスト (経路を外すたびに縮む)
- `case/56.gap_tp1187/tools/mdot_decay.py` (#8)

## 8. 完了条件

- [ ] §6.0 の検証表の各項目が、そこに書いた**絶対値の合格条件**を満たしている (**#0 を含む**)
- [ ] 起動時の拒否リストから、**検証表で R と A の両方を通した経路だけ**を外す。**CPU と cell は拒否を維持する** (「空になる」は完了条件ではない)
- [ ] `methods/time_integration/implementation.md` の現在仕様が対応範囲を反映している
- [ ] codex レビューを plan 段と result 段で受け、指摘の採否を §6.1 に記録している

## 変更ログ

- `2026-09-24`: 起票。v1a ([`time_integration-fp64-accumulator.md`](time_integration-fp64-accumulator.md)) が
  範囲を絞って閉じるにあたり、外した経路・未解決の帰属・合否から外した量を引き取る。
